## 2026-03-01 01:16 SoA移行計画 Step 1 + 全体 revert

**実施内容**
- SoA配列への移行計画（VarData を min/max/size の唯一の正規ソースにする）の Step 1〜7 を全実装し、152テスト全パス
- ユーザーからベンチマークで微妙に遅くなったとの報告を受け、全変更を revert
- Step 1（直接ドメイン操作の排除）のみ再実装：
  - `count_eq.cpp`: `xi->domain().remove(target_)` → `xi->remove(target_)` (2箇所)
  - `int_element_monotonic.cpp`: `index_var->domain().remove_below/above()` → `index_var->remove_below/above()` (4箇所)
  - `constraint_registry.cpp`: `b->domain().remove()` / `x->domain().remove()` → `b->remove()` / `x->remove()` (5箇所)
- `Variable::remove()` の戻り値変更を試み（`!domain_.empty()` 返却）→ 3テスト失敗 → `Domain::remove()` が既に「空になるなら除去せず false 返す」セマンティクスだったため、変更不要と判明し revert

**設計判断・理由**
- Step 2〜7 は propagation 中の Domain min_/max_ キャッシュ更新の省略が目的だったが、ベンチで性能劣化 → VarData bounds チェック追加や Variable accessor の非 inline 化がオーバーヘッドに
- Step 1 は純粋なリファクタリング（sync_soa() 呼び出しを保証）で性能影響なし
- `Domain::remove()` は既に「空になるなら false」を返すので `Variable::remove()` の戻り値変更は不要

**気づいた課題・TODO**
- Step 2〜7 の性能劣化原因: Variable accessor の非 inline 化が主因の可能性 → inline を維持したまま VarData 委譲できる設計が必要
- `count_eq.cpp` 466行目は旧パターン（`xi->remove()` + `xi->domain().empty()` チェック）のまま → `if (!xi->remove()) return false;` に統一可能

**関連ファイル・コミット**
- `9bb2726` (既にコミット済み): Step 1 の変更
