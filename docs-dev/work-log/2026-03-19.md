## 2026-03-19 AllDifferentExcept0 伝播拡張 + テスト追加

**実施内容**
- `AllDifferentExcept0Constraint` を `AllDifferentConstraint`（non-GAC）と同等の伝播強度に拡張
  - `on_instantiate` に非0確定値の除去ループを追加
  - `check_hall_pair` を実装（0を含むドメインは Hall pair 候補から除外）
  - `on_remove_value` / `on_set_min` / `on_set_max` を追加（Hall pair 検出）
  - `bump_activity` を追加（非0値の重複時のみ focused bump）
  - `prepare_propagation` に `set_no_bisect` を追加
- Hall pair の発火を確認する FlatZinc テスト3件を追加
  - `hall_pair.fzn`: 非0 Hall pair `{1,2}` による刈り取り確認
  - `hall_pair_with_zero.fzn`: Hall pair 刈り取り後も 0 が取れることを確認
  - `hall_pair_zero_skip.fzn`: `{0,v}` ドメインが Hall pair として扱われないことを確認

**設計判断・理由**
- Hall pair 検出で `{0, v}` を除外: 0は重複可能なので Hall pair の前提が成り立たない
- `bump_activity` で `val == 0` はデフォルト動作: 0の重複は制約違反ではないため
- テストは `int_search` で探索順を固定し、出力順を決定的にした

**気づいた課題・TODO**
- `rewind_to` に `override` キーワードが付いていない（既存コード、今回のスコープ外）

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`
- `src/core/constraints/global/all_different_except_0.cpp`
- `tests/fzn/constraints/alldifferent_except_0/hall_pair{,_with_zero,_zero_skip}.{fzn,expected}`

## 2026-03-19 14:20〜14:25 fzn_all_equal_int の実装

**実施内容**
- `fzn_all_equal_int` / `all_equal_int` 制約をconstraint_registryに登録
- int_eq チェインへの分解（vars[0]=vars[1], vars[0]=vars[2], ...）で実装
- is_defined_var ヒューリスティック: 配列中の非defined変数のうち1つだけ残し、残りを全て is_defined_var に設定

**設計判断・理由**
- ネイティブ AllEqual クラスは作らず int_eq 分解で対応（all_equal は int_eq の連鎖と等価で、専用伝播の利点が薄い）
- is_defined_var は最初に見つかった非defined変数を「残す」方式（探索対象として1つだけ残し、残りは伝播で決まる変数として扱う）

**気づいた課題・TODO**
- `tests/fzn/constraints/all_equal/unsat.expected` が `==========` になっているが、UNSAT問題なので `=====UNSATISFIABLE=====` が正しいはず（既存の問題）

**関連ファイル**
- `src/fzn/constraint_registry.cpp` — make_all_equal 追加 + 登録

## 2026-03-19 14:30〜14:35 all_equal テスト expected ファイル修正

**実施内容**
- `unsat.expected` を `==========` → `=====UNSATISFIABLE=====` に修正
- `basic.expected` / `single.expected` / `with_fixed.expected` を実際のソルバー出力に合わせて修正
  - 解の値を探索順に合わせて更新（5→1）
  - `==========` 終端を追加

**設計判断・理由**
- expected ファイルが satisfy モードのソルバー出力と一致していなかった（`==========` 終端の欠落、探索順変更による解の値の不一致）

**関連ファイル**
- `tests/fzn/constraints/all_equal/*.expected`
