## 2026-03-28〜29 作業

**実施内容**
- improvement probe の target 修正
  - minimize 時に `obj_lb + 5%`（下端寄り）→ `obj_ub - 5%`（上端寄り = best objective 側）に変更
  - ベンチマーク結果: 46問改善 vs 24問悪化（pattern4 vs baseline）、largescheduling -75%、gbac -80%、p1f-pjs が OPTIMAL 獲得
- int_eq_reif カウントによる初期 activity 設定を試行
  - 閾値 0.5, 0.9, dom_size-1 の3パターンをベンチマーク
  - いずれも baseline に対して明確な優位性なし → revert
- Lévy flight による多様化を3方式で試行
  1. 値選択の摂動（levy_perturb_ フラグ）: 40改善/39悪化だが OPTIMAL 喪失3件・解消失7件
  2. activity への直接ノイズ加算: ノイズが固定される問題
  3. select_linear 内での一時的ノイズ: ベスト変数の activity にノイズを加えて「守る」方式
  - いずれも pattern4 に対して悪化 → 全て revert
- bench_compare.py の修正
  - verify_solution で fzn-gecode プロセスが残留する問題を修正（`_run_checker` + `kill_process_tree` + `proc.wait()`）
  - CHECK_FAIL の解を judge_winner で UNKNOWN 扱いにして不正な解でスコアを稼がないようにした（MiniZinc 2.9.5 バグ対応）

**設計判断・理由**
- probe target: 下端寄りだと UNSAT で少しずつ下限を上げるだけ。上端寄りなら SAT で大きく改善、UNSAT で下限カット
- Lévy flight revert: 値選択摂動は OPTIMAL 喪失が深刻、activity 摂動はノイズ固定問題やベスト選択への干渉でリグレッション。探索多様化は現在のリスタート+activity decay で十分カバーされている可能性

**気づいた課題・TODO**
- MiniZinc 2.9.5 の -O1 最適化バグ（エイリアス置換時の制約消失）は未報告
- 停滞時の多様化は Lévy flight 以外のアプローチ（LNS 的な部分破壊+再構築）を検討する価値あり

**関連ファイル**
- `src/core/solver.cpp` — probe target 修正、verify_solution 強化
- `benchmarks/minizinc_challenge/bench_compare.py` — checker プロセス管理修正、CHECK_FAIL 対応
