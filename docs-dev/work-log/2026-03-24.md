## 2026-03-24 07:00〜07:45 FlatZincパーサ修正

**実施内容**
- FlatZinc パーサの変数エイリアス宣言対応（`var 0..10000: objective :: output_var = X_INTRODUCED_1111_;`）
  - `VarDecl` に `alias_target` フィールド追加
  - `parser.yy` に `= identifier` の文法ルール3つ追加（range, int, bool）
  - `model.cpp` の `to_model()` でエイリアス解決処理を追加
  - 対象問題: `mznc2015_probs/mapping/mapping.mzn` + `ring_2.dzn`
- FlatZinc パーサの混合配列引数対応（`[2, X_INTRODUCED_103_]`）
  - `constraint_arg` から `'[' int_list_inner ']'` ルールを削除し、`id_list_inner` に統一
  - `resolve_int_array` に `vector<string>`（`__inline_N` 形式）のハンドリング追加
  - 対象問題: `mznc2014_probs/fillomino/fillomino.mzn` + `5x5_1.dzn`

**設計判断・理由**
- 変数エイリアス: MiniZinc が生成する FlatZinc で `var X: name = other_var` パターンが使われるため。既存の `bool2int` エイリアスマップと同じ `add_variable_alias` を再利用
- 混合配列: Bison LALR(1) の reduce/reduce conflict で `int_list_inner` が優先され、混合配列がパースできなかった。`id_list_inner` は既に int/bool リテラルの混在に対応済みだったため、そちらに統一

**気づいた課題・TODO**
- bump activity のカスタマイズが上手くいかない制約について、bump しない選択肢の検討（議論のみ、未実装）

**関連ファイル**
- `include/sabori_csp/fzn/model.hpp` — VarDecl に `alias_target` 追加
- `src/fzn/parser.yy` — 文法ルール追加 + `int_list_inner` 削除
- `src/fzn/fzn_build_context.cpp` — `resolve_int_array` 拡張
- `src/fzn/model.cpp` — エイリアス解決追加

## 2026-03-24 08:00〜16:50 制約追加・cumulative修正・ベンチマークスクリプト修正

**実施内容**
- `bool_lt_reif` / `int_lt_reif` 制約実装
  - `IntLinLeReifConstraint([1,-1], [x,y], -1, b)` に分解、新クラス不要
- cumulative 制約の dur=0 ポイントタスク対応（Gecode互換）
  - FlatZinc仕様上は dur=0 はリソース消費なしだが、Gecode/Chuffed はカウントする
  - MiniZinc Challenge の問題がこのセマンティクス前提のため互換対応
  - TimeTabling で `[est, est+1)` のポイントイベント追加
  - 対象問題: `mznc2019_probs/stack-cuttingstock/d3`
- bench_compare.py の verify_solution 修正
  - Gecode クラッシュ時（stderr に "Error:" なし）に Chuffed フォールバックが効かず false CHECK_FAIL になるバグ修正
  - 対象問題: `mznc2020_probs/collaborative-construction/37`
- 疑似勾配の value ordering 簡略化
  - candidate_indices 収集を廃止、先にシャッフルしてから swap する方式に変更

**設計判断・理由**
- cumulative dur=0: FlatZinc仕様とGecode実装の乖離を確認。CP-SATでも検証し、Gecode互換を選択
- CHECK_FAIL: Gecode の TypeError クラッシュで stderr パターンが期待と異なり、フォールバックパスに到達しなかった
- value ordering: シャッフル後にswapすることで candidate_indices の一時 vector 確保を除去

**気づいた課題・TODO**
- bump activity のカスタマイズが上手くいかない制約について、bump しない選択肢の検討（議論のみ、未実装）

**関連ファイル・コミット**
- `src/fzn/constraint_registry.cpp` — bool_lt_reif / int_lt_reif 追加
- `src/core/constraints/global/cumulative.cpp` — dur=0 ポイントタスク対応
- `src/core/solver.cpp` — value ordering 簡略化
- `benchmarks/minizinc_challenge/bench_compare.py` — CHECK_FAIL フォールバック修正
