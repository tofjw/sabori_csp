## 2026-02-22 work-task-variation presolve ハング調査・修正・選別リバート

**実施内容**
- `work-task-variation.fzn` の presolve ハング問題を調査
  - `strace -c` で 9023 回の `brk` syscall（実行時間の 98.45%）を特定
  - 原因: bounds-only ドメインに対する `domain().values()` + `std::set` による大量ヒープ確保
- 以下の修正を実施（最終的に残したもの）:
  - `comparison.cpp`: IntEqConstraint / IntEqReifConstraint / IntNeReifConstraint の presolve で `is_bounds_only()` ドメインに対し bounds intersection を使用（`domain().values()` を回避）
  - `count_eq.cpp`: presolve 内の `domain().values()` 列挙ループを `assign()` に置き換え
- 以下の修正は試行後にリバート:
  - solver.cpp: presolve をワークリストベースに変更 → リバート
  - main.cpp: SIGALRM を solver 生成後に遅延 → リバート
  - int_element.cpp: bounds-only ドメイン用の sparse table presolve → リバート
  - global.hpp: IntElementSharedData 共有構造体 → リバート
  - constraint.hpp: `var_ptrs()` アクセサ追加 → リバート
  - solver.cpp: solve_all() の DFS 化 → リバート

**設計判断・理由**
- `strace -c` の `brk` 計測結果に基づき、実測でボトルネックが確認された修正のみ採用
- ワークリストやアラーム遅延等は presolve 自体の計算量削減ではなく回避策だったため、根本原因（`domain().values()` の大量ヒープ確保）への対処のみ残した

**気づいた課題・TODO**
- work-task-variation の presolve は改善されたが、元の naive 固定点ループ自体の効率問題は残存（大量制約での `constraint->variables()` コピー）
- SIGALRM が parser 中に発火する問題（`g_current_solver` が null）は未修正のまま

**関連ファイル**
- `src/core/constraints/comparison.cpp` — IntEq/IntEqReif/IntNeReif presolve 最適化
- `src/core/constraints/global/count_eq.cpp` — assign() への置き換え

## 2026-02-22 constraint->variables() コピー排除

**実施内容**
- `Constraint::variables()` が `std::vector<VariablePtr>` をコピーで返す問題を解消
- 基底クラスに `var_ptrs()` を追加（`vars_` の const 参照を返す非virtual inline メソッド）
- 3箇所の呼び出しサイトを `variables()` → `var_ptrs()` に切替:
  - `src/core/solver.cpp` presolve 固定点ループ（制約数×2×反復回数のコピー→0）
  - `include/sabori_csp/solver.hpp` `bump_activity()`（conflict ごとのコピー2回→0）
  - `src/core/model.cpp` `build_constraint_watch_list()`（一時オブジェクトへの参照→安全な参照）

**設計判断・理由**
- 全サブクラスのコンストラクタで基底 `vars_` に完全な変数リストがセットされていることを確認済み → 基底の `vars_` 参照で安全
- 既存の `variables()` (virtual) は残置し、互換性を維持

**気づいた課題・TODO**
- なし

**関連ファイル**
- `include/sabori_csp/constraint.hpp`
- `src/core/solver.cpp`
- `include/sabori_csp/solver.hpp`
- `src/core/model.cpp`
