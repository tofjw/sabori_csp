# 作業ログ 2026-06-16

## 2026-06-16 Phase 6: solver.cpp ファイル分割（900行目標達成）

**実施内容**
- solver.cpp(1,600行)を関数定義ごとに3 TU へ機械的分配（commit fda9b35）:
  - solver_search.cpp(606): restart 探索ループ系
    (search_with_restart[_optimize] / run_improvement_probe / restart helpers / handle_ascent)
  - solver_frame.cpp(372): 明示スタックのフレーム管理
    (run_search / 値列挙 / 分岐 / create_search_frame / handle_solution)
  - solver_propagate.cpp(253): 伝播エンジン
    (process_queue / propagate_instantiate / apply_unit_nogoods)
- CMakeLists.txt に3ファイル登録。solver.cpp は 414 行へ。

**設計判断・理由**
- file-local static / 匿名 namespace / free 関数が一切無く、全関数が Solver::/Literal::
  メンバ → 宣言(solver.hpp)を共有したまま定義のみを別 TU に移せる。各新 TU は同一の
  include 前文 + namespace ラッパのみ追加。
- 関数境界を完全 partition するスクリプトで切り出し、行の欠落・重複が起きないことを保証。
- record_constraint_call は solver.hpp の inline member template なので全 TU から利用可能。

**気づいた課題・TODO**
- Phase 6 の構造目標は完了。残るは find_all 解列挙設計レビュー（純リファクタでなく設計判断）。
- 次は Phase 5（Domain 表現・性能, 高リスク）。メモの通り Domain表現刷新中心か
  propagator/核ループの性能 ROI 中心かは着手時に再判断。

**検証**
- golden 182 byte一致（探索軌道不変）+ ctest 234/234 + python 100 passed。

**関連ファイル・コミット**
- fda9b35 solver 分割 / src/core/solver{,_search,_frame,_propagate}.cpp, src/core/CMakeLists.txt
- docs-dev/refactoring-plan-2026-06.md

## 2026-06-16 (続き) Phase 6: find_all 解列挙 設計レビュー

**実施内容**
- solve_all の2列挙エンジン（restart+solution NoGood 既定 / 非restart DFS Enumerate）を
  レビュー。restart+NoGood 経路の完全性・重複なし・終了性を評価。
- 独立オラクル照合テスト test_find_all_consistency.cpp 新設（commit 8eaa808）:
  restart on/off の2エンジンが同一解集合 + 重複ゼロ + 解析的解数一致を REQUIRE。
  カバー: unit NoGood 縮退 / all_different 順列(6・24) / 定数変数除外 / 線形等式 /
  presolve 一意確定 / UNSAT。ctest 241/241。
- レビュー文書 docs-dev/find-all-enumeration-review-2026-06-16.md 作成。

**設計判断・理由**
- golden は restart 経路を「自己出力との byte 一致」でしか検証せず、独立オラクルが無いのが
  検証ギャップ。非restart DFS（自明に完全）を参照オラクルにして2エンジン照合する形にした。
- 「重複報告ゼロ」を直接 REQUIRE することで、2026-06-10 の root 確定変数 watch 無限ループ
  （同じ解を再発見し続ける）型の事故を恒久ガード。
- 唯一の設計上の制約はメモリ/伝播コストの O(#解) スケール（permanent NoGood が解ごとに蓄積・
  GC されない）。これは正しさでなく性能トレードオフ。対処（find_all 時 DFS 既定化）は golden
  再録 + 解順変更を伴うためユーザー判断待ちの将来項目に分離した。

**気づいた課題・TODO**
- Phase 6 のレビュー項目はクローズ。Phase 6 は構造目標・レビュー全完了。
- 残るは Phase 5（Domain 表現・性能, 高リスク）のみ。
- 将来: 高解数の全解列挙が実問題になれば find_all を DFS 既定化（要 golden 再録 + 解順確認）。

**関連ファイル・コミット**
- 8eaa808 find_all consistency test / tests/cpp/test_find_all_consistency.cpp
- docs-dev/find-all-enumeration-review-2026-06-16.md, docs-dev/refactoring-plan-2026-06.md

## 2026-06-16 (続き2) Phase 5: Domain 設計衛生（純リファクタのみ実施・Phase 5 クローズ）

**実施内容**
- Phase 5 着手時の再判断: 再プロファイル(profile-20260614) + ユーザー方針で
  「設計衛生の純リファクタのみ、性能最適化は別イニシアチブへ分離」と確定。
- 死コード5ファイル削除(commit c2dfedf): SparseDomain/BoundsDomain
  (sparse_domain/bounds_domain.{hpp,cpp}) + それらからしか include されない var_data.hpp。
  全ブランチの全履歴で一度も CMakeLists 未登録の放棄された Domain 分割の試み。
  計画が想定した「Domain/SparseDomain/BoundsDomain のフラグ分岐」は実在しなかった
  （ライブ実装は Domain 1クラス + 内部 bounds_only_ 分岐のみ）。
- removed_set_ メンバシップ判定の DRY 化(commit ac8527b): bounds-only の
  find(v)==end() パターン6箇所を private inline is_removed() に一本化。

**設計判断・理由**
- removed_set_ の unordered_set→ビットマップ化は見送り(確定): bounds-only はレンジ>10000
  専用で上限ガード無し、巨大レンジ(例 0..2^31)でビットマップ即確保はメモリ激増(〜268MB/変数)。
  遅延確保の set の方が最悪ケース安全。自動選択はフラグ分岐増で衛生目的に逆行、profile 上 ROI も低い。
- BoolDomain / variant 化 / remove_value O(n) / rewind virtual は純リファクタでなく性能最適化。
  profile-20260614 の結論(性能 ROI は propagator 本体[IntLinEq 差分/TTEF]と核ループ定数倍
  [process_queue/set_min-max])より別イニシアチブへ分離。

**気づいた課題・TODO**
- リファクタ計画 v2 (Phase 0〜6) 全クローズ。残る性能最適化は計画枠外の独立タスクとして再起票する。
- (注) commit 時 git add -A で untracked ジャンクを巻き込み reset --mixed でやり直した。
  以降 domain 等の限定コミットはパス指定で add すること。

**検証**
- 各 commit でビルド green + golden 182 byte一致 + ctest 241/241。

**関連ファイル・コミット**
- c2dfedf 死コード削除 / ac8527b is_removed DRY
- docs-dev/refactoring-plan-2026-06.md（Phase 5 クローズ追記）

## 2026-06-16 (続き3) テスト不足の解消: golden 監査機構 + 死テスト復活 + 高リスク制約のテスト追加

**実施内容**
- **golden master に corpus 監査を追加**し Error 黙殺による死テスト化を恒久対策:
  - `tests/golden/excluded.txt`（理由付き除外マニフェスト、新規）。正当な除外は2件のみ
    （`int_lin_eq_subst/unsat_{eq,le}.fzn` = presolve UNSAT を "Error:" で出す設計、test_subst.sh で検証）。
  - `run_golden.sh` に `audit` サブコマンド + `check` 冒頭の `audit_corpus`: `tests/fzn/constraints/`
    配下の全 .fzn は corpus か excluded のどちらかに必ず属すことを強制し、漏れは大声で fail。
  - `build_corpus` の Error 除外を黙殺廃止し `[excluded]`/`[UNACK!!]` で報告。
- **死テスト発覚・復活 — int_element_monotonic 6件**: commit 4e65369 で追加されたが
  `array_int_element(idx, [10,20,...], result)` の inline 整数リテラル配列が HEAD パーサ未対応で
  "Error:" を出し golden に黙殺され**一度も実テストされていなかった**。名前付き配列
  `array [1..N] of int: arr = [...];` 形式（array_bool_element と同形）へ書き換えて復活。
  SAT/UNSAT/境界（重複・定数配列・逆伝播）を実テスト化。.expected も -a 出力で再生成。
- **高リスク未テスト制約の追加**:
  - `int_one_hot_channel`: C++ ブルートフォース等価テスト新設（test_one_hot_channel.cpp、8ケース
    /310 assertion）。exhaustive/partial 被覆・固定bool・UNSAT2種・単集合・非連続値・300ランダム照合。
    solver 解集合と独立オラクルが一致（false UNSAT なし）。
  - `int_lt_reif`: FZN テスト4件（SAT / x==y境界で b=0 / off-by-one で b=1 / UNSAT）。
    strict と int_le_reif の差を境界で検証。
- **connected 死コード削除**: `src/core/constraints/global/connected.cpp`（クラス宣言なし・CMake未登録・
  未コンパイル・registry未配線のコンパイル不能な放棄 WIP）と対の `share/minizinc/fzn_connected.xmzn`
  を削除。

**設計判断・理由**
- 死テスト化は「黙って除外」が根本原因。corpus を「全 fzn を漏れなく分類する」自己監査型に変え、
  除外には必ず理由を残す（excluded.txt）構造にした。これで新規破損・Error 退行・stale corpus が
  すべて check で検出される。
- int_element_monotonic は parser 修正（inline 整数リテラル配列対応）でなくテスト書き換えで対処。
  MiniZinc は named array を出すため実害が小さく、parser 変更は高リスク。limitation として記録のみ。
- int_one_hot_channel は presolve aggregator 経由で生成される制約だが、直接 add_constraint 可能なので
  propagator を単体で隔離してブルートフォース照合（find_all consistency と同じ独立オラクル方針）。
- connected はテスト対象として実在しない（コンパイル不能）ため、テスト追加でなく削除が正しい対処。

**気づいた課題・TODO**
- **parser limitation**: `array_int_element` が inline 整数リテラル配列 `[10,20,...]` を未サポート
  （named array 宣言は可）。MiniZinc 出力は named なので優先度低だが記録。
- **残ギャップ（②監査の未着手分）**: UNSAT 欠落14制約（all_different_except_0 / disjunctive_strict /
  bool 系等）、境界欠落多数（比較系・int_times overflow 等）。
- 最初の probe が stale バイナリ（HEAD と別状態でビルド）で誤誘導された。clean rebuild で真の挙動判明。

**検証**
- clean rebuild → ctest 248/248 pass、Python 100/100 pass。
- golden: corpus 192（元182 + 復活6 + int_lt_reif 4）、audit OK、全 green。

**関連ファイル・コミット**
- tests/golden/{run_golden.sh, excluded.txt, corpus.txt, expected/*}
- tests/cpp/test_one_hot_channel.cpp, tests/cpp/CMakeLists.txt
- tests/fzn/constraints/int_lt_reif/*, tests/fzn/constraints/int_element_monotonic/*
- 削除: src/core/constraints/global/connected.cpp, share/minizinc/fzn_connected.xmzn
- memory/golden-audit-and-dead-tests.md
