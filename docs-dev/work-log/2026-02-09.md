## 2026-02-09 IntLinEqConstraint::presolve 高速化

**実施内容**
- `Domain::remove_below(threshold)` / `Domain::remove_above(threshold)` を追加（dense配列1回スキャン + update_bounds 1回）
- `Variable::remove_below` / `remove_above` を追加（Domain一括除去 + sync_soa 1回）
- `IntLinEqConstraint::presolve()` を書き換え: `values()` + 逐次 `remove()` ループ → `remove_below` / `remove_above` に置換
- 全104テスト通過を確認

**設計判断・理由**
- `values()` のヒープ確保+コピーと、逐次 `remove()` による k回の `update_bounds()` O(n) が presolve のボトルネック（ATSP instance14 で60秒中46秒）
- 一括除去メソッドにより k個除去のコストが O(k×n) → O(n) に改善
- 既存の `remove()` API には手を加えず、新規メソッド追加のみで後方互換性を維持

**気づいた課題・TODO**
- `IntLinLeConstraint::presolve()` にも同様のパターンがあれば同じ最適化が適用可能
- ATSP ベンチマークでの実際の効果計測がまだ（presolve が秒単位で完了するか確認必要）

**関連ファイル**
- `include/sabori_csp/domain.hpp`
- `src/core/domain.cpp`
- `include/sabori_csp/variable.hpp`
- `src/core/variable.cpp`
- `src/core/constraints/global/int_lin_eq.cpp`

## 2026-02-09 remove_below/remove_above の全制約への展開

**実施内容**
- `IntLinEqConstraint::presolve()` 向けに追加した `remove_below`/`remove_above` を、同様のパターンを持つ全制約の `presolve()` に展開
- 対象制約（9種）:
  - `IntLtConstraint`, `IntLeConstraint`, `IntLeReifConstraint`
  - `IntMaxConstraint`, `IntMinConstraint`
  - `IntTimesConstraint`, `IntAbsConstraint`
  - `ArrayIntMaximumConstraint`, `ArrayIntMinimumConstraint`
- `IntTimesConstraint` では x==0/y==0 時の `values()` + ループを `assign(0)` に簡略化
- `array_var_int_element.cpp` は `model.remove_value()` 分岐があり Model レベルのメソッド追加が必要なためスキップ
- 全104テスト通過を確認

**設計判断・理由**
- `values()` によるヒープ確保 + 逐次 `remove()` による O(k×n) コストは全 bounds trimming パターンに共通するボトルネック
- 一括除去に統一することで presolve 全体が高速化し、コードも簡潔になる
- `array_var_int_element` は save_point による分岐が絡むため、Model レベルの一括除去メソッド追加を別タスクとした

**気づいた課題・TODO**
- `array_var_int_element.cpp` の `propagate_bounds()` にも同最適化を適用するには `Model::remove_below`/`Model::remove_above`（Trail付き一括除去）が必要
- `IntEqConstraint::presolve()` / `IntEqReifConstraint::presolve()` は intersection パターン（`values()` + `set` の交差判定）なので `remove_below`/`remove_above` では対応不可

**関連ファイル**
- `src/core/constraints/comparison.cpp` — IntLt, IntLe, IntLeReif, IntMax, IntMin
- `src/core/constraints/arithmetic.cpp` — IntTimes, IntAbs
- `src/core/constraints/global/array_int_maximum.cpp`
- `src/core/constraints/global/array_int_minimum.cpp`

## 2026-02-09 MiniZinc Challenge 2025 ベンチマーク実施 + 未実装グローバル制約調査

**実施内容**
- `bench_compare.py` で Sabori vs CP-SAT ベンチマーク実施（13問題、タイムアウト30秒）
- Sabori は fbd1 のみ OPTIMAL (1.5s)、他は大半 TIMEOUT
- 全20問題の .mzn ファイルから高レベルグローバル制約の使用状況を調査
- FlatZinc レベルでは全27制約が実装済みだが、MiniZinc レベルの多くのグローバル制約が分解されている

**設計判断・理由**
- FlatZinc の制約欠如ではなく、グローバル制約の分解による伝播の弱さと探索爆発がボトルネック
- `table` が4問題で使用されており、分解すると FZN が巨大化（proteindesign12: 30MB、groupsplitter: 17MB）
- ネイティブ実装の優先度を「分解の影響度 × 使用問題数」で整理

**問題と分解されているグローバル制約の対応**

| 問題 | 分解されている制約 |
|------|-------------------|
| black-hole | `table`, `inverse` |
| carpet-cutting | `cumulative`, `diffn` |
| cgt | `strictly_increasing` |
| groupsplitter | `table`, `count` |
| gt-sort | `strictly_increasing`, set変数 |
| hitori | `alldifferent_except_0` |
| ihtc-2024-kletzander | `cumulative`, `count` |
| ihtc-2024-marte | `bin_packing_load`, `cumulative`, `nvalue` |
| is | `table` |
| mondoku | `global_cardinality`, `value_precede_chain` |
| products-and-shelves | `diffn_nonstrict_k`, `seq_precede_chain`, `increasing`, `lex_chain_lesseq` |
| proteindesign12 | `table` |
| stripboard | `diffn`, `disjunctive`, `strictly_decreasing` |
| work-task-variation | `regular`, `global_cardinality` |

※ EchoSched, atsp, fbd1, skill-allocation, tower, tsptw は分解対象のグローバル制約なし

**未実装グローバル制約の実装優先度**

| 優先度 | 制約 | 使用問題数 | 影響度 |
|:---:|---|:---:|---|
| 1 | `table` | 4 | 大。STR アルゴリズムで劇的改善見込み |
| 2 | `cumulative` | 3 | 大。スケジューリングの要 |
| 3 | `diffn` | 3 | 大。2D パッキングの分解は弱い |
| 4 | `global_cardinality` | 2 | 中。フローベース伝播が強力 |
| 5 | `inverse` | 1 | 中。実装容易 |
| 6 | `count` | 2 | 低〜中 |
| 7 | `regular` | 1 | 中。オートマトン伝播 |
| 8 | `alldifferent_except_0` | 1 | 低。all_different の拡張 |

**ベンチマーク結果サマリー**

| 問題 | Sabori | CP-SAT |
|------|--------|--------|
| fbd1 | OPTIMAL 1.5s | OPTIMAL 0.3s |
| atsp | TIMEOUT | OPTIMAL 0.2s |
| groupsplitter | TIMEOUT | TIMEOUT |
| hitori | TIMEOUT | OPTIMAL 0.2s |
| ihtc-2024-kletzander | TIMEOUT | TIMEOUT |
| is | TIMEOUT | OPTIMAL 0.3s |
| mondoku | TIMEOUT | OPTIMAL 1.0s |
| products-and-shelves | TIMEOUT | OPTIMAL 11.1s |
| proteindesign12 | TIMEOUT | OPTIMAL 24.0s |
| skill-allocation | UNKNOWN 1.7s | OPTIMAL 8.4s |
| tower | TIMEOUT | OPTIMAL 20.5s |
| tsptw | TIMEOUT | TIMEOUT |
| work-task-variation | TIMEOUT | TIMEOUT |

**関連ファイル**
- `benchmarks/minizinc_challenge_2025/sabori_benchmark.html` — HTML レポート
- `benchmarks/minizinc_challenge_2025/bench_compare.py` — ベンチマークスクリプト

## 2026-02-09 NoGood 管理: リスタート時 Move-to-Front + FIFO Eviction

**実施内容**
- `NoGood` 構造体に `last_active` フィールドを追加（最後に prune を起こしたカウンタ値）
- `Solver` に `ng_use_counter_` を追加し、prune 発生時に O(1) で `last_active` を更新
- リスタート時に `nogoods_` を `last_active` 降順で `stable_sort` し、`max_nogoods_` 超過分を末尾（冷たい NG）から削除
- `add_nogood()` の O(n) `max_element` ベース eviction を削除（容量管理はリスタート時に遅延）
- `select_variable` を Activity 優先モードに変更（`1||activity_first_`）
- 全104テスト通過を確認

**設計判断・理由**
- 探索中に move-to-front を行うとバックトラックの size ベース cleanup が破綻するため、リスタート時にソートする方式を採用
- リスタートは数〜数十コンフリクトごとに発生するため、一時的な `max_nogoods_` 超過は限定的
- リスタート時の O(n log n) ソートは低頻度なので性能影響なし
- 探索中の `add_nogood()` が O(n) → O(1) に改善

**気づいた課題・TODO**
- ベンチマーク（ATSP 等）での実際の効果計測が必要
- `last_active` が 0 のまま（一度も prune を起こしていない）NG の扱いを再検討する余地あり

**関連ファイル**
- `include/sabori_csp/solver.hpp` — NoGood に `last_active`、Solver に `ng_use_counter_`
- `src/core/solver.cpp` — propagate_instantiate, search_with_restart, add_nogood

## 2026-02-09 TableConstraint (table_int) 実装 + クラッシュ修正

**実施内容**
- Compact Table アルゴリズムによる `TableConstraint` をネイティブ実装
  - `supports[var][val]` ビットセットで各値のサポート行を管理
  - `current_mask` で有効行を追跡、値削除時にビット AND で更新
- MiniZinc 連携: Chuffed パターンで `sabori_table_int` predicate を定義し、`fzn_table_int` から `array1d(t)` で変換
- FlatZinc パーサに `table_int` / `sabori_table_int` を登録
- テストファイル作成（simple, allsol, unsat, three_vars）
- black-hole 問題での heap-buffer-overflow クラッシュを修正
  - 原因: `presolve()` で `dom.begin()/dom.end()` イテレータを使いながら `var->remove(val)` を呼び、Domain の sparse set が変更されてイテレータが無効化
  - 修正: 削除対象を `std::vector` に先に収集してからまとめて削除

**設計判断・理由**
- MiniZinc の `fzn_table_int` は 2D 配列を受け取るが FlatZinc builtin では 2D 不可 → Chuffed と同じパターンでソルバー固有の `sabori_table_int(1D配列)` を定義
- `supports_` はフラット配列（`range * n_words_`）でキャッシュフレンドリーなレイアウト
- Trail は mask のスナップショットコピー（n_words_ ワード分のみ）で軽量

**気づいた課題・TODO**
- `-a` モード（全解探索）でリスタートベースの探索を使うと重複解が出る（table_int 固有ではなくソルバー既存の問題）
- black-hole 問題はクラッシュしなくなったがタイムアウトするのみで解は未発見（パフォーマンス改善の余地あり）

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp` — TableConstraint 宣言
- `src/core/constraints/global/table.cpp` — 実装本体
- `src/core/CMakeLists.txt` — ソースファイル追加
- `src/fzn/model.cpp` — パーサ登録
- `share/minizinc/sabori_csp/fzn_table_int.mzn` — MiniZinc predicate
- `share/minizinc/sabori_csp/redefinitions.mzn` — include 追加
- `tests/fzn/constraints/table_int/` — テストファイル
- `docs/CONSTRAINTS.md` — ドキュメント更新

## 2026-02-09 bool2int をビュー（変数エイリアス）として実装

**実施内容**
- `bool2int(b, i)` を制約（`IntEqConstraint`）ではなく変数エイリアスとして処理するよう変更
- `Model` クラスに `variable_aliases_` マップ、`add_variable_alias()` / `variable_aliases()` を追加
- `Solver::build_solution()` でエイリアスの値も解に含めるよう修正
- `fzn::Model::to_model()` で bool2int をスキャンしてエイリアスマップを構築、int変数の作成をスキップし、制約生成も省略
- 定数ケース（`fixed_value`）は従来通り `IntEqConstraint` にフォールバック
- bool2int テスト expected ファイルの `true/false` フォーマット更新

**設計判断・理由**
- `b`（bool変数）を正規変数とし、`i`（int変数）をエイリアスにした。`b` のドメインは常に {0,1} で最も制約が強いため
- エイリアスは `var_map` に直接登録することで、以降の制約構築で自動的に正規変数に解決される
- 目的関数変数がエイリアス側のケースは非常に稀（{0,1}の最適化は自明）なので初期実装では未対応

**気づいた課題・TODO**
- 複数の FlatZinc テストで expected ファイルが `1/0` だが実際の出力は `true/false`（bool変数の出力フォーマット変更が以前に行われた影響）。bool2int 以外のテスト（bool_not, int_ne_reif 等）の expected ファイルも更新が必要

**関連ファイル**
- `include/sabori_csp/model.hpp`
- `src/core/model.cpp`
- `src/core/solver.cpp`
- `src/fzn/model.cpp`
- `tests/fzn/constraints/bool2int/*.expected`

## 2026-02-09 search_with_restart: NG未獲得時に inner_limit 増加

**実施内容**
- `search_with_restart` のリスタートループで、`run_search` 前に `nogoods_.size()` を記録
- UNKNOWN（リスタート）処理ブロックで NG が増えなかった場合に `inner_limit++`, `outer_limit++` を実行
- 既存の fail ベース調整を NG ベース調整に統合: NG 未獲得時は limit を線形増加、NG 獲得時は Luby 風の乗算増加

**設計判断・理由**
- conflict_limit に達したが NG を1つも学習できなかった場合、同じ制限で再試行しても無駄なリスタートを繰り返す可能性が高い
- limit を増やすことで探索を深くし、NG を獲得できるチャンスを増やす
- fail が0のケースは NG 未獲得のサブセットなので、`limit_changed` フラグで重複適用を防止

**気づいた課題・TODO**
- ベンチマーク（ATSP, black-hole 等）での実際の効果計測が必要

**関連ファイル**
- `src/core/solver.cpp` — `search_with_restart()`

## 2026-02-09 値選択: ハッシュソート→ランダムインデックス選択（実験・未完了）+ hitori プロファイリング

**実施内容**
- 値選択の `std::sort` + `fmix64` ハッシュ → Fisher-Yates 部分シャッフルへの置換を実装・テスト
  - `value_buffer_` メンバ（スタック式再帰共有バッファ）を追加
  - `xorshift64` 軽量 RNG を追加
  - `select_variable` を `SIZE_MAX` 返却方式にリファクタ（全確定チェックの重複排除）
- 全104テスト通過を確認
- ATSP ベンチマーク比較で **大幅な性能劣化** を確認（旧: 62 restarts/6s vs 新: 7 restarts/20s+）
  - xorshift 置換・バッファクリア追加でも改善せず
  - ソートなし・シャッフルなし（自然順序）でも同等に遅い
  - **原因: ハッシュソートの値順序自体が探索効率に大きく寄与しており、単純な除去は探索品質を著しく悪化させる**
- hitori (h11-1) のプロファイリング実施（gprof, 120秒タイムアウト）

**hitori ホットスポット**

| 順位 | % | 関数 |
|---:|---:|:---|
| 1 | 20.9% | `Constraint::count_uninstantiated()` |
| 2 | 15.8% | `Solver::select_variable()` |
| 3 | 12.7% | `BoolClauseConstraint::is_satisfied()` |
| 4 | 7.7% | `ArrayVarIntElementConstraint::propagate_bounds()` |
| 5 | 6.2% | `Domain::values()` |
| 6 | 5.4% | `IntLinEqConstraint::is_satisfied()` |
| 7 | 5.1% | `IntEqReifConstraint::is_satisfied()` |
| 8 | 4.3% | `vector<long>::_M_realloc_insert` |
| 9 | 3.6% | `Solver::verify_solution()` |

- `count_uninstantiated` + `select_variable` = **36.7%** が最大ボトルネック
- `is_satisfied()` 群 = **23.2%** — `verify_solution` (3.6%) 経由で大量呼出し
- `Domain::values()` + `_M_realloc_insert` = **10.5%** — ヒープ確保コスト

**設計判断・理由**
- ハッシュソートの削除は探索品質に直結するため、単純な除去ではなく「同等品質で低コスト」な代替が必要
- `select_variable` リファクタ（候補収集 + shuffle → ランダムオフセット直接走査）は独立した改善として有効

**気づいた課題・TODO**
- 値選択の改善は「ソート除去」ではなく、`domain.values()` のヒープ確保回避に絞るべき（ソート自体は維持）
- hitori では `count_uninstantiated` / `select_variable` の最適化が最優先
- `verify_solution` が 1M回呼ばれ、全制約の `is_satisfied()` を評価している — 不要な verify を削減できる可能性

**関連ファイル**
- `include/sabori_csp/solver.hpp` — `value_buffer_`, `xor_state_` 追加
- `src/core/solver.cpp` — 値選択変更、`select_variable` リファクタ

**現在のコード状態**: ハッシュソート除去 + select_variable リファクタが uncommitted で残っている。ベンチマーク結果を踏まえて要方針決定。

## 2026-02-09 select_variable 最適化: Activity ソート + サンプリングスキャン

**実施内容**
- `select_variable` の Phase 2 ループにスキャン範囲制限を導入（先頭 10%、最低 64 変数）
- `search_with_restart` 内の 2 箇所の `std::shuffle` を `std::sort`（activity 降順）に置換
- 初期化時（`solve`/`solve_all`）のシャッフルは activity が全 0 のためそのまま維持
- 全104テスト通過を確認

**設計判断・理由**
- sparse set 導入後も `select_variable` が 68.8% を占めていた原因は、backtrack で `unassigned_count_` が復元されるためフルスキャン（平均 ~1067 変数）が毎ノード発生していたこと
- リスタート時に activity 降順ソートし、各ノードで先頭 10% だけスキャンすることで、activity 上位に MRV を適用する戦略
- `max(64, ...)` で変数が少ない問題での品質低下を防止

**気づいた課題・TODO**
- EchoSched ベンチマークで実際の効果測定が必要（select_variable の割合低下を確認）
- 10% / 64 の閾値が最適かはベンチマーク結果次第で調整の余地あり

**関連ファイル**
- `src/core/solver.cpp` — `select_variable`, `search_with_restart`
