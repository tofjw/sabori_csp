# 2026-07-07

## 2026-07-07 00:00 〜 01:55 int_eq_imp 再有効化 A/B と aggregator の imp 対応

**実施内容**
- 棚卸提案 #2: `int_eq_imp` / `bool_eq_imp` 宣言再有効化の A/B 計測
  - ハーネス `bench_int_eq_imp.py` 新設: 宣言有効化した mznlib 変種 + .msc を自動生成し、
    imp FZN に `*_eq_imp` が実出現する問題だけを対象化（2022-2025 で 34 問）。
    同一バイナリで FZN 直接実行 × 5 シード × 30s、obj 品質で判定
  - **Round 1: net(imp) = +0** (38勝/38敗/94分)
- Round 1 の敗因調査で交絡を発見: **OneHotChannelAggregator が
  `dynamic_cast<IntEqReifConstraint*>` のみ対象**で、imp 有効化すると hitori 型の
  「定数 eq_reif の束」(hitori では 1484 本全部が定数側) が集約から外れ、
  IntOneHotChannel の集約伝播 + bump_activity を丸ごと失っていた
- aggregator / IntOneHotChannel の imp 対応を実装:
  - エントリ単位の imp フラグ (`bools[i] -> (x==values[i])` 片方向)
  - 健全性ガード: x==v ⇒ b:=1 と b=0 ⇒ remove v を imp エントリで禁止、
    exactly-one 推論（残り1個のbを1に / 全b=0で矛盾）は `all_reif_` 必須に。
    対偶 (v∉dom(x) ⇒ b:=0) と at-most-one は imp でも健全なので維持
  - aggregator: IntEqImpConstraint も候補化、value 衝突は reif 優先
    （スキップ候補は元制約のまま残るので健全）
  - テスト: imp ランダム 300 反復 brute 同値、混在ケース、aggregator 統合 ×2。
    ctest 280/280、golden 249 green（reif 経路の挙動不変）
  - hitori で 1295 imp + 203 reif → 386 channels の集約発火を確認
- **Round 2 (交絡除去後): net(imp) = +6** (38勝/32敗/100分)

**設計判断・理由**
- **宣言は既定コメントアウトのまま維持**: 決着 70 ペアで 38勝32敗は符号検定 p≈0.55 で
  ノイズと区別できず、tie 100/170 の問題依存の諸刃。負け側
  (stripboard-2022 の解喪失 / traveling-tppv / yumi-static) も実在。
  net±0 級は既定変更しない基準どおり
- f8c701b (2026-03-05) の「少し遅い」判定の一部は集約バイパスの交絡で説明がつく
  (gfd-schedule +1→+5, train-scheduling −2→0 が交絡除去で好転)
- arithmetic-target (obj 2428→5, 全シード) と gfd-schedule の一貫大勝は
  opt-in arm (imp 宣言入り mznlib 変種) の弾として温存価値あり
- hitori は half-reif + 集約でも全シード解なし → FZN 構造でなく学習側の溝と再確定

**気づいた課題・TODO**
- [ ] imp 宣言入り mznlib 変種を portfolio 多様性 arm として検討 (arithmetic-target 級の
      大勝域がある。単スレ計測なので実並列 A/B が前提)
- [ ] IntEqImpConstraint 残課題(優先度低): 対偶検出が bounds-only
      (reif の support_value_ 方式より弱い) / prepare_propagation の共通値走査が
      break 不可で全域走査

**関連ファイル・コミット**
- `src/core/one_hot_channel_aggregator.cpp`, `src/core/constraints/global/int_one_hot_channel.cpp`
- `include/sabori_csp/constraints/global/misc.hpp`, `include/sabori_csp/constraints/comparison.hpp`
- `tests/cpp/test_one_hot_channel.cpp`（imp テスト群追加）
- `benchmarks/minizinc_challenge/bench_int_eq_imp.py`（新規）

## 2026-07-07 01:55 〜 03:00 groupsplitter 調査 → 基盤バグ2件修正で証明付き完勝に反転

**実施内容**
- 棚卸 (c) 探索バケツの groupsplitter（FZN が CP-SAT と byte 一致なのに 60s 解なし）を調査
- **原因1: par 定数配列の全要素が Variable として実体化**
  - parser.yy は `array [1..N] of int:` でも要素ごとに fixed_value の VarDecl を作り、
    to_model() が全てに Variable を生成。groupsplitter はテーブルデータ 483万セル
    → モデルが 4,835,805 変数（presolve verbose で発見・セル数と厳密一致）
  - 修正: 固定値 var_decl の eager 生成をスキップし、名前参照時に
    `FznBuildContext::get_var_by_name` で遅延実体化（出力対象・出力配列要素・
    目的変数は除外）→ 245 変数に
- **原因2: TableConstraint がイベントごとに即時 full filter**
  - 修正1後も 27s/決定で停止。gdb バックトレースで `filter_domains` 内と特定
    （ptrace 不可のため gdb 配下起動 + SIGINT 方式）
  - 全ドメインイベントが O(range×word_scan) の filter_domains を即起動し、
    filter の enqueue が他テーブルの filter を再起動する連鎖で二次爆発
    （arity3×256k行×15本 + arity6×17k行×15本 等の 75 テーブル）
  - 修正: bin_packing_load と同じ batch 集約に変更。イベントは word 更新
    （増分・trail）のみ行い `schedule_constraint_batch`、キュー空時に
    `propagate_batch` → filter 1回。`filter_gen_` を「イベントは現ラウンドに
    マーク / filter_domains が完了時に ++ してラウンドを閉じる」意味論に再構成
    （バッチ集約でも residue 検証の見逃しが出ないように）
- 効果（groupsplitter u15g1pref2, 60s）:
  - 直接 fzn: 解0/fails=0/enumerate=1 → 解6/obj=270/fails=14,140
  - bench 経路: TIMEOUT 解なし → **OPTIMAL obj=279 証明付き・CHECK_OK**（CP-SAT は 135）
- 2025 フル再計測（昨日と同条件）: **Sabori 12 / CP-SAT 3 / Tie 2**（昨日 11/4/2）。
  groupsplitter が勝ちに反転、他は軌道ジッタ範囲（cgt/tsptw/wtv 微改善、新規の負けなし）、
  全勝ち解 CHECK_OK

**設計判断・理由**
- 遅延実体化の例外（出力/目的変数）: 解出力は sol を名前引きするため、出力対象は
  eager 生成が必要。目的変数は find_variable_index で引かれるため同様
- filter_gen_ 再構成が必要な理由: イベントごとに gen を進める旧方式のまま batch 化
  すると、同一バッチ内の先行イベントの word 変更が「未変更」に見え、死んだ residue
  を信じて **under-filtering**（健全だが GAC 喪失）になる
- 両修正とも軌道可変（定数遅延化は par 配列を持つ全問題の変数 id 空間を変える）。
  golden 差分 5 件は解集合同一（機械照合）を確認して再記録

**気づいた課題・TODO**
- [ ] 2023/2024 での広域回帰確認（軌道可変変更のため。2025 は 12/3/2 で良好）
- [ ] mondoku は bench 経路でまだ解なし（GCC native 済み・fzn 直接では解1）
- [ ] gt-sort / hitori は既知の射程外（set 変数 / 学習側）

**関連ファイル・コミット**
- `src/fzn/model.cpp`, `src/fzn/fzn_build_context.cpp`（定数遅延実体化）
- `src/core/constraints/global/table.cpp`, `include/sabori_csp/constraints/global/extensional.hpp`（batch 化）
- `tests/golden/expected/`（5 件再記録: table_int×3, regular_simple, global_cardinality_open）
