# 作業ログ 2026-07-05

## 2026-07-05 06:48 SIMD 性能調査（perf/simd, step 0〜2）+ cumulative TTEF ①

**実施内容**
- グローバル制約の SIMD 化候補を調査し、段階的に試作・実測した。
- **step 0**: `-march=x86-64-v3`（AVX2）を CMake に追加。自動ベクトル化が有効化（fzn バイナリ %ymm 0→788）も、end-to-end は中立。→ commit c734f31
- **step 1**: table dense パス用 SIMD カーネル（AVX-512/AVX2/スカラ3段）を試作、`simd_bits.hpp` + table.cpp 配線。マイクロベンチでカーネル単体は AVX2 2.0-2.2×/AVX-512 2.7×だが、end-to-end 中立。**未コミット（保留）**。
- **step 2**: SoA ミラー案 → gather マイクロベンチで利得 1.04-1.10× のみと判明し不採用。代わりに int_lin_eq の単位係数 fast path（idiv 除去）を試作。中立だが素直に良いコードとして採用。→ commit 8edc45e
- **① cumulative**: 死蔵していた `prefix_energy_` を配線し `profile_integral` を O(P)→O(log P) 化。**largescheduling で +24〜34% throughput**（gprof: TTEF per-pass 18.84→14.78µs, −21.5%）。266 green・cumulative brute PASS。→ commit 1801099

**設計判断・理由**
- 各段で「推測せず先に実測」を徹底（step 0 で自動ベクトル化ゼロ仮定を棄却、step 1 で table 寸法実測→広テーブルは sparse ルーティングで dense に来ないと判明、step 2 で celar/nside の arity・係数を実測）。
- SIMD/SoA/idiv 除去が全て中立だった根因＝**このソルバは compute 律速でなく memory-latency / algorithm 律速**。散在変数アクセスのキャッシュミス遅延・伝播カスケードが支配し、算術スループット改善が効かない。
- cumulative だけ効いたのは TTEF が本当に compute/algorithm 律速だから（O(P) プロファイル走査は実在の仕事）。→「vectorization より algorithm/locality が先」というテーゼが数字で確定。
- 中立コミット（step 0/2）も残した理由：step 0 は安全な既定＋手書きカーネルの前提、step 2 は除算フリーで無害。コミットメッセージに「neutral・なぜ残すか」を明記し再探索を防ぐ。

**気づいた課題・TODO**
- **② TTEF 内側ループ1（energy_theta + L=min est）の O(n²) 冗長再計算 → running prefix で O(n) 化**（①より大きい可能性、O(n²) は O(n·P) より支配的）。
- **③ theta_cp を SoA パック+SIMD で連続化**（cumulative は gather 障壁が弱く、ソート済みタスクを連続配列に pack すれば SIMD が中立で終わらない見込み）。
- step 1（table SIMD, simd_bits.hpp + table.cpp）の去就：latent infra として温存 or revert。発火には sparse ルーティング/世代スキップの見直しが必要。
- 一般教訓：性能 ROI は vectorization でなくメモリ局所性（変数アクセスパターン）とアルゴリズム（伝播カスケード削減）に向けるべき。

**関連ファイル・コミット**
- commit 1801099（①, cumulative.cpp + scheduling.hpp）
- commit c734f31（step 0, CMakeLists.txt）
- commit 8edc45e（step 2, linear.hpp + int_lin_eq.cpp）
- 未コミット: include/sabori_csp/simd_bits.hpp（新規）, src/core/constraints/global/table.cpp（step 1）

## 2026-07-05 07:44 cumulative TTEF 続き（②/3a）+ step1 revert + ihtc-marte gprof

**実施内容**
- **step 1 revert**: table SIMD カーネル（simd_bits.hpp + table.cpp）を破棄。カーネル単体は2倍だが end-to-end 中立（sparse ルーティング/世代スキップで発火せず）のため不採用。
- **step 0 / step 2 を別々にコミット**（c734f31 / 8edc45e）。どちらも中立だがコミットメッセージに「neutral・なぜ残すか」を明記。
- **② TTEF 内側ループ O(n²)→O(n)**: forward/backward_pass の energy_theta と L(min est)/R(max lct) を running 累積化。largescheduling +29〜33%。→ commit 3d17690
- **3a theta_cp locality**: lst/ect/req を order 順に連続パック（メンバ scratch, 再確保なし）+ `if(lst<ect)` を clip に畳んだ分岐レス reduce。純 locality（SIMD なし）で +9〜12%。→ commit ba18ee7
- **3b SIMD は見送り**: theta_cp は int64 必須だが出荷ターゲット x86-64-v3(AVX2) に int64 max/min/mullo が無い（AVX-512VL/DQ 専用）。AVX-512 専用パスは native でしか効かず死蔵になるため不採用。
- **ihtc-2024-marte で cumulative スタックを計測 → 1.000（中立・回帰なし）**。gprof で支配項を特定。

**設計判断・理由**
- cumulative TTEF スタック（①+②+3a）合計 largescheduling 約 **1.75〜1.89×**。TTEF が hot な問題（大 n cumulative）に効く問題特化 win、効かない問題では無害。
- **ihtc-marte gprof の決定的発見**: 支配項は `IntLinEqConstraint::propagate_lower/upper_bounds` = **76.5%**（32.4M + 12.0M calls, TTEF は top20 外）。self 12.68s / 32.4M = **0.39µs/call = 呼び出し頻度律速**。→ int_lin は SIMD/idiv（per-call 算術）でなく **再伝播回数の削減**がレバー。step 2 が中立だった理由と一致。
- 皮肉: perf/**simd** ブランチの実効改善はすべて非 SIMD（アルゴリズム/locality）。「compute でなく memory-latency / algorithm 律速」を全編で実証。

**気づいた課題・TODO**
- **【次の本命】int_lin_eq 再伝播頻度の削減**: ihtc-marte 76.5% / celar でも同系統。32M calls の発生源＝process_queue の再キュー条件。有意に締まった時だけ再キュー / affected 変数限定伝播 など。複数問題に波及。
- TTEF に残る候補: `energy_up_to` 5%、pruning loop3 の O(n²)（分岐+enqueue で SIMD 不向き）。
- ソルバの二大ホットスポット = int_lin_eq propagate（頻度律速）と TTEF（アルゴリズム律速）。

**関連ファイル・コミット**
- commit 3d17690（②, cumulative.cpp）
- commit ba18ee7（3a, cumulative.cpp + scheduling.hpp）
- step 1 は revert 済み（未コミットのまま破棄）

## 2026-07-05 10:15 int_lin_eq no-op スキップ（次の本命）

**実施内容**
- ihtc-marte gprof の発見（int_lin_eq propagate 76.5%）を受け、まず**無駄率を計測**: propagate_lower/upper の呼び出しのうち何も enqueue しない割合 = **ihtc-marte 73.1% / celar 50.7%**（pending_updates_size() で実効性を判定する RAII 計装、後で除去）。
- **静的 slack スキップを実装**: 変数 j が枝刈り可能なのは `|c_j|*width_j > slack`（lower: slack=total_max-target, upper: target-total_min）の時のみ。探索中 width は縮むだけなので、**根での max_j |c_j|*width_j が恒久的な上界**。prepare_propagation で一度計算（max_static_ub_）し、`slack >= 上界` で早期 return true。→ commit 1e8ab58
- **健全性**: 数学的に sound（枝刈り不能が証明できる呼び出しのみ skip）、**トレイル不要**（上界は単調安全）＝false-UNSAT 系バグを構造的に排除。celar が完全同一（fails=84753/restarts=1328/obj=159）で探索木不変を確認。266 green + 線形 brute(4714 assertions) + golden。
- **効果**: ihtc-2024-marte **+13.2%**（enumerate/15s 安定）。celar/opd/mrcpsp/nside/code-generator は中立（1.000 前後、回帰なし）。

**設計判断・理由**
- celar は「完了問題で fails 完全一致」を使い、skip が探索軌道を変えていない（＝真の中立、時間差はノイズ）ことを立証。~10s spike は両arm に出る系ノイズ。
- **skip の効き所 = int_lin_eq がボトルネック かつ slack が緩い**（大 arity・緩い制約）。ihtc-marte が該当。code-generator は bool_clause/reif/element 支配 + slack タイトで発火せず中立（int_lin_eq 1402本あっても効かない）。celar は arity 3・root width 広く静的上界が緩いため発火しにくい。
- 「win はホットスポットのある所にだけ、回帰なし」が int_lin でも再現。

**気づいた課題・TODO**
- **動的（トレイル）上界**: 静的上界は根の width 依存で緩い。max_j|c_j|*width_j を全確定時に締め直しトレイル復元すれば、slack タイトな問題（celar 系）でも発火余地。ただし health-critical なので慎重検証（trail 復元の正しさ）。
- int_lin_eq は依然 ihtc 76.5% の大物。skip は no-op を消しただけで、**effective な 27% の伝播自体のコスト**（0.39µs/call の呼び出し頻度）は残る。再伝播トリガの削減（affected 変数限定伝播）が次段。
- 同型スキップを int_lin_le / int_lin_le_imp にも展開できる可能性（未着手）。

**関連ファイル・コミット**
- commit 1e8ab58（int_lin_eq skip, linear.hpp + int_lin_eq.cpp）

## 2026-07-05 10:44 int_lin_eq 動的スラック上界（静的スキップの拡張）

**実施内容**
- 静的上界（根の max|c|*width）は domain が縮むと緩く、slack が大きい時しか発火しない。**動的上界 max_contribution_** を実装: フルスキャン（非skip 呼び出し）時に**厳密な max_j|c_j|*width_j へ締め直し**、`rewind_to` で静的上界へリセット。→ commit 354e41c
- **トレイル改変を回避した健全性設計**: contribution は方向非依存で「no-op ⟺ max ≤ slack」。width は探索中縮むだけ＝キャッシュ値は有効上界のまま。backtrack 時は境界変化が必ず save_trail_if_needed→dirty→rewind_to を通るのでリセット保証 → **max_contribution_ 自体はトレイルしない＝false-UNSAT リスク圏外**。
- **独立 O(n) パス版は ihtc（arity 605）で −10% 悪化** → prune ループに **畳み込み**（変数あたり2命令: contrib 計算と mc 比較、skip_idx は mc 集計後に pruning のみスキップ）→ 冗長読み取りが消え **ihtc +46% に反転**。

**設計判断・理由**
- 効果（静的スキップ比）: **ihtc-2024-marte +46% / celar +24%（静的では中立だった）/ code-generator +1〜2% / opd,mrcpsp,nside,largescheduling 中立・回帰なし**。
- 健全性: celar 完全同一（fails=84753/restarts=1328/obj=159）、266 green + 線形 brute(4714)。
- **教訓の再確認**: 事前分析（「動的は複雑で危険」）でなく実測が正解を出した。独立パスで一度失敗 → 畳み込みで反転、という measure-driven の過程そのものが成果。大 arity では「1回の冗長 O(n) パス」が支配的コストになる。

**気づいた課題・TODO**
- **達成済み**: 「動的トレイル上界」TODO（前セクション）を、トレイル不要のリセット方式で解決。
- 残: int_lin_le / int_lin_le_imp への同型スキップ横展開（int_lin_eq と同じく slack ベース）。
- 残: effective な伝播自体（再伝播トリガ頻度）の削減は未着手。
- int_lin_eq スタック合計: no-skip → 静的(+13% ihtc) → 動的(ihtc さらに+46%, celar +24%)。

**関連ファイル・コミット**
- commit 354e41c（動的上界, linear.hpp + int_lin_eq.cpp）

## 2026-07-05 11:38 no-op スキップの int_lin_le / int_lin_le_imp 横展開

**実施内容**
- int_lin_eq の動的スラックスキップを不等式制約へ横展開。`Σc_j x_j ≤ bound` で prune 可能 ⟺ `|c_j|*width_j > S`（S = bound − total_min）。同型の動的上界（フルスキャン時に厳密 max へ締め直し／rewind_to で静的リセット／トレイル不要）で `S ≥ max_contribution_` を skip。→ commit e546286
- **int_lin_le**: 既存の O(n) エンテイルメントループ（total_max ≤ bound 判定）を、その**上位集合の O(1) スキップ + リフレッシュ**に置換（no-op 呼び出しが O(n)→O(1)、エンテイルメント ⟹ 私のスキップ発火なので coverage 減らない）。
- **int_lin_le_imp**: エンテイルメント無し。b=1 の伝播経路に同型スキップを追加。
- 効果: **opd +46%**（int_lin_le/le_imp が律速）。ihtc/code-generator 中立（le/le_imp が hot でない）。

**設計判断・理由**
- 健全性: `prune_sum_le` は純粋関数（実際に締まる時だけ enqueue、no-op で無副作用）＋ skip 条件が prune 条件と数学的に厳密一致 → **domain 枝刈りは不変で sound**。opd/mrcpsp/rcpsp-wet すべて同一最適値、266 green + 線形 brute(4714)。
- **軌道差の機構を特定**: mrcpsp は両版とも obj=27 を 0.07s で最適証明するが fails が決定的に異なる（316 vs 277, nogoods 65 vs 66）。原因は「no-op を速く返す → 制約間の伝播順序が変わる → 複数制約が導ける境界の nogood 理由帰属が変わる → 学習節が変わる」。**LCG 系で既知の健全な現象**（正しさ不変、最適一致）。int_lin_eq が celar で完全同一だったのは順序影響が節帰属を flip しなかった偶然。
- 判断: 健全＋opd 実益大なので採用。ただし「no-op スキップは挙動中立ではなく軌道を動かしうる（sound）」と明記。

**気づいた課題・TODO**
- **達成**: 「int_lin_le / le_imp 横展開」TODO 完了。
- int_lin スキップ系カバレッジ: eq（ihtc +46%, celar +24%）+ le/le_imp（opd +46%）。線形律速の複数クラスをカバー。
- 残: 再伝播トリガ頻度そのものの削減（effective な伝播）、int_lin_le_reif への展開検討。
- 教訓: 伝播変更の健全性は「domain 枝刈り不変」で判定し、探索軌道差（fails/nogood）は sound なら許容。決定性チェック（版内で fails 安定）+ 最適値一致 が健全性の実務的検証。

**関連ファイル・コミット**
- commit e546286（le/le_imp skip, int_lin_le.cpp + int_lin_le_imp.cpp + linear.hpp）

## 2026-07-05 12:31 no-op スキップの int_lin_*_reif 展開検討 → 不採用（reif は cold）

**実施内容**
- reif（b ⟺ 線形関係）への同型スキップ展開を検討・実装・実測。
- **構造分析**: int_lin_le_reif は「b 確定時の線形伝播（propagate_bounds_le b=1 / propagate_bounds_gt b=0）」と「b 未確定時のエンテイルメント検出（b 確定）」の2面。後者は min_rem/max_rem 維持済みで **O(1)**（スキップ対象なし）。スキップ対象は前者2パスのみ。skip 条件は le: S=bound−total_min、gt: S=total_max−(bound+1)、同じ max_contribution_。
- **prevalence**: reif 多用 top = **ihtc=eq_reif 25455 / opd=le_reif 19544**（ホット2問題が reif 最大ユーザー）。
- **実装+健全性**: le_reif に静的→動的スキップ実装。b 確定分岐が唯一 save_trail を呼ばない穴 → 動的上界の rewind リセット結合のため save_trail を追加。266 green + 線形 brute + opd/cable-tree/bnn/tower 全て同一最適で健全確認。

**設計判断・理由（不採用）**
- 実測（no-skip 比）: opd 静的1.014 / 動的0.982、cable-tree 1.00 / bnn 1.05 / tower 0.99 ＝ **全 reif 多用問題で中立〜わずかに negative**。
- 根本原因: **reif は探索中 b が大半未確定 → 線形伝播パスがほとんど走らない**。実行されるのは O(1) の b 確定判定で、そこにスキップ対象が無い。動的版はリフレッシュ overhead だけ乗って opd 0.982。**table SIMD と同じ「健全な機構だがワークロードが踏まない」パターン**。→ revert。
- 対比: int_lin_eq/le/le_imp は b 無し（or imp の b=1 固定）で線形伝播が常時走る → 受益あり。reif は cold パス。

**気づいた課題・TODO**
- **結論**: int_lin スキップ系のカバレッジは eq/le/le_imp で概ね飽和。reif は伝播経路が cold で受益なし（TODO の le_reif 展開はクローズ）。
- 効くのは「線形伝播が実際に頻繁に走る」制約に限る。次の大物は依然 int_lin_eq の effective 伝播そのもの（再伝播トリガ頻度削減）だが、これは伝播頻度律速の本丸で難度高。
- 教訓: prevalence（制約数の多さ）は受益の必要条件だが十分条件でない。伝播経路が hot かを実測で確認すること（reif は数は多いが cold）。

**関連ファイル・コミット**
- コミットなし（le_reif は実装→neutral→revert）。int_lin_le_reif.cpp / linear.hpp は変更なしに復帰。

## 2026-07-05 12:43 大制約の改善余地 調査（プロファイル+3エージェント分析）

**実施内容**
- 行数の多い global 制約に改善余地があるか調査。gprof で多様な問題（spot5/blocks-world/p1f-pjs/openshop/test-scheduling）をプロファイルし hot な大制約を特定、hot 3制約を general-purpose エージェント3体で並行深掘り。
- **プロファイル・マップ（未最適化で hot な大制約）**:
  - spot5: **Table ~40%**（filter_domains 11% + on_instantiate 10% + **save_trail 9% + rewind 9%=トレイル18%**）
  - p1f-pjs: **all_different ~16%**（run_bounds_filter 9.9% + GAC compute_sccs 6.2%）+ **array_var_int_element 11%**
  - openshop: **Disjunctive ~24%**（edge_finding 15% + count_set_bits 9%）
  - blocks-world: 変数選択 24.5% 支配、count_eq 6.5%(軽微)。test-scheduling: cumulative TTEF(最適化済)

**設計判断・理由（改善候補ランキング）**
- **Tier1 低リスク高ROI**:
  1. **Table フラットトレイル(18%)**: TrailEntry が各自 heap 確保の word_diffs を所有 → セーブポイント毎に malloc+free。フラット diff ログ+マーカースタック(容量維持)へ置換。機械的で pruning 述語に不干渉=低リスク。**最初に着手**。
  2. **Disjunctive edge_finding(24%)**: cumulative の①②3a と同型3修正が適用可(O(n²)→running累積 / count_set_bits→prefix-popcount[timeline_ read-only 確認済] / theta_cp locality)。実証済みパターン。
  3. **all_different**: GAC `gac_val_to_idx_` unordered_map→フラット vector 索引(dense前提・値ごと2回ハッシュ) / `run_bounds_filter` の std::sort×2→適応挿入ソート(ほぼソート済) / tarjan stack メンバ化 / 死フラグ `matching_valid_`(7書き0読み)削除。
- **Tier2 要注意**: Table filter_domains の per-value `model.contains()`→実ドメイン反復(11%,低中) / array_var_int_element の support-tracking ゲート緩和(11%,**伝播強度変化=ベンチゲート必須**)。
- 全て data構造/アルゴリズム変更で pruning 述語に不干渉(false-UNSAT 圏外)。「行数多い=改善余地」は今回当たり。

**気づいた課題・TODO**
- 着手順: ① Table フラットトレイル → ② Disjunctive edge_finding → ③ all_different(GAC/Bounds)。
- array_var_int_element ゲート緩和は伝播タイミング変更なので最後、ベンチゲートで審査。
- 教訓: 大制約のトレイル/allocation 設計に隠れコスト（Table 18%は per-node malloc/free）。gprof で self% 上位に trail/save/rewind が来たら allocation churn を疑う。

## 2026-07-05 17:25 ① Table フラットトレイル実装 → +71〜96%

**実施内容**
- 調査 Tier1-① を実装。TableConstraint のトレイルが `TrailEntry{ vector<pair<size_t,uint64_t>> word_diffs; ... }` で、各セーブポイントが自前 heap の word_diffs を所有 → save_word の初回 push で malloc、rewind の pop_back で free。gprof spot5 の save_trail 9%+rewind 9%=18% は純粋な per-node malloc/free churn。
- **フラット diff ログ + マーカースタック**へ置換: `trail_diffs_`（(word,old) 追記のみ・解放なし）+ `trail_markers_`（save_point, diff_start, old_last_nz）。rewind は marker を新しい順に word 復元して `trail_diffs_.resize(diff_start)`（容量維持）。generation dedup 不変なので marker 内 word 一意（復元順不問）、marker 間は deepest-first で旧 LIFO を再現。→ commit 74c2cb7

**設計判断・理由**
- 健全性（trail 正確性 critical）: **`-a` 全解出力が byte 完全一致**（allsol/simple/unsat/three_vars）＝ rewind 復元の決定的証拠。spot5 両インスタンス同一最適（2014:278[base -t30 で到達確認], 2015:276106）、266 green。
- 効果: throughput fails/10s **spot5-2014 +71% / spot5-2015 +96%**（trail churn 排除で table 律速が約1.7〜2×）。spot5 -t8 の fails 差(77k vs128k)は timeout throughput であって不健全ではない（-a 一致で確定）。
- 機械的な表現置換で CT pruning ロジックに不干渉 → false-UNSAT 圏外。

**気づいた課題・TODO**
- 次: ② Disjunctive edge_finding（cumulative ①②3a と同型の O(n²)→running / count_set_bits→prefix-popcount / theta_cp locality）。
- 残: ③ all_different（GAC unordered_map→vector索引 / Bounds sort→適応 / 死フラグ削除）、Table filter_domains の contains 削減。
- 同型トレイル設計（per-node vector 所有）が他制約にもあれば同じ churn の可能性 → 横断チェック候補。

## 2026-07-05 18:39 ② Disjunctive edge_finding(revert) / ③ all_different(cleanup保持)

**実施内容**
- **② Disjunctive edge_finding**: cumulative 同型の内側ループ1 O(n²)→running累積を両パスに実装。266 green + disjunctive brute PASS。しかし openshop/train-scheduling/vrplc/flowshop 全て **enumerate 完全一致(中立)** → revert。原因: これら instance の m(disjunctive あたりタスク数)が小さく O(n²)≈O(n)。**openshop の粗プロファイル(786 fails)が edge_finding 24% を過大評価**していた。count_set_bits/theta_cp も同じ小 m 理由で中立の公算。
- **③ all_different**: GAC 値→index を unordered_map→フラット vector 索引(dense 前提, hash 除去, <unordered_map> 依存削除)、run_bounds_filter の std::sort×2→適応挿入ソート(ほぼソート済で O(n))。→ commit 5d62b0c。健全性: `-a` 全解 byte 一致(デフォルト+ -G 強制GAC), 266 green。効果 p1f +1.5%(ノイズ帯)。per-constraint n が小さく headline 改善にならず、**コード簡潔化として保持**(正直に記載)。

**設計判断・理由**
- 大制約改善の総括: **Table フラットトレイル(spot5 40%,信頼)=+71〜96% だけが本物**。Disjunctive(openshop 24%,粗)=中立/revert、all_different(p1f 16%)=+1.5%/cleanup。
- **教訓(重要)**: 粗いプロファイル(少 fails)は大制約の self% を過大評価する。spot5(140k fails)は信頼でき Table が本物、openshop(786 fails)は edge_finding を誤判定。**改善余地は「profile% が高い」+「per-constraint の n が実際に大きい」の両方が必要**。プロファイルは fails 数が十分多い(≥数万)問題で取ること。
- revert/keep 基準: 純粋簡潔化かつ微正なら keep(all_different: hash 除去)、完全中立(1.000)なら revert(disjunctive)。

**気づいた課題・TODO**
- 大制約改善は Table で決着。残候補(Table filter_domains contains 削減 / array_var_int_element ゲート)は中〜要ベンチゲートで優先度低。
- disjunctive edge_finding の O(n²)修正は「大 m の disjunctive 問題」が来れば有効(latent)。ベンチコーパスには該当なし。

**関連ファイル・コミット**
- commit 74c2cb7(① Table flat-trail, 採用), 5d62b0c(③ all_different cleanup, 採用)
- ② disjunctive は実装→中立→revert(コミットなし)
