## 2026-02-18 06:58

**実施内容**
- `process_queue` の `RemoveValue` ケースで、削除した値が min/max だった場合に `propagate_bound_nogoods()` が呼ばれていないバグを修正
  - 下限変化時: `propagate_bound_nogoods(model, var_idx, true)` を追加
  - 上限変化時: `propagate_bound_nogoods(model, var_idx, false)` を追加
- テストファイル `tests/cpp/test_nogood.cpp` を新規作成（3テストケース）
  - IntNe + bisect での正確性テスト
  - AllDifferent + bisect での正確性テスト
  - 最適化 + IntNe + bisect でのテスト
- `tests/cpp/CMakeLists.txt` に `test_nogood.cpp` を追加

**設計判断・理由**
- `SetMin`/`SetMax` ケースでは既に `propagate_bound_nogoods` が呼ばれていたが、`RemoveValue` 経由の境界変化では漏れていた。同じパターンに揃えた
- テストは pre-inject 方式を検討したが、`solve_all` が開始時にノゴードをクリアするため、solver が自然に学習・利用するシナリオで設計

**気づいた課題・TODO**
- `test_backjumping.cpp` が `set_backjumping` (存在しないAPI) を使用しており、CMakeLists.txt に含まれていないため未コンパイル。整理が必要

**関連ファイル**
- `src/core/solver.cpp` (L1702-1707, L1716-1721)
- `tests/cpp/test_nogood.cpp` (新規)
- `tests/cpp/CMakeLists.txt`

## 2026-02-18 logical.cpp on_instantiate: model API 移行

**実施内容**
- `logical.hpp` / `logical.cpp` の4つの制約クラス（ArrayBoolAnd, ArrayBoolOr, BoolClause, BoolNot）について、`on_instantiate` 内の変数アクセスを SoA 配列ベースの Model API に移行
- 各クラスに変数IDキャッシュメンバを追加（`var_ids_`, `r_id_`, `pos_ids_`, `neg_ids_`, `a_id_`, `b_id_`）
- `var->is_assigned()` → `model.is_instantiated(var_id)`, `var->assigned_value().value()` → `model.value(var_id)` に置換
- `find_unwatched_candidate` を `const Model&` パラメータ付きに変更
- BoolClause の `can_satisfy`, `is_satisfied_by` に Model ベースオーバーロード追加、`get_var_id` ヘルパー追加

**設計判断・理由**
- `on_instantiate` はホットパスであり、`var->is_assigned()` は shared_ptr 経由の間接参照 + Domain オブジェクトアクセスが必要だが、`model.is_instantiated()` は SoA 配列の直接アクセス（min == max 比較）でキャッシュフレンドリー
- `presolve()`, `check_initial_consistency()`, `on_final_instantiate()` 等は変更しない（ホットパスではない）
- 既存の Variable ポインタ経由のヘルパー（`can_satisfy`, `is_satisfied_by`）は presolve 等から呼ばれるため残し、Model ベース版をオーバーロードとして追加

**気づいた課題・TODO**
- 他の制約ファイル（comparison.cpp, reification.cpp 等）にも同様の移行が適用可能

**関連ファイル**
- `include/sabori_csp/constraints/logical.hpp`
- `src/core/constraints/logical.cpp`

## 2026-02-18 solve_all で unit_nogood 伝播後の解見逃しバグ修正

**実施内容**

- `solve_all` で `int_times(x,y,z)` の全解探索が 24/25 解になるバグの根本原因を調査・修正
- 前回の `permanent_nogood_count_ == 0` ガードを revert（粗すぎて正当な unit nogood 学習も阻害）
- デバッグログ追加 → verbose 実行で (0,-1,0) が欠落する原因を特定
- `search_with_restart` の `select_variable == SIZE_MAX` チェックが根本原因と判明、修正
- 全 142 テスト合格を確認

**設計判断・理由**

- 根本原因: `apply_unit_nogoods` 後に unit nogood + permanent NG 伝播で全変数が確定するケースで、`select_variable == SIZE_MAX` → `return std::nullopt` としていたため、有効な新解を見逃していた
- 修正: `while (select_variable == SIZE_MAX)` ループで `verify_solution` → 解報告 → `add_solution_nogood` → backtrack → `apply_unit_nogoods` を繰り返す形に変更
- `permanent_nogood_count_` メンバーは不要になったため削除。unit nogood 学習自体は正当（permanent nogood 存在下でも全子ブランチが exhaustive に UNSAT なら正しい）

**気づいた課題・TODO**

- `search_with_restart_optimize` 側には同様の `select_variable == SIZE_MAX` チェックがないが、最適化探索では find_all パスを通らないため問題なし
- UNKNOWN (リスタート) パスでも `apply_unit_nogoods` 後に全変数確定する可能性はあるが、次の `run_search` 呼び出しで正しく処理されるため問題なし

**関連ファイル**

- `include/sabori_csp/solver.hpp` — `permanent_nogood_count_` メンバー削除
- `src/core/solver.cpp` — revert + `select_variable == SIZE_MAX` ハンドリング修正

## 2026-02-18 arithmetic.cpp on_instantiate/on_last_uninstantiated: Model API 移行

**実施内容**
- `arithmetic.hpp` / `arithmetic.cpp` の2つの制約クラス（IntTimesConstraint, IntAbsConstraint）について、ホットパスメソッドの変数アクセスを SoA 配列ベースの Model API に移行
- IntTimesConstraint: `x_id_`, `y_id_`, `z_id_` をヘッダーに追加、コンストラクタで初期化
- IntAbsConstraint: `x_id_`, `y_id_` をヘッダーに追加、コンストラクタで初期化
- `on_instantiate`, `on_last_uninstantiated` 内で:
  - `v->is_assigned()` → `model.is_instantiated(v_id_)`
  - `v->assigned_value().value()` → `model.value(v_id_)`
  - `v->id()` → `v_id_`
  - `model.var_min(v->id())` / `model.var_max(v->id())` → `model.var_min(v_id_)` / `model.var_max(v_id_)`

**設計判断・理由**
- `on_set_min` / `on_set_max` は arithmetic.cpp に存在しないため対象外
- `presolve()`, `propagate_bounds()`, `check_initial_consistency()`, `on_final_instantiate()` 等はホットパスではないため変更しない
- `domain().contains()` は Model API に対応するメソッドがないため据え置き

**気づいた課題・TODO**
- `propagate_bounds` 内の `v->id()` 呼び出しも `v_id_` に置換可能だが、presolve 経由のみなのでホットパスではない

**関連ファイル**
- `include/sabori_csp/constraints/arithmetic.hpp`
- `src/core/constraints/arithmetic.cpp`

## 2026-02-18 array_var_int_element: Model API 移行

**実施内容**
- `ArrayVarIntElementConstraint` のホットパスメソッド（`recompute_bounds_support`, `propagate_via_queue`, `on_last_uninstantiated`）の変数アクセスを SoA 配列ベースの Model API に移行
- ヘッダーに `index_id_`, `result_id_` を追加、コンストラクタで初期化
- `arr_var->id()` → `var_ids_[2 + idx_0based]` に置換（vars_ は [index, result, array[0], ...] の構造）
- `index_->is_assigned()` / `result_->is_assigned()` → `model.is_instantiated(index_id_)` / `model.is_instantiated(result_id_)`
- `index_->assigned_value().value()` / `result_->assigned_value().value()` → `model.value(index_id_)` / `model.value(result_id_)`

**設計判断・理由**
- `propagate_bounds`, `is_satisfied`, `on_final_instantiate`, `prepare_propagation`, `check_initial_consistency` はホットパスではないため変更対象外
- `propagate_via_queue` 内の step 3 で `arr_var` のローカル変数を `arr_id` に置き換え、shared_ptr 経由のアクセスを排除

**気づいた課題・TODO**
- `on_last_uninstantiated` で `last_var.get() == array_[...].get()` の比較は Variable ポインタ経由だが、代替が難しいため据え置き
- `result_->domain().contains()` も Model API への移行候補だが、Model に `contains` メソッドがないため据え置き

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`
- `src/core/constraints/global/array_var_int_element.cpp`

## 2026-02-18 global constraint 一括 Model API 移行

**実施内容**
- 以下の8つのグローバル制約ファイルについて、ホットパスメソッド（`on_instantiate`, `on_set_min`, `on_set_max`, `on_last_uninstantiated`, `propagate` 等）の変数アクセスを SoA 配列ベースの Model API に移行：
  1. `all_different.cpp` — `on_instantiate`, `on_last_uninstantiated`
  2. `array_int_maximum.cpp` — `on_instantiate`, `on_last_uninstantiated`（`m_id_` 追加）
  3. `array_int_minimum.cpp` — `on_instantiate`, `on_last_uninstantiated`（`m_id_` 追加）
  4. `circuit.cpp` — `on_last_uninstantiated`
  5. `count_eq.cpp` — `on_last_uninstantiated`, `propagate`（`c_id_` 追加）
  6. `int_element.cpp` — `on_instantiate`, `on_last_uninstantiated`, `on_set_min`, `on_set_max`（`index_id_`, `result_id_` 追加）
  7. `table.cpp` — `on_last_uninstantiated`, `filter_domains`
  8. `disjunctive.cpp` — `on_instantiate`, `propagate_bounds`, `update_compulsory_part`, `edge_finding`

- 移行パターン（全ファイル共通）:
  - `v->is_assigned()` → `model.is_instantiated(v_id)`
  - `v->assigned_value().value()` → `model.value(v_id)`
  - `v->id()` → キャッシュ済み `v_id` or `var_ids_[idx]`
  - `model.var_min(v->id())` → `model.var_min(v_id)`

**設計判断・理由**
- `all_different`, `circuit`, `table` はヘッダー変更不要（基底クラスの `var_ids_` で十分）
- `disjunctive.cpp` の `edge_finding` は presolve（`direct=true`）と search（`direct=false`）で共有されるため、三項演算子で Variable アクセスと Model API を切り替え
- range-for ループ（`for (auto& var : x_)`）はインデックス付きループに変換して `var_ids_[offset + i]` アクセスを可能に

**気づいた課題・TODO**
- `domain().contains()` は全ファイルで据え置き（Model API に対応メソッドなし）
- comparison.cpp, reification.cpp にも同様の移行が適用可能

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`
- `src/core/constraints/global/all_different.cpp`
- `src/core/constraints/global/array_int_maximum.cpp`
- `src/core/constraints/global/array_int_minimum.cpp`
- `src/core/constraints/global/circuit.cpp`
- `src/core/constraints/global/count_eq.cpp`
- `src/core/constraints/global/int_element.cpp`
- `src/core/constraints/global/table.cpp`
- `src/core/constraints/global/disjunctive.cpp`

## 2026-02-18 diffn 制約の実装

**実施内容**
- `diffn` グローバル制約（非重複矩形配置）をネイティブ実装
  - ペアワイズ obligatory region propagation（状態なし、O(n²)）
  - strict / nonstrict 両対応
- FlatZinc パーサーに `fzn_diffn` / `fzn_diffn_nonstrict` を登録
- MiniZinc 連携ファイル（predicate 定義 + redefinitions.mzn）を追加
- テスト4件作成（simple, unsat, forced_separation, three_rects）— 全 PASS
- 全147テスト PASS を確認
- CP-SAT で accap 問題（accap_a3_f20_t10）を解き、sabori で解の検証を実施
  - 全変数固定で検証 → PASS
  - yCoor 3変数を自由にして検証 → PASS（別の有効解を発見）

**設計判断・理由**
- 状態なしのペアワイズ propagation を採用（シンプルさ優先、rewind_to 空実装で済む）
- check_initial_consistency でエネルギーチェック（最小面積合計 vs 外接矩形面積）を実装
- propagate_pairwise / propagate_pairwise_direct の2版（callback 用 / presolve 用）は DisjunctiveConstraint と同パターン

**気づいた課題・TODO**
- 現在の O(n²) ペアワイズは n が大きい問題（stripboard 等）で遅い可能性あり
- sweep-line ベースの propagation や energetic reasoning の追加が将来的に有効

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp` — DiffnConstraint クラス宣言
- `src/core/constraints/global/diffn.cpp` — 実装本体（新規）
- `src/core/CMakeLists.txt` — ビルド対象追加
- `src/fzn/model.cpp` — パーサー登録
- `share/minizinc/sabori_csp/fzn_diffn.mzn`, `fzn_diffn_nonstrict.mzn` — MiniZinc predicate
- `share/minizinc/sabori_csp/redefinitions.mzn` — include 追加
- `tests/fzn/constraints/diffn/` — テスト4件
- `docs/CONSTRAINTS.md` — ドキュメント更新

## 2026-02-18 bisect モードのランダム分岐

**実施内容**
- bisect モードでヒント解（`current_best_assignment_`）がない場合、左側固定だった分岐選択をランダム（`rng_() & 1`）に変更

**設計判断・理由**
- ヒントなし時に常に左（下半分）を選ぶと、問題構造によっては偏った探索になる
- `rng_` は既存の `std::mt19937` メンバーを使用。ヒントがある場合の動作は変更なし

**気づいた課題・TODO**
- なし

**関連ファイル**
- `src/core/solver.cpp` (L760-767)
