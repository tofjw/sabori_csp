# 2026-06-28 作業ログ

## 2026-06-28 improvement probe の ablation（§7 の未計測機構）

**実施内容**
- 指摘: §7 には独立した2機構がある——勾配ヒント（値順序バイアス）と improvement probe（`run_improvement_probe`、ベスト目的値側から ~5% 改善を狙う軽量サブ探索）。`SABORI_GRADIENT` で測ったのは勾配ヒントの方で、**probe を走らせるか否かは未計測**だった。
- 計測用トグル `SABORI_PROBE=0` を追加（`run_improvement_probe` 冒頭の早期 return に `probe_enabled_` ガード）。bench `bench_probe.py`（最適化36問×5シード、SAT 除外、勾配と同条件）。
- 結果: **probe_on vs probe_off = net −23（24–47）、5 シード中 4 で off 勝ち・1 引分・probe_on は無勝利**。勾配ヒント（−13）より明確な足枷。
- §7 を JP/EN とも更新: 勾配ヒント（平均で負け・ポートフォリオ向き）と improvement probe（より明確に有害）を分離。まとめ表の最適化行・結論バレットに probe を追記。

**設計判断・理由**
- **§7 の最適化レイヤは勾配＋probe の両方が net マイナス、probe の方が悪い**。`probe_fail_limit_` を既定で正に（probe ON に）している現状は最適化問題で損。**既定オフが妥当**（structural 既定（§4）と同じ見直し候補）。
- temporal（第1基準）の発覚と同じ教訓: **コードに別機構が同居しているとき、トグルが片方しか覆っていないと未計測の機構を見落とす**。§7 は「勾配＋probe」一括説明だったが、トグルは勾配だけだった。
- 向きの頑健性: 30秒 wall-clock 判定の run ブレはあるが、4/5 シード一貫＋probe_on 無勝利で、向きは確か。

**気づいた課題・TODO**
- デフォルト変更候補が2つ溜まった: `bump_activity` の structural→none/base（§4）、probe ON→OFF（§7）。性能改善として別途検討の価値（ただし §4 は差が薄い、probe は −23 と明確）。
- 他の章にも「一括説明だがトグルが部分的」な機構が残っていないか、棚卸しの余地。

**関連ファイル・コミット**
- 実装: `include/sabori_csp/solver.hpp`, `src/core/solver.cpp`, `src/core/solver_search.cpp`（`SABORI_PROBE` トグル）
- bench: `benchmarks/minizinc_challenge/bench_probe.py`
- 記事: `articles/search-algorithm-explained.md`, `articles/search-algorithm-explained-en.md`（§7 に probe 実測小節 + まとめ表・結論）
- 全テスト 252/252 緑・既定動作不変を確認
- コミット: 3123f20

## 2026-06-28（続）probe 結論を vs CP-SAT で訂正——指標の乖離

**実施内容**
- 指摘: 上記 probe の結論（−23＝有害・既定オフ候補）は **probe_on vs probe_off の直接対決（目的値）**で出したもの。実際の Challenge のゴールは **CP-SAT（フィールド）に勝つこと**で指標が違う。`SABORI_PROBE=1/0 python bench_compare.py 2016 2025`（Sabori vs CP-SAT 勝敗）で比較。
- 結果（2016+2025 合算、Sabori 勝 / CP-SAT 勝 / 引分）:
  - **probe ON（既定）: 15 / 17 / 6**
  - **probe OFF: 12 / 17 / 9**
  - → **両年とも probe ON の方が Sabori 勝が多い（15 vs 12、CP-SAT 勝は同数）**。probe は引分を Sabori 勝に変え、CP-SAT には余計に負けていない。
- 「既定オフ候補」を撤回。§7 を「直接対決では負け（−23）だが対 CP-SAT では勝ちを増やす＝残すのが正解」に訂正（JP/EN）。**方法論の注記**を追加。
- `bench_probe.py` の YEARS を `SABORI_BENCH_YEARS` env で指定可能に（既定 2023,2024）。

**設計判断・理由**
- **直接対決（平均目的値）と vs フィールド（CP-SAT に勝つ）は別指標**。probe は「多くの問題で目的値を僅かに悪化させるが、数問で CP-SAT を超える閾値を押し上げて勝ちに変える」ため、直接対決マイナス・対フィールドプラスに乖離した。
- ユーザー体感では**他のアブレーション（gradient/structural/Bloom 等）は CP-SAT 勝敗と概ね一致**しているとのことなので、方法論注記は「probe は乖離した明確な例。多くの機構では2指標は概ね一致」と calibrate（他章の結論は据え置き）。
- 教訓: ablation の指標選択が結論を左右する。負け/有害系の結論は、本来のゴール指標（vs フィールド）で裏取りすべき場合がある。

**気づいた課題・TODO**
- 「既定変更候補」から probe を撤回（残すのが正解）。structural（§4）はまだ候補だが、これも vs CP-SAT で再確認の余地。
- 余裕があれば gradient/structural も vs CP-SAT で裏取り（ただし体感では一致見込み）。

**関連ファイル・コミット**
- bench: `bench_probe.py`（YEARS env 化）。比較は既存の `bench_compare.py`（year 引数）を使用
- 記事: §7 probe 小節の訂正 + 方法論注記 + まとめ表・結論バレット（JP/EN）
