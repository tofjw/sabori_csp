## 2026-03-02 13:13 activity_inc 修正

**実施内容**
- activity のインクリメントがハードコード `1.0` を使っていた箇所を、すべて `activity_inc_` ベースに修正
  - `solver.cpp`: 全値失敗時・両分枝失敗時の直接インクリメント（2箇所）
  - `nogood_manager.cpp`: `propagate_eq_watches` / `propagate_bound_nogoods` の conflict bump（3箇所）、`learn_from_conflict` の学習 bump（1箇所）
- NoGoodManager の3メソッドのシグネチャに `double activity_inc` パラメータを追加
- 全152テスト合格確認

**設計判断・理由**
- `activity_inc_` は decay 時に `/ 0.99` で増加していく設計。新しいインクリメントに `activity_inc_` を使うことで、過去のインクリメントが相対的にすべて decay されたのと同等になる（VSIDS 方式）
- `learn_from_conflict` の `0.01` 係数はそのまま維持（コンフリクト学習は元々控えめな bump なので比率を保持）

**関連ファイル**
- `include/sabori_csp/nogood_manager.hpp`
- `src/core/nogood_manager.cpp`
- `src/core/solver.cpp`

## 2026-03-02 16:19 dom/wdeg 変数選択ヒューリスティック試行→revert

**実施内容**
- dom/wdeg 変数選択ヒューリスティックを実装（`domain_size / Σ constraint_weight` でスコアリング）
  - `solver.hpp`: `constraint_weight_` ベクタ追加、`bump_constraint_weight()` 追加、`bump_activity()`・`activity_first_`・`activity_selection_`・`bump_activity_enabled_` 削除
  - `solver.cpp`: 制約 fail 時に `bump_constraint_weight` を呼ぶ方式に変更、直接 activity bump 削除、`activity_first_` トグル削除、`update_bump_activity_flag()` 削除
  - `variable_selector.cpp`: `select_linear` を dom/wdeg スコアリングに書き換え、`select_restart_pivot` も同様に更新
  - Python bindings: `set_activity_first` / `set_activity_selection` 削除
- ベンチマーク比較を実施（変更前・変更後の両方で 2024/2025 問題セット）
- **リグレッションが確認されたため、全変更を revert**

**設計判断・理由**
- dom/wdeg は制約レベルの fail 回数を蓄積し、1回の fail で即座にシグナルが出る利点がある
- しかしベンチマーク結果は悪化（2025: 4→3 wins、2024: 2→0 wins）
- 特に atsp が OPTIMAL 0.1s → TIMEOUT、mondoku が OPTIMAL 0.5s → OPTIMAL 2.1s に悪化
- variable activity ベースの方がリスタート後に有益な情報を保持しており、propagation で解ける部分では dom/wdeg の weight 蓄積が不十分
- ランダム開始オフセットの有無も試したが改善せず

**気づいた課題・TODO**
- variable activity は現状十分に機能しており、dom/wdeg への単純置換は逆効果
- 改善するなら dom/wdeg と variable activity のハイブリッド、または constraint weight を variable activity の補助情報として使うアプローチが考えられる
- ベンチマーク実行時に zombie プロセスが残る問題あり（`cleanup_stale_processes` の `pkill -f "minizinc"` が snapfuse を巻き込む可能性）

**関連ファイル**
- ベンチマーク結果 HTML: `sabori_benchmark_20260302_155047.html` (変更前 2025), `sabori_benchmark_2024_20260302_155417.html` (変更前 2024), `sabori_benchmark_20260302_161156.html` (dom/wdeg 2025), `sabori_benchmark_2024_20260302_161519.html` (dom/wdeg 2024)

## 2026-03-02 17:00 variable_selector スキャン順ラウンドロビン試行→revert

**実施内容**
- `select_linear` のスキャン開始位置をランダム (`rng() % n`) からラウンドロビン（前回の次の位置）に変更
- `decision_scan_pos_`, `defined_scan_pos_` メンバを追加し、decision vars / defined vars それぞれ独立にスキャン位置を追跡
- 3構成でA/Bベンチマーク比較（mznc2025_probs、全20問、30秒タイムアウト）:
  1. **ベースライン**: ランダム起点 + リスタート時シャッフル（元の実装）
  2. **ラウンドロビン + シャッフル**: スキャン起点のみ変更
  3. **ラウンドロビン + シャッフルなし**: リスタート時のシャッフルも削除

**設計判断・理由**
- 全変数を公平にスキャンすることで選択の偏りを減らす狙いだった

**ベンチマーク結果**

| 構成 | 勝ち | 負け | 備考 |
|---|---|---|---|
| ラウンドロビン + シャッフル | 4 | 8 | stripboard のみ TIMEOUT→OPTIMAL の大幅改善 |
| ラウンドロビン + シャッフルなし | 2 | 11 | atsp 0.08s→5.89s 等、全体的に大幅悪化 |

**結論: 変更を全て破棄し、元のランダム起点+シャッフルに戻した**
- リスタート時シャッフルが探索多様化の主要因
- スキャン起点の変更だけでは効果薄（むしろ悪化傾向）

**気づいた課題・TODO**
- stripboard だけはラウンドロビンで劇的改善 → この問題固有の構造（変数選択の偏りが致命的）がある可能性
- 探索多様化は別のアプローチ（activity decay 調整、value ordering 等）で検討すべき

**関連ファイル**
- `include/sabori_csp/variable_selector.hpp`
- `src/core/variable_selector.cpp`
- `src/core/solver.cpp`
- ベンチマークスクリプト: `/tmp/bench_ab.py`（一時ファイル）
