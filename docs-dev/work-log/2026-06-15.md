# 2026-06-15

## 2026-06-15 リファクタリング Phase 3: 機械的重複削減（SparseSetPool / ConstraintTrail）

**実施内容**

- **SparseSetPool クラス抽出**（commit `5256c19`）
  - `include/sabori_csp/sparse_set_pool.hpp` 新規。可逆 Sparse Set を共通化。
  - all_different / all_different_except_0 が個別に持っていた `pool_values_` / `pool_sparse_`
    (unordered_map) / `pool_n_` のスワップ削除ボイラープレートを `pool_.remove()` /
    `contains()` / `consumed()` / `value_at()` に置換（constraint 層で net ~115 行削減）。
  - sparse 写像は値域が密なら offset 付きフラット配列、疎で広域なら unordered_map を
    自動選択（計画の「unordered_map フラット配列化」を安全に実現）。
  - AllDifferentGAC サブクラスの直接メンバ参照（`is_val_in_pool`, GAC スナップショット
    構築）を `pool_` アクセサに追従。
  - `tests/cpp/test_sparse_set_pool.cpp` 新規: 参照モデル（std::set）とのランダム照合。
    flat/map 両パス、restore 可逆性を 33,518 assertion で検証。
- **ConstraintTrail<State> テンプレート抽出**（commit `b803462`, `65eb553`）
  - `include/sabori_csp/constraint_trail.hpp` 新規。「同一セーブポイントでは一度だけ保存」
    「save_point 超えを巻き戻す」trail 定型を `save_if_needed()` / `rewind_to(restore_fn)` /
    `clear()` に共通化。
  - int_lin_eq/le/ne + reif/imp の計7制約（`b803462`）、all_different 系2制約（`65eb553`）に適用。
    push/pop/back の手続きを排除し、復元は各制約のラムダで明示。
- **検証**
  - 全コミットで ctest 205/205 green（golden master = 182 fzn の `-a -s` 出力 byte 一致）。
  - SparseSetPool は alldiff bench ゲートも実行: Sabori Wins 6 / Timeouts 0 で
    baseline-20260614 と完全一致（退行なし）。

**設計判断・理由**

- unordered_map フラット化は memory 上「性能 ROI 低（flat profile に出ない）」のため、
  主目的は**重複削減（設計衛生）**と位置づけ。フラット配列は offset ベースだが、値域が
  広すぎる場合（span > max(1024, 4×|values|)）は memory blowup を避けて unordered_map へ
  フォールバックする自動選択にした。
- ConstraintTrail は hot path（IntLinEq の bound 差分計算）に**不介入**。trail は
  イベント/バックトラック毎の O(1) 簿記のみで、復元ラムダは Release で inline 展開される
  ため定数倍劣化なし。純リファクタなので golden master を主検証とした。
- **circuit の SparseSetPool 化は見送り**。circuit は元から内部インデックス 0..n-1 用の
  フラット配列（unordered_map 不使用）で、trail が union-find 状態と密に絡む。dedup 利得が
  小さく false UNSAT リスクが高いため、Phase 3 のスコープから外した。

- **死コード処分**（commit `9ccab04`）: 呼び出し元のない private メソッド3件を削除
  （AllDifferent/Except0 の `remove_from_pool`、IntLinEq の `check_feasibility`）。
  grep で src/include/tests/python 全体に caller がないことを確認。
  - bump_activity 系の `#if 0` ブロック群（constraint.cpp / increasing.cpp / comparison.hpp）は
    activity ヒューリスティクスの A/B 実験アーカイブ（日付ラベル付き・active なチューニング
    領域）であり停滞死コードではないため**保持**。AllDifferentGAC も保持。
  - → これで Phase 3（機械的重複削減・死コード削除）は一通り完了。

## 2026-06-15 (続き) Phase 4 着手: LinearConstraintBase

**実施内容**

- **LinearConstraintBase 新設**（commit `0bde8a4`）
  - int_lin_* 7制約（eq/le/ne + reif/imp）のコンストラクタが個別複製していた線形項集約
    （同一変数の係数合算・係数0除外・unique_vars 構築）を `aggregate_terms` に共通化。
    係数列 `coeffs_` も基底へ移動。全7制約を `: public LinearConstraintBase` に変更。
  - 非 reif(eq/le/ne) の冗長な inner-loop（VariablePtr=Variable* なので shared_ptr 探索は不要）を
    排除。reif/imp の b 追加ロジックも aggregate_terms 後の2行に集約。
  - ~80行削減。

**設計判断・理由**

- **potential 管理(min_rem/max_rem/fixed_sum)の差分更新は基底化しない判断**:
  potential の形状が eq=min+max / le=min のみ / ne=fixed_sum+unfixed_count と分岐し、単一基底に
  きれいに収まらない。かつ on_instantiate/on_set_min/on_set_max という **hot path**（IntLinEq は
  celar で 60%）に介入するため、限界的な dedup のために定数倍リスクを取らない。集約（constructor、
  1回限り）のみで安全に ROI を確保した。
- 検証は golden master を主とした。var_ids_ の順序（unordered_map 反復順）が完全保存された
  ことを 205/205 green が保証（順序が変われば golden は全面赤になる）。constructor のみの変更で
  hot path 不介入。

**気づいた課題・TODO**

- Phase 4 残（いずれも伝播ロジックに触れ false UNSAT リスクあり。計画通りランダム全解照合
  テストの併設が前提）:
  - ReifConstraintBase（enforce_b1/enforce_b0/infer_b の3フック化, ~450行）
  - comparison.cpp のドメイン交差・双方向伝播ヘルパ抽出（~200行, 1719行の大ファイル）
  - presolve/propagate 二重実装統一（DomainWriter, diffn/disjunctive）
  - コールバック署名整理（var_idx/internal_var_idx 一本化, フェーズ最後）
- ConstraintTrail はそのまま Reif/Linear 基底に持ち上げ可能。
- circuit の SparseSetPool 化は Phase 4/5 で trail 再設計と合わせて再検討の余地。

**関連ファイル・コミット**

- `5256c19` SparseSetPool 抽出 + test_sparse_set_pool.cpp
- `b803462` ConstraintTrail（int_lin_* 7制約）
- `65eb553` ConstraintTrail（all_different 系2制約）
- 新規共通基盤: `include/sabori_csp/sparse_set_pool.hpp`, `include/sabori_csp/constraint_trail.hpp`
- 計画: docs-dev/refactoring-plan-2026-06.md（Phase 3）

## 2026-06-15 (続き) Phase 4: ReifConstraintBase — eq_reif/ne_reif 統一

**実施内容**

- **ブルートフォース安全網を先行構築**（commit `6957db3`, `tests/cpp/test_lin_reif_brute.cpp`）
  - int_lin_eq_reif / int_lin_ne_reif のランダム小規模インスタンスを `solve_all` で全解列挙し、
    総当たり参照列挙と**解集合一致**を照合。b-free / b-fixed(0/1) / 負係数 / 重複変数（aggregate
    経路）を網羅。806 assertion が**リファクタ前の現行コードで green**であることを確認してから着手。
  - 計画の鉄則「reif 変更には false UNSAT を捕捉するランダム全解照合を併設」を満たす。
- **IntLinEqNeReifBase で eq_reif/ne_reif を統一**（commit `12aad80`）
  - 両者は内部状態（fixed_sum / min,max ポテンシャル / unfixed_count / trail）と差分更新が
    **byte 一致**で、述語 P=(sum==target) の極性のみが異なる。`negated_` フラグで出し分ける基底に集約。
  - b<->述語 のロジック（b 未確定→推論 enqueue、b 確定→矛盾検出）をファイル内で約5回複製していたのを
    `reconcile_b(min_sum,max_sum)` に一本化。`b_true_means_P = !negated_` で eq/ne を代数的に統一。
  - 公開2クラスは name() + コンストラクタフラグだけの薄い派生に。registry / model_simplifier の
    dynamic_cast / pybind11 バインディングは無変更（型は別個のまま保持）。
  - net **~470行削減**（source 748→335, header -58）。

**設計判断・理由**

- **挙動差は「観測上 no-op」と確認した上で安全側に統一**:
  - ne_reif は元々 `Constraint::on_instantiate`（2WL 簿記）を呼んでいなかった（eq/le は呼ぶ）。
    eq/ne_reif は on_last_uninstantiated を持たず watch を伝播に使わないため出力不変 → 呼ぶ側に統一。
  - on_final のガード・確定後の冗長再チェックは eq 版（厳密に安全側）を採用。到達時は必ず true を返す
    no-op であることを代数的に確認。
  - prepare_propagation の初期化を `model.is_instantiated/value`（propagation callback の推奨 API、
    protein リグレッションの教訓）に統一（ne は従来 `is_assigned` だった）。初期化時は両者整合。
  - 空係数時の極性差（eq は b を確定させず check のみ / ne は確定させる）は presolve 後到達不能な
    防御経路だが、挙動を変えないため `negated_` で明示的に保存。
- **le_reif/le_imp は対象外**: b 確定時に線形変数を `propagate_bounds_le/gt` で刈り込む形状で、
  eq/ne（線形側は刈らず b 推論のみ）と構造が異なる。le 用の brute net を併設してから別途検討。
- 検証は golden master（182 fzn の `-a -s` byte 一致＝探索軌道不変）を主、brute 806 を副とした。
  golden byte 一致のためベンチゲートは不要（軌道不変の最強証拠）。

**気づいた課題・TODO**

- Phase 4 残: le_reif/le_imp 統一（要 le 用 brute net）、comparison.cpp ヘルパ抽出（~200行）、
  presolve/propagate 二重実装統一（DomainWriter）、コールバック署名整理（フェーズ最後）。
- `reconcile_b` パターンは le_reif にも応用可能だが、刈り込みと絡むため le 移行時に再設計。

**関連ファイル・コミット**

- `6957db3` brute-force net（test_lin_reif_brute.cpp）
- `12aad80` IntLinEqNeReifBase 統一（int_lin_eq_ne_reif.cpp 新規、eq/ne_reif.cpp 削除）

## 2026-06-15 (続き2) Phase 4: le_reif/le_imp — 共通刈り込みカーネル抽出

**実施内容**

- **brute net を le 系へ拡張**（commit `8d555b0`）
  - `test_lin_reif_brute.cpp` を `Rel` enum（EqReif/NeReif/LeReif/LeImp）に一般化。le_reif の
    双条件、le_imp の含意（b=1→sum<=bound）を参照列挙で網羅。randomized 4 suite + b 固定の
    刈り込みエッジ。1614 assertion を**現行コードで green** にしてから着手。
- **`LinearConstraintBase::prune_sum_le` 抽出**（commit `fe736eb`）
  - `sum<=bound` の境界絞り loop が IntLinLe::propagate_bounds / IntLinLeReif::propagate_bounds_le /
    IntLinLeImp::propagate_bounds で**3 重に逐語複製**されていたのを基底のカーネルへ一本化。
    3 箇所とも委譲に置換。int_lin_le は entailment 早期 return を残してから loop を委譲。
  - カーネルは先頭 `coeffs_.size()` 個を走査 → reif/imp は末尾 b を除外、素の le は b 無しで
    `var_ids_.size()` と一致するため3者で挙動同一。net **-48行**。

**設計判断・理由**

- **le_reif/le_imp の全クラス統一は見送り**。le_imp は le_reif の「b=1 半分」で、
  状態形状が違う（min ポテンシャルのみ・max_rem 無し）、推論も対偶（min_sum>bound→b=0）の
  一方向のみ。統一すると le_imp に未使用の max_rem 追跡と毎コールバックのフラグ分岐を hot path に
  持ち込み、eq/ne 統一で確認した「hot path に限界的 dedup のため介入しない」原則に反する。
  → 真の重複（刈り込みカーネル＝最大の共有ブロック）だけを抽出する方が ROI が高く安全。
- gt 方向（`propagate_bounds_gt`）は le_reif にしか無く重複が無いため対象外。
- 検証は golden byte 一致を主、brute 1614 を副。golden 一致のためベンチゲート不要。

**気づいた課題・TODO**

- Phase 4 reif 系はこれで一段落（eq/ne 統一 + le 系カーネル抽出）。
- Phase 4 残: comparison.cpp ヘルパ抽出（~200行, 1719行の大ファイル）、presolve/propagate
  二重実装統一（DomainWriter, diffn/disjunctive）、コールバック署名整理（フェーズ最後）。
- presolve の固定点刈り込みループ（le_reif/le_imp/le に各々ある直接操作版）も将来 prune_sum_le の
  presolve（直接 remove_above/below）版として共通化の余地あり。

**関連ファイル・コミット**

- `8d555b0` brute net 拡張（Rel enum, le_reif/le_imp）
- `fe736eb` prune_sum_le カーネル抽出（int_lin_base.cpp + le/le_reif/le_imp 委譲）

## 2026-06-15 (続き3) Phase 4: comparison.cpp presolve ヘルパ抽出

**実施内容**

- **scalar comparison brute net 新設**（`tests/cpp/test_cmp_reif_brute.cpp`）
  - int_eq/ne/lt/le + reif/imp を `Cmp` enum で網羅。x,y 範囲を重なり/分離/包含で総当たりし、
    b は free/0/1 全極性。参照列挙と解集合一致を照合。1296 assertion を現行コードで green 確認。
- **presolve ヘルパ2種を抽出**（comparison.cpp）
  - `intersect_eq(model, x, y, changed)`: x==y のドメイン交差（bounds 版 / sparse 版）。
    IntEq::presolve / IntEqReif::presolve(b=1) / IntEqImp::presolve(b=1) の**逐語3複製**を一本化。
  - `enforce_le(model, lo, hi, strict, changed)`: lo<=hi（strict で lo<hi）を直接操作で強制。
    IntLt(strict) / IntLe / IntLeReif(b=1: x<=y) / IntLeReif(b=0: x>y を lo=y,hi=x,strict で表現)。
    le_reif b=0 の「y<x」は enforce_le の引数入替＋strict で元ロジックと一致。
  - net **-69行**（141 del / 72 ins、ヘルパ込み）。

**設計判断・理由**

- ヘルパは comparison.cpp 内の無名 namespace に file-local 配置（これらの制約は Constraint 以外の
  共通基底を持たないため、基底メソッド化せず free function が素直）。直接ドメイン操作（remove_above/
  below, remove）は presolve のみで許される（CLAUDE.md / memory のパターン）ため presolve 専用。
- propagation 側（on_instantiate/on_set_min/max の相互伝播）は b ロジックと絡み presolve ほど
  逐語的でないため今回は対象外。
- 検証は golden byte 一致を主、scalar comparison brute 2910 を副。golden 一致のためベンチゲート不要。

**気づいた課題・TODO**

- Phase 4 残: presolve/propagate 二重実装統一（DomainWriter, diffn/disjunctive）、
  コールバック署名整理（var_idx/internal_var_idx 一本化, フェーズ最後）。
- comparison.cpp の propagation 側相互伝播ヘルパ化は ROI 中で別途。

**関連ファイル・コミット**

- scalar comparison brute net（test_cmp_reif_brute.cpp）
- comparison.cpp: intersect_eq / enforce_le 抽出（IntEq/EqReif/EqImp/Lt/Le/LeReif 委譲）

## 2026-06-15 (続き4) Phase 4: diffn の presolve/propagate 二重実装統一（DomainWriter）

**実施内容**

- **diffn brute net 新設**（`tests/cpp/test_diffn_brute.cpp`）
  - 非重複矩形配置を総当たり列挙 vs solve_all で照合。strict/nonstrict、0面積矩形の免除、
    強制分離、2〜3矩形。現行コードで green 確認後に着手。
- **`diffn_pairwise<Acc>` テンプレートに統一**（commit 続き）
  - `propagate_pairwise`(enqueue) と `propagate_pairwise_direct`(presolve 直接 remove)は
    obligatory-region 分離ロジックの逐語2複製で、差分は (a) bounds 読み出し元
    (`model.var_min` vs `variable()->min()`) と (b) 書き込み (`enqueue_set_*` vs `remove_*`+矛盾検出)
    のみ。accessor ポリシー（`EnqueueAccess`/`DirectAccess`）で読み書き両方を抽象化し本体を1箇所に。
  - net -23行。2つの分離ロジックが乖離するリスクを除去。

**設計判断・理由**

- **読み出しも accessor に含めたのが要点**: presolve 中は var_data_ がラグするため direct 経路は
  Domain を直接読む必要がある（protein リグレッションの教訓）。読み出し元を accessor に閉じ込めて
  各経路の参照源を厳密保存。テンプレートなので伝播 hot path に仮想呼び出しが入らない。
- **disjunctive は DomainWriter 対象外と判定**: `update_compulsory_part_direct` は CP=∅ 前提・
  trail 無しの presolve 特化アルゴリズムで full 版と別物（読み書き方式だけの差ではない）。
  `set_bits`/`set_bits_direct` は bit 幾何のみ共有・trail 挿入有無で分岐するため Trailer ポリシーで
  統合可能だが payoff ~30行・edge-finding 隣接の subtle 領域につき、要 disjunctive brute net の
  別タスクに切り出した（長時間セッション末に安全網無しで複雑 propagator を弄らない判断）。
- 検証は golden byte 一致を主、diffn brute を副。golden 一致のためベンチゲート冗長。

**気づいた課題・TODO**

- Phase 4 残: disjunctive `set_bits` の Trailer 統合（要 disjunctive brute net）、
  コールバック署名整理（var_idx/internal_var_idx 一本化, フェーズ最後）。

**関連ファイル・コミット**

- diffn brute net（test_diffn_brute.cpp）
- diffn.cpp: diffn_pairwise<Acc> + EnqueueAccess/DirectAccess

## 2026-06-15 (続き5) Phase 4: disjunctive set_bits の trail-policy 統合

**実施内容**

- **disjunctive brute net 新設**（`tests/cpp/test_disjunctive_brute.cpp`）
  - 単一資源 no-overlap を総当たり列挙 vs solve_all で照合。strict/nonstrict、0実行時間の免除、
    強制順序、2〜3タスク。現行コードで green 確認後に着手。
- **`apply_set_bits(timeline, start, len, trail)` に統一**（commit 続き）
  - `set_bits`(trail あり・伝播) と `set_bits_direct`(trail なし・presolve)は timeline の
    bit-mask 幾何（語境界 + 端マスク）を逐語複製していた。trail(w) を各語書き込み**直前**に呼ぶ
    ポリシーでカーネル化。direct=no-op、伝播版=旧値退避ラムダ（`timeline_[w]` を書き込み前に読む）。
  - `set_bits` は len<=0 ガード + ensure_dirty_marked を残して委譲（trail dirty マークは作業時のみ）。
  - net -4行。bit 幾何2複製の乖離リスクを除去。

**設計判断・理由**

- **`update_compulsory_part` / `_direct` は統合しない（確定）**: `_direct` は CP=∅ 前提・trail 無しの
  presolve 特化アルゴリズムで、full 版（incremental CP 拡張 + trail）とロジックが別物。
  「読み書き方式だけの差」ではないため DomainWriter 対象外。前回 (続き4) の暫定判断を確定とした。
- 検証は golden byte 一致を主、disjunctive brute を副。

**気づいた課題・TODO**

- Phase 4 残はコールバック署名整理（var_idx/internal_var_idx 一本化）のみ。全制約に波及する
  sweeping 変更のためフェーズ最後に、シグネチャ変更でコンパイラに非互換を検出させる形で実施予定。

**関連ファイル・コミット**

- disjunctive brute net（test_disjunctive_brute.cpp）
- disjunctive.cpp: apply_set_bits<Trail>（set_bits / set_bits_direct 委譲）

## 2026-06-15 Phase 4 まとめ + コールバック署名整理は defer

**本日の Phase 4 成果（実体的 dedup 完了）**

- reif 統一: `IntLinEqNeReifBase`（eq/ne_reif, ~470行）+ le 系 `prune_sum_le` カーネル（~48行）
- comparison.cpp presolve ヘルパ: `intersect_eq` / `enforce_le`（~69行）
- presolve/propagate 二重実装統一（DomainWriter）: diffn `diffn_pairwise<Acc>`（~23行）+
  disjunctive `apply_set_bits<Trail>`（~4行）
- 計 **~610行削減 / 全コミット golden byte 一致**。
- brute 安全網を新規4本追加: lin_reif / cmp_reif / diffn / disjunctive（計 ~2920+ assertion）。
  すべてリファクタ前の現行コードで green を確認してから着手（false UNSAT 捕捉の前提）。

**コールバック署名整理は defer（ユーザー判断）**

- `var_idx`/`internal_var_idx` 一本化は機能改善ゼロ・churn 高（~30ファイル sweep）。実体 dedup が
  完了したため、専用セッションに切り出す。着手方向はメモ済み（plan 参照）: `internal_var_idx` のみ残し
  `var_ids_[internal_var_idx]` で O(1) 導出、`override` でコンパイラに全 override の非互換を検出させる。
- → Phase 4 は署名整理を除き完了。次は Phase 5（Domain 表現・性能）または署名整理の専用セッション。

## 2026-06-15 (続き6) Phase 4: コールバック署名整理（var_idx 除去）— defer 分を実施・Phase 4 全完了

**実施内容**

- **4イベントコールバックから冗長な global `var_idx` を除去**（commit `4312dda`, 48ファイル）
  - `on_instantiate` / `on_set_min` / `on_set_max` / `on_remove_value` は global `var_idx` と
    制約内 `internal_var_idx` の両方を持ち、常に `var_idx == var_ids_[internal_var_idx]` だった。
    `var_idx` を除去し、必要な制約は既存の `var_id(internal_var_idx)` アクセサで導出（注入47箇所）。
  - 基底 virtual 4 + 定義（constraint.hpp/.cpp）、派生 135 override、solver dispatch 7箇所、
    明示的 base/parent 呼び出し（`Constraint::on_*` / `AllDifferentConstraint::on_*`）を更新。
  - 自己呼び出し2件（IntTimes/IntAbs の on_set_max → on_set_min 委譲）も引数除去＋不要注入削除。
  - 機械変換は Python スクリプト（param ユニット置換 / base 呼び出し / solver dispatch / var_idx 注入を
    paren・brace マッチで処理）。残存旧シグネチャ 0 を grep で確認。
- **ユニットテスト修正**（test_constraints / graph / linear / alldifferent）
  - `test_constraints.cpp`: 比較制約（IntLt/Le/Eq/Max/Min/EqReif）は「実グローバル id + dummy
    internal=0」の**不整合ペア**を渡していた。旧コードは var_idx しか見ないので通っていたが、リファクタで
    internal が真実源になるため正しい内部 index へ補正（x→0 / y→1 / m·b→2、コンストラクタ
    `extract_var_ids({x,y,m/b})` 順で確認）。
  - graph/linear/alldiff: リテラル index を渡しており internal 値が保存されるため第3引数を落とすだけ
    （挙動不変）。
- **検証**: golden master **182/182 byte一致**（探索軌道完全保存）、ctest **222/222**、
  Python **100 passed**、全ターゲットビルド成功。

**設計判断・理由**

- **安全網は `override` キーワード**: 当初 grep で「派生宣言に override 未付与」と判定したが、これは
  multi-line 宣言の override が最終行にありメソッド名行と分離するための**grep アーティファクト**だった。
  paren マッチで再検証した結果、派生 135 宣言は全て override 済みと判明。よって基底署名を変えるだけで
  全 override の非互換をコンパイラが検出する状態が既に成立しており、追加の override 付与は不要だった。
- **internal_var_idx を唯一の真実源に**: dispatch が (var_idx, internal) の不整合ペアを渡す余地を構造的に
  排除。var_idx を使わない hot-path 線形/scheduling コールバックは引数1つ減（IntLinEq on_set_min/max は
  元から internal_var_idx 使用なので導出コストも無し）。
- net +39行（注入47 が param 削減を相殺）。目的は行数削減でなく**重複・不整合源の除去**（設計衛生）。
  golden byte 一致のためベンチゲートは不要（軌道不変の最強証拠）。

**気づいた課題・TODO**

- **Phase 4 全完了**（実体 dedup + 署名整理）。次は Phase 5（Domain 表現・性能, 高リスク・ベンチゲート
  厳格）または Phase 6（solver.cpp 再分解）。Phase 5 は memory 上「removed_set_/pool_sparse_/remove_value/
  rewind は性能 ROI 低」のため主目的は設計衛生になる点に留意。
- 署名整理がテストの隠れた (var_idx, internal) 不整合を**顕在化・修正**した（リファクタの副次的健全化）。

**関連ファイル・コミット**

- `4312dda` 署名整理（48ファイル、注入47、golden 182 byte一致 + ctest 222 + python 100）
- 計画更新: docs-dev/refactoring-plan-2026-06.md（Phase 4 全完了マーク）

## 2026-06-15 (続き7) Phase 6: solver.cpp ヘルパ抽出（二重実装解消 + optimize スリム化）

**実施内容**

- `search_with_restart` / `search_with_restart_optimize` に二重実装されていたブロックをヘルパへ抽出。
  - **mode_reward EMA 更新 + mix_p 再抽選**（byte一致32行重複）→ `update_mode_reward_and_resample()`
    （commit `9702606`）
  - **restart 共通簿記**（restart_count++ / community 動的レポート / select_best_assignment / bloom リセット /
    restart pivot / NoGood GC+rebuild / activity 減衰）→ `apply_restart_bookkeeping(model)`（commit `d13afc3`）
- optimize 専用の肥大ブロックを分離（単一使用だが探索ループと decouple）。
  - **疑似勾配計算 + 勾配変数選択**（~40行）→ `compute_improvement_gradient(const Model&)`（commit `57a4bbf`）
  - **improvement probe**（~175行）→ `run_improvement_probe(model, callback, root_point)` が `ProbeAction`
    enum を返す（commit `8d1a2f6`）
- 計画更新（commit `bde498c`）。

**設計判断・理由**

- **経路差は呼出側に残置**: 非最適化版のみの `temporal_activity_` リセット、optimize のみの gradient
  リセットはヘルパに含めず inline 維持。ヘルパに巻き込むと optimize 側で挙動が変わる（軌道破壊）リスク。
- **probe の break/return を enum 化**: probe 内の `break`(timeout) → `BreakInnerLoop`、
  `return best_solution_`(最適確定) → `ReturnOptimal`、正常完了 → `Continue`。呼出側は **if 連鎖**で
  while ループを break/return に変換（`switch` 内 `break` は switch を抜けてしまうため。`break` が
  while に直接効く形を採用）。既存の `handle_ascent` → `AscentAction` enum と同パターンで idiomatic。
- **全 commit golden byte一致**＝ rng 系列を含む探索軌道の完全保存。軌道不変が最強証拠のためベンチ
  ゲートは不要（過去の署名整理コミットと同方針）。

**成果**

- `search_with_restart_optimize`: ~460 → ~200 行、`search_with_restart`: ~200 → ~150 行。
  両 search ループが構造的に並行になり可読性が大きく向上。
- ctest 222/222 + python 100 passed、golden 182 byte一致（全コミット）。

**気づいた課題・TODO**

- **次段**: 抽出したヘルパのクラス化（RestartController / ModeRewardPolicy / GradientStrategy）と単体
  テスト追加。solver.cpp の **900行以下**目標はクラス化フェーズで達成予定（現状 1753 行、ヘルパ抽出は
  ファイル内移動のため行数は横ばい）。
- cycle begin/end 構造の共通化は見送り（optimize は `domain_count` 追跡 + `cycle_interrupted` で形状が
  異なる）。
- 残: 統計記録の ConstraintStats レイヤ化、find_all + restart の解列挙設計レビュー。

**関連ファイル・コミット**

- `9702606` update_mode_reward_and_resample / `d13afc3` apply_restart_bookkeeping /
  `57a4bbf` compute_improvement_gradient / `8d1a2f6` run_improvement_probe / `bde498c` 計画更新
- `src/core/solver.cpp`, `include/sabori_csp/solver.hpp`, `docs-dev/refactoring-plan-2026-06.md`

## 2026-06-15 (続き8) Phase 6: 状態クラスタのクラス化（ModeRewardPolicy / GradientStrategy）

**実施内容**

- **ModeRewardPolicy クラス新設**（`include/sabori_csp/mode_reward_policy.hpp`, header-only, commit `e6ceaf9`）。
  mode_reward EMA 抽選の状態（reward[5] / p_idx / mix_p / improvement / max_depth）と更新・再抽選ロジックを
  カプセル化。Solver からは `mix_p()` / `note_improvement()` / `observe_depth()` / `update_and_resample(rng)` /
  `set_fixed()` で利用。`update_mode_reward_and_resample()` メソッドと関連メンバ・定数を Solver から削除。
- **GradientStrategy クラス新設**（`gradient_strategy.{hpp,cpp}`, commit `9484825`）。optimize 専用の疑似勾配
  ヒント状態（gradient / hint var-dir-ref / eligible / prev solution）と `rebuild_eligible` / `compute` を
  カプセル化。値順序付け（order_values / create_search_frame）の読み出しと、散在していた gradient リセット
  5箇所を accessor（`hint_active_for` / `direction` / `ref_val` / `consume_hint` / `disable_hint` / `clear` /
  `set_prev_solution`）に集約。未使用だった `all_constraints` ローカルも除去。
- **単体テスト 2本追加**: `test_mode_reward_policy.cpp`（5 TC: default / set_fixed / グリッド値域 / 決定性 /
  改善バイアス, commit `1a81e71`）+ `test_restart_controller.cpp`（7 TC: reset / begin_cycle / advance_inner
  幾何増加 / end_cycle grow-shrink / cap-floor / reset_outer, commit `44b63d4`）。

**設計判断・理由**

- **ModeRewardPolicy は header-only**（状態 + 純ロジックのみで Model 非依存・軽量）。**GradientStrategy は
  hpp/cpp 分離**（`compute` が Model の全 API を使うため。header では Model を前方宣言、value_type は
  domain.hpp で軽量解決）。
- **gradient リセットの散在を集約**: `gradient_var_idx_ = SIZE_MAX; gradient_direction_ = 0;` が5箇所に
  散在していたのを `disable_hint()`、値順序付けで一度使ったら解除する箇所を `consume_hint()` に統一。
  ヒント有効判定 `var_idx == gradient_var_idx_ && gradient_direction_ != 0` は `hint_active_for(var_idx)` に。
- **rng 消費系列を厳密保存**: 抽選（mode_dist / idist）と値順序付けの rng 呼び出し順を完全保存。全 commit
  golden byte一致で探索軌道不変を確認（ベンチゲート不要）。

**成果**

- solver.cpp 1,784 → 1,650 行。restart 制御は RestartController / ModeRewardPolicy / GradientStrategy の
  3クラスに整理完了。状態 + ロジックが焦点クラスにまとまり単体テスト可能に。
- ctest **234/234**（golden 182 byte一致 + 新規 12 TC）+ python **100 passed**。

**気づいた課題・TODO**

- **900行目標**には統計記録の ConstraintStats 層分離 + `search_with_restart` 系の更なる共通化（cycle
  begin/end は domain_count/cycle_interrupted の形状差で今回見送り）が必要。
- GradientStrategy.compute は Model 依存のため単体テストは golden に委ね、純状態アクセサのみ対象（compute の
  ロジックは optimize 問題の golden で網羅）。

**関連ファイル・コミット**

- `e6ceaf9` ModeRewardPolicy / `1a81e71` ModeRewardPolicy test / `9484825` GradientStrategy /
  `44b63d4` RestartController test / `52bc24b` 計画更新
- `include/sabori_csp/mode_reward_policy.hpp`, `include/sabori_csp/gradient_strategy.hpp`,
  `src/core/gradient_strategy.cpp`, `tests/cpp/test_{mode_reward_policy,restart_controller}.cpp`,
  `src/core/solver.cpp`, `include/sabori_csp/solver.hpp`

## 2026-06-15 (続き9) Phase 6: search ループ系の更なる共通化

**実施内容**

- **restart 末尾の共通4行**（`mode_policy_.update_and_resample` + `var_selector_` シャッフル +
  `init_tracking` + `unassigned_trail_` クリア）→ `resample_and_reshuffle(model)`（commit `1ca8ae2`）。
- **timeout エピローグ**（community 動的レポート + verbose ログ + `sync_nogood_stats`）→
  `finish_search_on_timeout()`（commit `1ca8ae2`）。戻り値差（通常 nullopt / 最適化 best_solution_）は
  呼出側に残置。
- **find_all SAT 処理（~58行）** → `handle_find_all_solution(model, callback, result, root_point)` が
  `FindAllAction` enum（ContinueLoop / Stop）を返す形に分離（commit `6d33cf2`）。

**設計判断・理由**

- **非対称部分は呼出側に残しフラグ引数を回避**: restart 末尾の前後にある「通常のみ temporal_activity_
  リセット」「最適化のみ gradient disable」は位置が異なる（前 / 後）。`resample_and_reshuffle` は両者で
  連続・同一の4行だけを切り出し、非対称部分は呼出側の元の位置に残した（bool フラグ引数の smell を回避）。
- **内側 SAT 処理を focused helper へ委譲する対称構造に統一**: optimize は `run_improvement_probe`
  （ProbeAction）、通常 find_all は `handle_find_all_solution`（FindAllAction）。両 search ループが
  「outer cycle + inner loop で SAT 処理を helper に委譲、restart は共通 helper」という並行構造になった。
- **UNKNOWN backtrack プロローグ・cycle begin/end の共通化は見送り**: UNKNOWN は break/return・戻り値・
  `current_decision_` リセット・Stopped/Conflict の扱いが分岐し、cycle は domain_count 追跡 +
  cycle_interrupted で形状が異なる。シグナル enum 無しでは綺麗に収まらず、2箇所のみ・分岐本体も別物
  （find_all 列挙 vs branch-and-bound）のため callback/template 間接化は over-engineering と判断。

**成果**

- `search_with_restart` ~141 → ~85 行。両 search 関数が薄く対称な骨格に。
- 全 commit golden 182 byte一致（探索軌道不変）+ ctest **234/234** + python **100 passed**。

**気づいた課題・TODO**

- **900行目標**には統計記録の ConstraintStats 層分離が残る（solver.cpp は helper を同ファイル内に持つため
  総行数は微増だが、関数本体の肥大・重複は解消済み）。次段はそこ、または Phase 5（Domain 表現）へ。

**関連ファイル・コミット**

- `1ca8ae2` resample_and_reshuffle + finish_search_on_timeout / `6d33cf2` handle_find_all_solution /
  `aea41cd` 計画更新
- `src/core/solver.cpp`, `include/sabori_csp/solver.hpp`, `docs-dev/refactoring-plan-2026-06.md`

## 2026-06-15 (続き10) Phase 6: 統計記録の ConstraintStats レイヤ集約

**実施内容**

- verbose 統計記録（type別 `constraint_stats_` + instance別 `instance_stats_` の
  call/fail/fail_depth/reduction）+ 失敗時 `bump_activity` のパターンが3経路に重複していた:
  `propagate_instantiate` / `process_queue` の `invoke_cb` ラムダ / batch propagator。
- member template `record_constraint_call(model, constraint_idx, bump_var_idx, call)` に一本化
  （commit `2e05e7e`）。`process_queue` の `invoke_cb` は `(v_idx, w, call)` を転送する薄いアダプタに、
  他2経路は inline の verbose/非verbose 2系統重複を解消。

**設計判断・理由**

- **inline template で hot path をインライン化**: `process_queue` / `propagate_instantiate` は伝播の
  ホットループ。template を `inline` 宣言、call は `[&]` ラムダ thunk で渡すため、非verbose codepath は
  構造的に従前と同一の codegen になる。
- **header template が成立する根拠**: `Constraint` は solver.hpp で完全型（model.hpp → constraint.hpp）の
  ため、template 本体の非依存式 `constraints[idx]->name()` が定義時に解決可能。
- **hot path 二重検証**: golden byte一致（伝播回数・ノード数同一）に加え、プロジェクトルール（hot path 変更は
  ベンチゲート）に従い A/B 計測を実施。tsp_large / magic_square_large を 30回反復で before/after 比較し、
  ノイズ範囲内で非劣化を確認（after が同等〜やや速い）。

**成果**

- solver.cpp 1,654 → 1,600 行。制約呼び出しの統計記録が単一の ConstraintStats レイヤに集約。
- ctest **234/234**（golden 182 byte一致）+ python **100 passed**。

**気づいた課題・TODO**

- **900行目標**には `run_search` 系（明示スタック探索ループ）/フレーム管理の別ファイル分離が残る。重複・
  関数肥大は解消済みで、残りは「ファイル分割」の領域。次段はそこ、または Phase 5（Domain 表現）へ。

**関連ファイル・コミット**

- `2e05e7e` record_constraint_call 集約 / `867c17b` 計画更新
- `include/sabori_csp/solver.hpp`, `src/core/solver.cpp`, `docs-dev/refactoring-plan-2026-06.md`
