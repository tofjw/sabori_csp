## 2026-03-06 04:19 array_bool_xor 制約の実装

**実施内容**
- `array_bool_xor` 制約を新規実装
  - FlatZinc仕様: `array_bool_xor(array[int] of var bool)` — 配列全体のXOR（パリティ）が true であることを要求
  - `ArrayBoolXorConstraint` クラスを `logical.hpp/cpp` に追加
  - presolve / on_instantiate / on_final_instantiate / on_last_uninstantiated を実装
  - `constraint_registry.cpp` に登録
- FlatZinc統合テスト3件を追加（simple, two_vars, all_solutions）
- `docs/CONSTRAINTS.md` に制約を追記
- 全テスト（160件）パス確認

**設計判断・理由**
- `array_bool_and/or` と異なり結果変数なし（FlatZinc仕様に準拠）
- 2-watched literal は不使用（XORは全変数がパリティに影響するため早期打ち切り不可、単純カウントで十分）
- 残り1変数時にパリティから値を推論する伝播で完全

**気づいた課題・TODO**
- なし

**関連ファイル**
- `include/sabori_csp/constraints/logical.hpp`
- `src/core/constraints/logical.cpp`
- `src/fzn/constraint_registry.cpp`
- `tests/fzn/constraints/array_bool_xor/`
- `docs/CONSTRAINTS.md`

## 2026-03-06 14:28 bump_activity の制約固有化

**実施内容**
- `bump_activity` を制約側の仮想メソッドに委譲するリファクタリングを実施
  - `Constraint` 基底クラスに `virtual bump_activity()` を追加（デフォルト: 全 instantiated 変数に均等加算）
  - `Solver::bump_activity` を仮想メソッドへの委譲に変更（`trigger_var_idx` を追加）
  - `solver.cpp` の全6箇所の呼び出しを更新
- `AllDifferentConstraint` に override を実装
  - 値重複（pool_sparse_ で検出）→ 同じ値を持つ変数のみ bump（通常2変数）
  - 鳩の巣原理等 → デフォルト動作にフォールバック
- rescale 閾値チェックの重複を `bump_variable_activity` 静的ヘルパーに共通化
- all_different を含むベンチマーク抽出スクリプト `bench_alldiff.py` を作成
  - mznc2023/2024/2025 から all_different 使用問題を自動検出（20問）
- 全テスト（160件）パス確認

**設計判断・理由**
- `activity` 配列ポインタと `need_rescale` フラグを渡す設計により、Solver の内部状態への依存を最小化
- `bump_variable_activity` を静的メソッドとしてヘルパー化し、base/override 間の rescale ロジック重複を排除

**気づいた課題・TODO**
- ベンチマークで実際に効果があるか要検証（`bench_alldiff.py` で before/after 比較）

**関連ファイル**
- `include/sabori_csp/constraint.hpp`
- `include/sabori_csp/constraints/global.hpp`
- `include/sabori_csp/solver.hpp`
- `src/core/constraint.cpp`
- `src/core/constraints/global/all_different.cpp`
- `src/core/solver.cpp`
- `benchmarks/minizinc_challenge_2025/bench_alldiff.py`

## 2026-03-06 制約ごとの統計情報を `-v` で出力する機能を追加

**実施内容**
- `ConstraintStats` 構造体を `solver.hpp` に追加（call_count, reduction_count, fail_count, fail_depth_sum）
- `Model::pending_updates_size()` を追加（伝播キューサイズでドメイン削減を検出）
- `solver.cpp` の `propagate_instantiate()` と `process_queue()` 内の6箇所のコールバック呼び出しに計測コードを挿入
  - `verbose_` ガードで非verbose時のオーバーヘッドをゼロに
- `main.cpp` の `print_stats()` に制約統計テーブル出力を追加（呼び出し回数降順ソート）
- Fails 列の幅を 8→10 に修正（大きな数値で列が結合する問題を修正）

**設計判断・理由**
- `verbose_` ガードで if/else 分岐し、非verbose時は既存コードそのままを実行（計測オーバーヘッドゼロ）
- `pending_updates_size()` の差分でドメイン削減を検出（制約がenqueueしたかどうかで判定）
- 制約名は `Constraint::name()` を使用し、同名制約タイプをまとめて集計

**気づいた課題・TODO**
- なし

**関連ファイル**
- `include/sabori_csp/solver.hpp`
- `include/sabori_csp/model.hpp`
- `src/core/solver.cpp`
- `src/fzn/main.cpp`
