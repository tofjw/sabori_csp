# 2026-07-09

## 2026-07-09 大負け問題の機構別分類 + G1(a) ヒューリスティクス設計

**実施内容**
- 「証明支配」一括りを解体: constraints3 の CP-SAT 勝ち 106 問から時間差のみの
  60 問を除いた 46 問を、解の到達度 × 目的関数構造 × CP-SAT 証明時間で 6 群に分類
  → docs-dev/loss-taxonomy-20260709.md
- G1 (ペナルティ和・下界崩壊型 7問) の指紋を定量化: obj ドメインと最適値の乖離が
  数桁 (zephyrus 5..100000/最適12、arithmetic-target 1..73268/最適5)
- G2 (微小整数最適の UNSAT 証明型 7問) だけが真の学習支配コア。
  G3 (数値 bound 型 5問) は TTEF/precedence LB で内製可能、G4 (充足未達 4問)、
  G5 (set 1問)、G6 (漸進差 12問)
- G1(a) 対策の設計: 既存機構の棚卸で「incumbent 誘導・改善プローブは実装済みだが
  G1 では不発/逆効果」と判明 → bottom-up optimistic restart (H-A) を本命に、
  cone 零極性 (H-B)、dive (H-C) を控えに設計

**気づいた課題・TODO**
- [ ] H-A 実装 (opt-in): run_improvement_probe の lb 側対称形 + UNSAT 時の
      root lb 引き上げ
- [ ] G3: rcpsp/mapping/depot の TTEF・precedence LB 強化は別スレッド

## 2026-07-09 H-A 実装: bottom-up optimistic probe (SABORI_BOTTOMUP, opt-in arm)

**実装** (`run_bottomup_probe`, improvement probe の対称形)
- リスタート時に obj <= lb+δ を投機: SAT → 準最適へジャンプ / 証明付き UNSAT →
  root で lb 引き上げ (健全) / UNKNOWN → δ 半減 + 長期撤退 (skip 64<<streak)
- **staircase ポリシー**: UNSAT が続く限り 1 呼び出し内で連続して階段を登る
  (生産的な間だけ投機を続け、決着しない問題では本探索に道を譲る)
- 学習 NoGood は仮定 (Leq/Geq) を decision literal に含む条件付き連言 = 大域健全

**実装中に踏んだバグ 2 件（教訓）**
1. run_search の cl は「フレーム毎リトライ予算」で深さ方向に爆発する
   (500 指定で 217k fails)。グローバル予算は restart_fail_cutoff_ を使う
2. probe 後に var_selector_.init_tracking + unassigned_trail_.clear() が必須
   (improvement probe の呼び出し元と同じ処置)。怠ると本探索が解を見つけられなく
   なる (gfd で解ゼロとして発現)

**ポリシー試行錯誤**: 固定500=中立 → 適応倍増=全面悪化 (投機が本探索を食う) →
staircase + 長期撤退=採用

**結果 (30s, off vs SABORI_BOTTOMUP=20000)**
| 問題 | off | on20k |
|---|---|---|
| zephyrus15 (opt=12) | 5834 | **12 = 最適値** (bench経路 CHECK_OK, gecode/CP-SAT とも一致) |
| gfd-schedule22 (opt=9) | 642 | **142** |
| roster-shifts-bool23 (MAX) | 27975 | **28890** |
| arithmetic-target22 | 108 | 3106 (悪化) |

問題依存の諸刃 → **opt-in arm として温存** (LNS/-C と同じ棚)。既定オフのため
ctest 299/299・golden 260 不変。budget は 500 だと中立、20000 で大勝ち/負け
が分かれる = ポートフォリオ arm としてワーカー 1 本に載せる価値あり

**TODO**
- [ ] budget/δ/撤退のグリッドを bench 化 (bench_restart_policy.py の流儀)
- [ ] feature/mp 統合時に portfolio arm として配線 (zephyrus/gfd 型を拾う)

## 2026-07-09 追記: UNSAT 側予算と activity 汚染の検討（ユーザ指摘）

- **fail 予算は UNSAT 側にも適用済み**: 各階段ステップは restart_fail_cutoff_ 下で
  走り、予算内に枯渇証明が完了したときだけ UNSAT 扱い（超過は UNKNOWN → 撤退）。
  staircase の累計は「最大 64 段 × 各段予算」が上限
- **activity 汚染の隔離を実装して A/B** (SABORI_BOTTOMUP_ISOLATE, opt-in):
  probe 前後で activity_/temporal_activity_ を snapshot/restore
  （NoGood・lb・best は健全な成果物なので残す）
- 結果 (30s, budget=20000): zephyrus 12/12 同着、**gfd は隔離で悪化 (142→637)**
  = tight bound 下の conflict activity は G1 で正の転移になる、
  roster/arithmetic は隔離で微改善 = 汚染が効く問題もある → 両刃
- 判定: 両ノブとも opt-in 温存、budget × isolate のグリッドを bench 化 TODO に統合

## 2026-07-09 追記: 相転移カットオフ（ユーザ提案）

**仮説**: SAT/UNSAT 境界に接近すると UNSAT 証明が指数的に重くなる（相転移）
→ コスト急増を検知したら階段を切るべき

**実測（step_fails 計装、zephyrus opt=12）**: target=5: **0** → 7: 2,519 →
9: 4,751 → 11: **>20,000（壁）**。超線形上昇 = 相転移プロファイルが数値で確認。
gfd は初段（lb=4, opt=9）から壁 = 改善の源泉は activity 転移のみと判明

**実装**: 証明成功でも step_fails > 予算/8 なら「境界接近」と判定して階段を
停止（lb 進捗は保持）、UNKNOWN と同じ長期撤退。壁ステップの予算丸損を予防

**結果（30s, budget=20000, cutoff なし → あり）**: zephyrus 12→12 維持
（target=7 で切り上げても lb 伝播で本探索が完走）、gfd 142→142、
roster 28890→28890、**arithmetic 3106→887（負け幅 1/3.5）**。全問題で改善か同等

## 2026-07-09 グリッド bench (bench_bottomup.py) → 既定値確定

**グリッド**: budget {2k, 20k, 80k} × isolate {0,1} × cutoff {/8, off}、
G1 8問 + 対照2問 (tsptw, rcpsp)、2シード × 30s、基準 = off

**結果 (net vs off, 問題×シード単位)**
| 構成 | net | 備考 |
|---|---|---|
| **b2k (cutoff=/8, iso=0)** | **+5 (8勝3敗9分)** | 勝者 |
| b2k-iso | −3 | 隔離は害 (activity 転移が有用) |
| b2k-nocut | ±0 | **cutoff が +5 の源泉** |
| b20k / b80k 系 | −2〜+2 | 大予算の一律優位なし |

**個別の発見**
- **b2k が「解なし→解あり」×2**: collaborative-construction (全構成中唯一 obj=12)、
  code-generator (off=NA → 6340 = CP-SAT の最適値)
- 対照 (tsptw/rcpsp) は全構成で不変 = 非 G1 への害なし
- arithmetic-target だけ 80k で最適値 5 到達 (budget の一律最適は存在しない
  = portfolio arm 側で budget 違いのワーカーを並べる余地)
- zephyrus はこの 2 シードでは off も 12 到達 (シード揺れ大の問題)

**既定値の確定**
- budget=2000 / cutoff=/8 / isolate=off をコード既定に
- `SABORI_BOTTOMUP=1` = 既定構成での有効化 (糖衣)
- arm 自体は既定オフのまま (諸刃プロファイル。feature/mp の portfolio arm 候補)

## 2026-07-09 G3 攻略可能性調査

**実施内容**
- G3 (数値 bound 証明型) 5問に bottomup probe を適用して攻略可能性を調査
  - まず既定 arm (b2k/b20k, 30s): 全問 SOL のまま証明ゼロ、obj も実質不変
  - -v で階段を観察 → rcpsp は lb 45→60 が step_fails=0（TTEF+precedence 即答）、
    68 まで無料同然と判明 → 「prover 構成」(budget 10M / cutoff 無効) を試行
- prover 構成の結果:
  - **rcpsp13: OPTIMAL 完全証明 118s 単スレ**（CP-SAT 12s の約10倍だが閉じる。
    probe が incumbent 発見 82→79→77 と lb 証明の両輪を担った）
  - mapping15 / depot16: probe jackpot で最適値 (793 / 13995) は即発見するが、
    lb が 511/793・8191/13995 の壁で 300s でも不動
  - largescheduling: lb が incumbent の 63% まで全段無料登坂、b20k で obj 微改善
  - liner-sf: 毎段 ~20k fails × obj スケール 4M で階段が構造的に遅い（arm 微負）

**設計判断・理由**
- **G3 を G3a/G3b に分割**（loss-taxonomy-20260709.md に記録）
  - G3a (schedule-bound 型) = 攻略可能。obj≤K が cumulative/precedence に直結し
    lb 階段の大部分が無料。G1 は jackpot (SAT側) が武器、G3a は UNSAT 側階段が本体
  - G3b (int_max 集約型) = 射程外。obj=max(e_i) の「obj>K」は全 e_i≤K の一括
    フィージビリティ反証で漸進化できない → 節学習の領分、外部ポートフォリオ枠
- prover 構成は単スレ 30s では解探索を飢えさせる（rcpsp UNKNOWN 化）ので
  既定にはしない。feature/mp で budget 違いワーカー（b2k + prover 10M）を
  並べるのが正しい形

**気づいた課題・TODO**
- budget は壁の1段を跨げるかの閾値（rcpsp: 1M 不可 / 10M 可）。prover 亜種は
  グリッド既定 2000 と別物として扱う
- probe の root lb 引き上げはワーカー内限り → feature/mp 統合時に
  **lb 共有**（現状 ub=best_obj のみ）を検討
- TTEF 強化（energetic reasoning）は G3a の壁段を直接削る候補（118s→12s 方向）

**関連ファイル・コミット**
- docs-dev/loss-taxonomy-20260709.md（G3 節更新 + 調査セクション追記）
- 計測: scratchpad の g3_*.fzn（2013 rcpsp inst11 / 2015 mapping full2x2 /
  2016 depot att48_6 / 2019 liner fm3_0 / 2015 largescheduling 0100-1）

## 2026-07-09 G1(b) LP surrogate bound 上限計測

**実施内容**
- ユーザ提案「presolve 変数消去の流れで surrogate ペナルティ和制約を導出
  (LP 双対のサブセット)」の到達上限を、実装前に外部 LP で計測
- lp_bound_probe.py 新規作成 (FZN → LP 緩和抽出、scipy/HiGHS)
- G1 8問 + G3b 2問で LP bound vs dom bound vs 最適値を比較

**設計判断・理由**
- 「上限 = 線形緩和 LP 値」を先に測ることで、bounded-FM 等の実装に入る前に
  生死判定できる (死産なら設計不要)
- 結果: zephyrus (5→10, opt12) / code-generator (0→6340=opt) で有意、
  gfd/arithmetic/network は LP 不可視の結合 (≠網・element) で死産
- 詳細は loss-taxonomy-20260709.md の G1(b) セクション

**気づいた課題・TODO**
- 次: FZN に LP bound を注入して sabori±bottomup で end-to-end 検証
  (証明が実際に閉じるか)、bounded-FM の Python プロトタイプで
  「presolve 内でどこまで LP 値に迫れるか」を計測
- 実装形態の選択肢: (a) 有界 FM 消去 (b) dual ascent (c) 軽量 LP optional 依存

**関連ファイル・コミット**
- benchmarks/minizinc_challenge/lp_bound_probe.py (untracked, bench 慣行)
- docs-dev/loss-taxonomy-20260709.md

## 2026-07-09 G1(b) surrogate 実装トライアル (注入実験 + FM プロトタイプ)

**実施内容**
- lp_bound_probe.py に --emit (bound 注入 FZN 生成) を追加し、
  zephyrus / code-generator で {base, prover, 注入, 注入+prover} を計測
- 有界 FM 消去のプロトタイプ fm_bound_probe.py を作成し、presolve 実装の
  到達可能性を計測

**結果と設計判断**
- **presolve LP surrogate 案は不採用で確定**(詳細 loss-taxonomy-20260709.md):
  FM は cap をどう緩めても reif big-M 網で崩壊 (lb=dom lb)。一方
  bottomup prover が同じ bound に動的到達し、しかも超える
- **副産物が本日最大の成果: prover 構成が zephyrus (300s) と
  code-generator (0.39s!) を単スレ OPTIMAL 証明** — G1 大負け筆頭2問の反転
- 注入は解品質には効く (zephyrus b2k 14→12@60s) が証明は加速しない

**気づいた課題・TODO**
- prover arm (budget 10M / cutoff off) の feature/mp 編入価値がさらに強化
  (G3a rcpsp + G1 zephyrus/code-generator が単スレで閉じる)
- prover 構成は 30-60s 窓では飢餓リスク (zephyrus 60s で obj 66330)
  → 短時間窓では b2k、長時間/並列で prover の使い分け
- lp_bound_probe.py は「LP 可視の結合か」の事前判別に温存

**関連ファイル・コミット**
- benchmarks/minizinc_challenge/{lp_bound_probe,fm_bound_probe}.py (untracked)
- docs-dev/loss-taxonomy-20260709.md

## 2026-07-09 presolve ギャップ分析 + 物量計測

**実施内容**
- presolve/変数削減の実装棚卸し (ModelSimplifier / Solver::presolve /
  ビルド時パス / 探索準備) と標準カタログの突き合わせ
  → docs-dev/presolve-gap-analysis-20260709.md
- ギャップ候補の物量計測 presolve_volume_probe.py を新規作成、
  274問 corpus (.fzn_cache_one_hot) で計測

**設計判断・理由 (計測に基づく優先度)**
- 採用候補A = 制約 hash-consing (dup_row + dup_reif b統合):
  stripboard/community-detection で変数の 11-12%、harmony で制約の 16% の
  局所物量。one-hot 集約が「重複を含むグループを集約しない」ため解禁効果も
- 採用候補B = root probing: 静的計測不能、G4 で直接 A/B すべき
- 不採用: dual fixing (59個) / pure literal (29個) / 素 int_eq (36個) /
  union-find 等価マージ (現 ModelSimplifier が上限の 86% 達成済み)
- 教訓: minizinc コンパイラの前処理が強く、MIP presolve 定番の多くは
  FZN 入力に物量が残らない。残るのは経路違いの重複 reif と探索的手法のみ

**関連ファイル・コミット**
- docs-dev/presolve-gap-analysis-20260709.md (新規)
- benchmarks/minizinc_challenge/presolve_volume_probe.py (untracked, bench 慣行)

## 2026-07-09 候補A (制約 hash-consing) 実装 + ポインタ順修正の移植

**実施内容**
- FZN ビルド層に hash-consing 実装 (257427e): 完全重複行除去 + 同一条件
  reif の b 統合。全テスト 299 + golden 260 green
- 効果 A/B (30s×2seed×10問): net −1/20 = wash → 衛生として既定 ON 採用
- 初回 A/B の zephyrus 怪現象 (dedup no-op なのに軌道差) から
  **ポインタ順非決定性の残存を発見**: aggregate_terms の unordered_map
  反復。feature/mp のみにあった修正 9a06ccd を cherry-pick (e64d480)

**設計判断・理由**
- dedup は「意味論的に無料の厳密重複除去」なので wash でも既定 ON
  (LNS/probe 系の行動変更 arm とは性質が違う)。kill switch 温存
- one-hot 解禁ボーナス仮説は実測で棄却 (重複 reif は le_reif 系だった)
- 対照問題の on/off bit 一致で決定性回復を確認してから A/B を判定

**気づいた課題・TODO**
- feature/mp との branch 間で修正の相互移植漏れがないか、マージ時に要確認
- 候補B (root probing) は未着手 — G4 問題での A/B が次

**関連ファイル・コミット**
- 257427e (dedup), e64d480 (pointer-order fix 移植)
- docs-dev/presolve-gap-analysis-20260709.md

## 2026-07-09 constraints4→5 の TIMEOUT 増加調査

**実施内容**
- 20260707_constraints4 (07-08) → constraints5 (07-09) で sabori TIMEOUT 18→27
  / OPTIMAL 115→111 の原因を調査 (CP-SAT 対照は不変 = 環境ノイズではない)
- 定量化: 両OPT 74問の時間比 中央値1.021 (減速なし・分散は拡大)、
  両SOL の obj 勝敗 34勝38敗57分 (対称)
- 切り分け: SOL→TIMEOUT 9問中6問は dedup 発火ゼロ。89464dc (変更前) の
  バイナリを worktree ビルドして直接対決 → portal 旧1/新0、collab 旧2/新0、
  yumi 旧11/新5、cable 旧78/新31 で **主因 = e64d480 (変数順安定化) の
  全域軌道変化**と確定。dedup は副次的・符号混在 (cg19 は ON が良い)

**設計判断・理由**
- **リバートしない**: c4 の好成績の一部はヒープレイアウト依存 (環境依存で
  再現不能) の偶然の変数順に乗っていた。e64d480 は計測の再現性に必須
  (dedup A/B の幻の差はこれ無しで誤診しかけた)
- **constraints5 を新 baseline に**。失われた9問はナイフエッジ (初解 30s
  窓際)。系統的処方箋は feature/mp の並列シード多様性 (mondoku 前例と同じ)

**関連ファイル・コミット**
- e64d480 (主因・意図的修正), 257427e (副次)

## 2026-07-09 候補B (root probing) 実装

**実施内容**
- SABORI_PROBE_ROOT (opt-in) で failed-literal 検出を実装、全テスト+golden 緑
- 副作用分離を3段階で達成: activity/rng/var_order の snapshot-restore で
  fixed=0 なら off と bit 同一 (zephyrus prefix 一致で実証)
- A/B: G4 で「解なし→解あり」×2 (prize-collecting/collaborative)、負けセル 0

**設計判断・理由**
- var_order_ 恒久並べ替えリーク (backtrack は end のみ復元) の発見が核心。
  probe 系機能すべてに共通の地雷 → VariableSelector::restore_order を新設
- opt-in 温存。既定 ON は広域ゲート後に判断

**気づいた課題・TODO**
- bottomup probe も var_order_ 並べ替えの影響を受けている可能性
  (あちらは「軌道が変わって良い」設計なので実害はないが、isolate arm の
  意味論が不完全だった可能性あり)
- 既定 ON 昇格ゲート / feature/mp での probe 予算多様性ワーカー

**関連ファイル・コミット**
- src/core/solver_search.cpp (run_root_probing), variable_selector.hpp

## 2026-07-09 root probing の共通剪定回収 + defined 死角の実証

**実施内容**
- probing の unit fix は「100% defined 側」と実測 (verbose に内訳追加)
  → 厳格優先で深さ0テストを受けない defined 層 = 浮上機構の死角が
  probing の取り分という機構を確定 (ユーザ仮説の検証)
- 両側生存 probe の trail 交差で共通 bound を root 回収する拡張を実装
- A/B: amaze12 NONE→SAT / gfd 大幅改善 / 負けセル0、net +4/+3

**設計判断・理由**
- 「probe のコストは払済み、ついでに回収」(ユーザ方針)。bounds 交差は
  無条件健全で DB も要らない最安の回収形
- 含意収穫 (長さ2 ng) はフィルタ設計が本体なので見送り継続

**関連ファイル・コミット**
- src/core/solver_search.cpp, include/sabori_csp/model.hpp (for_each_trailed_var)
