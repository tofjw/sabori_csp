## 2026-03-27 02:00〜08:11 作業開始〜終了

**実施内容**
- ArrayVarIntElementConstraint の activity bump 機能を追加
  - index が instantiate したとき、選ばれた配列要素変数と result 変数が未確定なら activity を上げる
  - Model に `enqueue_activity_bump` / `pending_activity_bumps` キューを追加
  - Solver::process_queue 末尾で activity bump リクエストを消費
- prize-collecting (mznc2016) で CP-SAT の解を sabori で検証 → FZN 制約レベルでは正しい解として認められることを確認
- mznc2021 connect の CHECK_FAIL 調査
  - Python FZN 外部検証スクリプトを作成し、全 2074 FZN 制約が満たされていることを確認
  - `-O0` (2291制約) vs `-O1` (2074制約) の比較で 217 制約が最適化で消失していることを発見
  - 根本原因: MiniZinc 2.9.5 の `-O1` 最適化バグ（エイリアス置換時の制約消失）
  - Gecode/Chuffed/CP-SAT は同じバグのある FZN を受け取るが、解を見つけられないためバグが顕在化しない
- bench_compare.py の修正
  - `judge_winner` に `s_check` パラメータを追加、CHECK_FAIL の解を UNKNOWN 扱いにして不正な解でスコアを稼がないようにした
  - MiniZinc 2.9.5 のバグについてコメントを記載
- Solver::verify_solution を強化（`on_final_instantiate` も全制約に対して呼ぶ）

**設計判断・理由**
- activity bump は Model 経由のキュー方式にした。propagation callback 内では Solver の activity 配列に直接アクセスできないため
- `-O0` を Sabori だけに適用する案は性能比較が不公平になるため却下。代わりに CHECK_FAIL 解をスコアから除外する方式を採用
- MiniZinc コンパイラバグの修正は sabori 側では不可能。ワークアラウンドとして checker の結果を勝敗判定に反映

**気づいた課題・TODO**
- MiniZinc 2.9.5 の -O1 最適化バグを MiniZinc プロジェクトに報告すべき
- ArrayVarIntElementConstraint に `is_satisfied` を実装すべき（現在 nullopt を返すため verify_solution でスキップされていた）
- connect 問題は全ソルバーが 60 秒以内に解を見つけられない難問

**関連ファイル・コミット**
- `src/core/constraints/global/array_var_int_element.cpp` — activity bump 追加
- `include/sabori_csp/model.hpp` — enqueue_activity_bump API
- `src/core/model.cpp` — clear_pending_updates で activity bump もクリア
- `src/core/solver.cpp` — process_queue で activity bump 処理、verify_solution 強化
- `benchmarks/minizinc_challenge/bench_compare.py` — CHECK_FAIL 解の無効化
