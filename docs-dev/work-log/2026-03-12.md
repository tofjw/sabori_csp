# 2026-03-12 作業ログ

## 2026-03-12 activity bump のコミュニティ連動・乱数実験（すべて revert）

**実施内容**
- NoGoodManager::learn_from_conflict の activity bump に一様乱数を乗算する実験
  - [0, 1.0]、[0.1, 0.5]、[0.01, 0.05] の範囲を試行 → 効果なし → revert
- propagate_eq_watches / propagate_bound_nogoods の activity bump にも乱数を適用 → 効果なし → revert
- コミュニティサイズ降順で activity 初期値を設定する実験（最大: 0.1、以降 0.5倍減衰）→ 若干改善だが劇的ではない → revert
- コミュニティ別 activity ボーナスの実験
  - リスタート毎に上位コミュニティから1つ選んでボーナスを設定、変数選択時に activity + bonus * activity_inc を使用
  - ボーナス値 1.0 / 0.1、上位5 / 上位3 を試行 → 効果なし → revert
- learn_from_conflict で fail 変数と同じコミュニティの変数は係数 0.1、異なるコミュニティは係数 0.01 / 0.0 / 0.01*乱数 で bump する実験 → 効果なし → revert

**設計判断・理由**
- activity の微調整（乱数・初期値・コミュニティボーナス・コミュニティ連動 bump）で探索多様性・集中度の改善を狙った
- コンフリクト原因変数と同じコミュニティの変数を優先 bump する方向も試行

**気づいた課題・TODO**
- activity の係数調整全般を複数パターン試行したが、いずれも劇的な改善には至らなかった
- activity 以外のアプローチ（変数選択戦略、値選択、伝播強化など）を検討すべき

**関連ファイル・コミット**
- 変更なし（全て revert 済み）

## 2026-03-12 06:33 LNS ヒント破壊の改良実験（revert）

**実施内容**
- LNS ヒント破壊で非改善解発見時に `restarts_since_improvement_ /= 2` するカウンタ半減を実装
- 破壊率を固定値ではなく `[0, destroy_rate)` のランダム値に変更
- 破壊数の基準を全ヒント変数数から決定変数数 (`decision_var_end()`) に変更
- ベンチマーク結果で大きな改善が見られなかったため、全変更をリバート

**設計判断・理由**
- カウンタ半減: 谷を渡る探索（一時的悪化→改善）を可能にする狙い
- ランダム破壊率: 毎回同じ量を壊すより探索の多様性が増す
- 決定変数ベース: 全変数に対して決定変数は少なく、全変数ベースだと破壊数が過大になる

**気づいた課題・TODO**
- LNS ヒント破壊の現行パラメータ（`destroy_rate` の上限・増加速度）は現状維持
- 別アプローチ（破壊対象の選択戦略、activity ベースの破壊など）の検討余地あり

**関連ファイル・コミット**
- `src/core/solver.cpp` — 変更後リバート済み、コミットなし

## 2026-03-12 ブリッジ変数ヒューリスティクス実験（revert）

**実施内容**
- コミュニティ間ブリッジ変数（複数コミュニティにまたがる制約に参加する変数）の特定と探索ヒューリスティクスへの組み込み
  - `CommunityAnalysis` にブリッジスコア（他コミュニティへのエッジ重み合計）を計算・保持
  - `init_search()` で activity 初期値にブリッジスコアを加算
  - `select_linear()` のタイブレイクにブリッジスコアを追加（domain_size/activity と ng_bloom_overlap の間）
- ビルド・全160テスト pass だが、実問題で効果なし → revert

**設計判断・理由**
- ブリッジ変数を早期に判定させることでコミュニティ間整合性の早期確認を狙った
- activity 初期ブースト + タイブレイク優先の2段構え

**気づいた課題・TODO**
- activity 初期値調整系の実験（コミュニティサイズ順、ブリッジスコア等）は全般的に効果薄
- コミュニティ構造の活用は変数選択の微調整より、探索の分解やバックトラック戦略に使うべきかもしれない

**関連ファイル・コミット**
- 変更なし（全て revert 済み）

## 2026-03-12 all_different GAC 保存・forward checking 削除・verbose 統計追加

**実施内容**
- all_different GAC (Régin's algorithm) の実装を `AllDifferentGACConstraint` として別クラスに分離・保存
  - `include/sabori_csp/constraints/all_different_gac.hpp` (新規)
  - `src/core/constraints/global/all_different_gac.cpp` (新規)
  - `src/core/CMakeLists.txt` にビルド追加
  - GAC テスト4件を `AllDifferentGACConstraint` を使うよう更新
- `AllDifferentConstraint::on_instantiate` から確定値の他変数ドメイン除去処理（forward checking ループ）を削除
- `-v` (verbose) モードで all_different 制約の統計情報を表示する機能を追加
  - 全変数数、未確定変数数、残り値数(プールサイズ)、比率(unfixed/vals)
- ratio の計算を `全変数数/vals` → `未確定変数数/vals` に修正（presolve 確定済み変数の影響を正しく反映）

**設計判断・理由**
- GAC 実装はベンチマークで改善なしだったが、将来の活用のため別クラスとして保存
- `AllDifferentConstraint` の `pool_values_` 等を `protected` に変更し GAC サブクラスからアクセス可能に
- forward checking 削除: プール管理 + hall pair 検出のみに簡素化
- verbose 統計: `Solver::init_search` の presolve 完了後に表示（既存 verbose ログと同じ場所）
- `unfixed_count()` アクセサを追加し、正しい比較（未確定変数 vs 残り値数）を表示

**気づいた課題・TODO**
- GAC は Sudoku 等の sparse set ドメイン問題で消費済み値の扱いに注意が必要（`is_val_in_pool` で対応済み）
- GAC 有効化の閾値（unfixed >= 4 かつ全 sparse set）は今後調整の余地あり

**関連ファイル・コミット**
- `include/sabori_csp/constraints/all_different_gac.hpp` — GAC クラス宣言（新規）
- `src/core/constraints/global/all_different_gac.cpp` — GAC 実装（新規）
- `src/core/constraints/global/all_different.cpp` — forward checking 削除
- `src/core/solver.cpp` — verbose 統計追加
- `include/sabori_csp/constraints/global.hpp` — `pool_*` を protected 化、`unfixed_count()` 追加
- `tests/cpp/test_global_constraints.cpp` — GAC テスト更新
- コミット: 0776001 (hall set 導入)
