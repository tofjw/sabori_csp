# 2026-07-04 作業ログ

## 2026-07-04 00:00〜03:11 記事「探索アルゴリズム解説」の全面推敲（日英・長短4本）

**実施内容**
- `articles/search-algorithm-explained.md`（日本語全文）を stop-ai-slop-jp 基準でレビュー→修正
  - 全角ダッシュ `——` 80→0、太字 380→約200スパン、`本記事`21→0・`位置づけ`14→0・比喩語`土台`→`基盤`統一
  - 翻訳調/false agency（「数字が物語る」等）、命題型見出しの `——` 副題を句点・括弧化
- 反復の削除：全体像/LCG対比/「基盤 vs 精緻化」の横断再説を各章から除去し、まとめ1回に集約（§3・§8）
- メタ自評の削除：「この記事の価値は」「ささやかな追試」「気に入っていた」「一番価値のある情報」等
- 方法論の §0 集約：散在していた「数字の等級」「差分/孤立ボックス」「直接対決/フィールド注」を §0 の新設サブセクション「数字の読み方（計測の約束）」に統合、各所を §0 参照に置換
- 整合性修正：**§0 の「5シード」全称が mix_p・対CP-SAT の1シードと矛盾**していたのを「標準38問×5シード、一部1シード」に訂正
- TL;DR を6段落+リスト → 3段落に圧縮
- フィラー半減：「整合します」5→3・「正確に(は)」6→3、「読み取りは」4回を各別表現に
- 末尾の冗長段落（立ち位置の再表明）を削除、SAT追試の一文で本文終了
- 短い版 `search-algorithm-short.md` を同基準で整形（`——`15→0、`土台`→`基盤`、メタ自評削除）。inner-outer 構造の初出未説明を修正、細部（temporalマスク・全シード悪化・シード反転）を削減、結びを長い版の「先行研究把握済み」に統一
- 英語版2本（`-en.md`／`-en-short.md`）を**日本語を基準に**齟齬修正。TL;DR圧縮・§0方法論集約・メタ自評削除・末尾段落削除・各種トリムを反映
  - **数値誤り修正**：EN long の `ng_on vs ng_off` が +17（誤）→ +18（54−36=18、JP一致）

**設計判断・理由**
- 日本語を正、英語を従とした（ユーザ指示）。全主要 net（temporal+25 / bloom−4 / ng+18 / structural−4 / grad−13 / onehot+6）と主証拠+81・+21 の一致を機械照合で確認
- 「価値」は機構の効き目を述べる技術的主張（adaptiveのrobustness等）は残し、記事・アイデアへの自己評価だけ削除
- 術語「冗長な代替」は §4・まとめから参照されるため保持（言い換え提案を退け、断定「典型」だけ弱めた）
- 方法論の主証拠バレット（+81/+21）は「結論の合成」なので §0（結果の前）でなくまとめに残置

**気づいた課題・TODO**
- 各章の計測条件再掲（「38問×5シード×30秒」）は §0 と重複するが、per-measurement spec として温存（不整合ではない）。さらに削るかは要判断
- 英語版は元々一部段落が圧縮訳で、日本語にある「思い当たる原因（干渉）」段落等が欠落。今回は自分の編集分の反映を優先し、既存の圧縮訳ギャップは未拡張

**関連ファイル**
- `articles/search-algorithm-explained.md` / `search-algorithm-short.md` / `search-algorithm-explained-en.md` / `search-algorithm-en-short.md`
- バックアップ: scratchpad `search-algorithm-explained.bak.md`（日本語全文の推敲前）

## 2026-07-04 07:00〜13:00 LNS（大近傍探索）の実装と多角検証 → どの役割でも不採用

**実施内容**
- LNS を optimize ループに健全実装（既定OFF, `SABORI_LNS`）。`run_improvement_probe` を雛形に `run_lns_iteration`/`select_lns_fix_set`。incumbent 値へ可逆固定→予算付きサブ探索→`backtrack(root_point)` で巻き戻し。ノブ: `FRAC/BUDGET/FREQ/STALL/DESTROY(uniform/activity/community/related)/SCHED/ISOLATE/QUIET`。250 最適化 fzn で verdict+obj 一致、`SABORI_LNS` 未設定=`=0` byte 一致、260 C++テスト+golden 242 緑。
- チューニング（2023-2024 × 5seed, 30s, `bench_lns.py`）: 強制既定にすると **net -35〜-53（全シード一致で負）**。`iso≈noiso(-1)` で損失は機会費用（近傍探索が多様化リスタートより非生産的）と判明。`related < uniform(-22)`。
- 2025 対CP-SAT（`bench_compare.py`, Gecode検証）: base **9/6/4** → LNS-on は最良（高stall / quiet）でも **7/7/5**（stripboard / work-task-variation / mondoku が回帰）。既定OFFで base を byte 保存することを確認。
- quiet(side-channel)を2版試作しどちらも失敗:
  - v1（共有 best を締めるが redirect せず）: 締まった bound に stale hint で本探索が空回り→悪化。
  - v2（真の side-channel: `lns_best_` を本探索と分離 + `report_if_better` 単調出力 + rng/activity/temporal/best_assignment/**stats_** 隔離）: hijack 消滅・決定的化したが **time theft**（大近傍85%固定の伝播コスト）で広く回帰。**2025 対CP-SAT = 6/8/6 で最悪**。両版不採用、既定 `lns_quiet_=false`。
- ポートフォリオ VBS 実験（`vbs_2025.py`, 5アーム = base + seed2/seed3 + luby:100/constant:100）: base 9 → **VBS 11（+2, 回帰なし）**。seed3 単独=11, policy 各10。flip は ihtc-2024-marte(luby100) / proteindesign12(seed3)。
- restart/`outer` を記事 §5 と照合: 固定 Luby は不採用が正しい（**adaptive vs luby:100 = +14**, adaptive 勝ち）。`outer` の適応信号は情報ゼロ（scrambled と同点、+6/+7）。私の 2025 単年 luby 優位はノイズだった（記事の「loses on one seed」帯）。
- **LNS 固有カバレッジ確認**（`coverage_lns.py`, 2023-2025, 58 最適化問題, 強 seed ポートフォリオ base+seed2+seed3+no_nogood+mrv に積極LNSアーム stall1/freq1/frac0.3 を追加）: **LNS が全アーム中で厳密最良 = 0件（毎年0）, 対CP-SAT 勝ち上乗せ +0**。

**設計判断・理由**
- LNS の健全性は構造で担保: 近傍固定下の UNSAT は大域最適/UNSAT でない。`ReturnOptimal` は改善解受理後の root-only 伝播矛盾のみ（[[lcg-soundness-incident]] / [[allsol-incomplete-unsound-learning]] の轍を踏まない）。
- ポートフォリオでは best-of で LNS の負けは無害 → 唯一の論点は「他アームが到達しない解を LNS が固有に出すか」。答えは 0件。LNS の対base勝ちは**軌道多様化にすぎず新規到達能力ではない**。seed 変種が 30s で同じ盆地に time theft なしで安く到達し LNS を subsume。feature/mp の設計「seed-first・軸は弱い」と整合（LNS は特に高コストな弱軸）。
- 改善の本命は **seed 多様 portfolio**（VBS +2, 回帰なし）。残余は per-problem oracle gap(-37) = オンライン適応の問題で、固定 schedule でも `outer` 再設計でも埋まらない。

**気づいた課題・TODO**
- LNS は feature/mp に混ぜない。スレッド枠は seed 追加に使うのが実証的に最善。
- LNS コード（feature/lns 未コミット）は既定OFFで温存 or 撤去を要判断。
- v2 の副産物知見: `stats_` を隔離しないと adaptive restart cycle（max_depth/prune_delta）が分岐して本探索が非決定化（隔離必須）。`report_if_better` は `-a` 単調出力の保証に有用（非LNSでも保持価値あり）。
- 単一スレッド既定の固定schedule化は不要（記事 §5 で adaptive 優位が確定済み）。

**関連ファイル・コミット**
- 実装（feature/lns, 未コミット）: `include/sabori_csp/solver.hpp`, `src/core/solver.cpp`, `src/core/solver_search.cpp`, `include/sabori_csp/community_analysis.hpp`
- ベンチ（新規）: `benchmarks/minizinc_challenge/bench_lns.py`, `vbs_2025.py`, `coverage_lns.py`
- メモリ: `lns-portfolio-arm-net-negative.md`（結論を集約）
