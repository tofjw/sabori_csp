## 2026-03-26 14:00〜16:39

**実施内容**
- Python 高レベル API ラッパを新規実装（CP-SAT 風の命名規則）
  - `_expressions.py`: `IntVar`, `LinearExpr`, `BoundedExpr`, 非線形式ノード（`TimesExpr`, `AbsExpr`, `DivExpr`, `ModExpr`）、演算子オーバーロード（`+`, `-`, `*`, `//`, `%`, `abs`, `==`, `!=`, `<=`, `<`, `>=`, `>`）、`sum()` 対応
  - `_model.py`: `CpModel`（`int_var`, `bool_var`, `int_var_from_domain`, `constant`, `add`, `minimize`/`maximize`）、制約のローレベル変換（バイナリ → `IntEqConstraint` 等、一般線形 → `IntLinEqConstraint` 等、非線形 → aux 変数自動生成）
  - `_solver.py`: `CpSolver`, `SolveStatus` enum (`FEASIBLE`/`OPTIMAL`/`INFEASIBLE`)
  - `_globals.py`: 14 種のグローバル制約ヘルパ関数（`all_different`, `circuit`, `cumulative`, `element`, `table`, `maximum`, `minimum`, `count`, `nvalue` 等）
  - `__init__.py` 更新: 高レベル API を再エクスポート
- テスト 71 件作成（全パス）
  - `test_expressions.py`: 式構築、演算子、正規化（29 件）
  - `test_model.py`: 変数生成、制約変換、N-Queens、最適化（28 件）
  - `test_globals.py`: 各グローバル制約（14 件）
- セルフレビューで 3 件のバグ修正
  - `_NonLinearExpr.__ge__` が `__le__` と同一結果を返す → `ComparisonOp.GE` 追加 + `_post_bounded_expr` で正規化
  - `_NonLinearExpr` に `__lt__`/`__gt__` 未実装 → 追加
  - `element` の配列型判定が先頭要素のみ → `all()` に変更
- `docs/python/` ドキュメント再構成
  - `high-level/`: quickstart.md, reference.md, operators.md（新規）
  - `core/`: quickstart.md, reference.md（既存を移動）
  - `README.md`: 2 層構成の目次に更新

**設計判断・理由**
- CP-SAT 風命名（`CpModel`/`CpSolver`/`int_var`）: ユーザが OR-Tools 経験者に馴染みやすい
- Eager posting（`model.add()` 時に即座に C++ 制約生成）: 遅延バッチ方式より実装がシンプルで、エラーが呼び出し元で即座に表面化する
- `LinearExpr` は `{IntVar: coeff}` dict + constant: 項の合併が O(n) で自然、`sum()` もそのまま動作
- Reified 制約は初回スコープ外: 基本的な制約と演算子の安定を優先
- `all_different` に `LinearExpr` を渡せるようにした（`_realize_list` 経由で aux 変数自動生成）: N-Queens の対角制約 `queens[i] + i` を自然に書けるようにするため

**気づいた課題・TODO**
- `_realize()` が `_NonLinearExpr`/`_AggregateExpr` を扱えない → `minimize(abs(x))` が動かない（エッジケース）
- Reified 制約の高レベル API サポート（`only_enforce_if(b)` 等）は未実装

**関連ファイル**
- `python/sabori_csp/_expressions.py`, `_model.py`, `_solver.py`, `_globals.py`, `__init__.py`（新規/変更）
- `tests/python/test_expressions.py`, `test_model.py`, `test_globals.py`（新規）
- `docs/python/README.md`, `docs/python/high-level/`, `docs/python/core/`（新規/変更）

---

## 2026-03-26 16:40〜24:26

**実施内容**
- `docs/python/examples/` に高レベル API サンプル 5 本追加（既存 core API 版と対比）
  - `06_basic_highlevel.py`: 基本操作、全解探索、最適化
  - `07_nqueens_highlevel.py`: N-Queens（対角制約が 10 行→2 行に）
  - `08_scheduling_highlevel.py`: ジョブスケジューリング（makespan 制約が算術式に）
  - `09_sudoku_highlevel.py`: 数独
  - `10_magic_square_highlevel.py`: 魔方陣（`sum(row) == magic_sum` で記述可能に）
- `docs/python/README.md` を更新し、core / high-level の対比テーブルを追加
- `InverseConstraint` の offset パラメータ化
  - C++ コンストラクタに `int64_t offset = 1` パラメータ追加（既存コードは `offset_` メンバを既に使っていたため変更は最小限）
  - Python binding に `offset` パラメータ追加（デフォルト `1`、FlatZinc 互換）
  - 高レベル API の `inverse()` はデフォルト `offset=0`（0始まり）に設定
  - テスト: 0始まり / 1始まり 両方のテストを追加、C++ テスト・FlatZinc テストも通過確認
- 他の制約の添字オフセット調査
  - `element` 系: `zero_based` パラメータ既存、高レベル API で `True` 渡し済み → 対応済み
  - `circuit`: ドメイン最小値から自動検出 → 対応済み
  - `regular`: DFA 慣例で 1始まり固定 → 変更不要
  - `connected`: FlatZinc 内部のみ → Python 未公開、変更不要

**設計判断・理由**
- `inverse` の高レベル API デフォルトを 0始まりにした理由: Python/C++ の配列添字と一致させ、ユーザが +1/-1 変換を不要にするため
- C++ / core binding 側のデフォルトは 1 のまま: FlatZinc 互換性を維持、既存コードを壊さない

**気づいた課題・TODO**
- なし

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`, `src/core/constraints/global/inverse.cpp`（offset パラメータ追加）
- `python/_bindings.cpp`（offset パラメータ追加）
- `python/sabori_csp/_globals.py`, `_model.py`（高レベル API 側対応）
- `tests/python/test_globals.py`（inverse テスト 0-based / 1-based）
- `docs/python/examples/06〜10_*_highlevel.py`（新規サンプル 5 本）

---

## 2026-03-26 24:26〜24:31

**実施内容**
- `InverseConstraint` のデフォルト offset を全レイヤーで 0 に統一
  - C++ ヘッダ: デフォルト引数 `offset = 1` → `offset = 0`
  - Python core binding: デフォルト `offset = 1` → `offset = 0`
  - FlatZinc ファクトリ (`constraint_registry.cpp`): 明示的に `1` を渡すよう変更（FlatZinc 仕様は 1始まり固定）
  - C++ テスト: 全テストケースを 0-based ドメイン (0..n-1) に書き換え
  - Python 高レベル API: 変更なし（既に `offset=0` がデフォルト）

**設計判断・理由**
- まだ外部ユーザがいないため、破壊的変更のコストはゼロ
- C++/Python/高レベル API 全てが 0始まりで一貫することで、レイヤー間で offset を意識する必要がなくなった
- FlatZinc のみが例外だが、これは仕様上 1始まり固定なので明示的に渡す方が正しい

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`（デフォルト引数変更）
- `python/_bindings.cpp`（デフォルト引数変更）
- `src/fzn/constraint_registry.cpp`（明示的に offset=1）
- `tests/cpp/test_global_constraints.cpp`（0-based に書き換え）
