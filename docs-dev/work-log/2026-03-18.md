## 2026-03-18 13:12 inverse 制約の実装

**実施内容**
- FlatZinc `fzn_inverse` 制約をネイティブ実装
- `f[i] = j <-> invf[j] = i` の双方向伝播を実装
- presolve: ドメイン制限 `[1, n]`、固定点ループで代入伝播・ドメインサポートフィルタリング
- on_instantiate: 双方向代入 + 両配列の all_different 的値除去
- on_remove_value: 双方向値除去
- on_set_min/on_set_max: 境界変化を値除去として伝播
- 内部状態なし（全伝播は enqueue 経由）→ rewind_to は空実装
- MiniZinc redefinition、FlatZinc テスト、C++ テスト、ドキュメント更新

**設計判断・理由**
- DiffnConstraint と同じパターン（デフォルトコンストラクタ + var_ids_ 直接代入）を採用。ラムダ式でのvar_ids初期化は回避
- on_instantiate で all_different 的伝播も行うことで、暗黙の all_different 制約を別途追加する必要をなくした
- offset_ = 1 固定（FlatZinc inverse は常に 1-indexed）

**気づいた課題・TODO**
- solver の `search_with_restart` + `solve_all` で、presolve が単一解に完全解決するケースで無限ループが発生する既知の問題を発見。C++ テストでは `solver.solve()` に変更して回避した。FlatZinc ソルバー（fzn_sabori）では発生しない
- `is_satisfied` のオーバーライドは未実装（デフォルトの nullopt を使用）。verify_solution では問題なし

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp` — クラス宣言
- `src/core/constraints/global/inverse.cpp` — 実装（新規）
- `src/fzn/constraint_registry.cpp` — FlatZinc 登録
- `share/minizinc/sabori_csp/fzn_inverse.mzn` — MiniZinc redefinition（新規）
- `tests/fzn/constraints/inverse/` — FlatZinc テスト（新規）
- `tests/cpp/test_global_constraints.cpp` — C++ テスト追加
- 全 182 テスト合格

## 2026-03-18 17:11 solve_all 無限ループ修正 + inverse ベンチマークスクリプト作成

**実施内容**
- `search_with_restart` で `find_all=true` かつ presolve が全変数を確定するケースの無限ループを修正
- `backtrack(model, root_point)` 直後に `var_selector_.all_assigned()` チェックを追加（2箇所）
- C++ テストに `solve_all` 版の InverseConstraint テストを追加（全 182 テスト合格）
- `bench_inverse.py` を bench_alldiff.py ベースで作成（inverse 制約を使う問題をフィルタ）

**設計判断・理由**
- チェック位置は backtrack 直後・apply_unit_nogoods 前。backtrack が no-op（探索判定なし）だったことだけを検出し、unit nogood 伝播で新しい解を見つける正常パスは阻害しない
- 当初 apply_unit_nogoods 後にチェックしたが、unit nogood が新たな変数を確定させるケース（正常パス）を殺してしまうため位置を変更

**気づいた課題・TODO**
- inverse を使う問題は4問のみ（2023: elitserien, travelling-thief、2024: cable-tree-wiring、2025: black-hole）。ベンチマーク結果は未実行

**関連ファイル**
- `src/core/solver.cpp` — while ループ内に進捗チェック追加（2箇所）
- `tests/cpp/test_global_constraints.cpp` — solve_all テスト追加
- `benchmarks/minizinc_challenge/bench_inverse.py` — 新規作成
