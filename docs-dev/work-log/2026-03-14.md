## 2026-03-14 00:00〜00:30 all_different value removal ガード条件の実験

**実施内容**
- all_different の `on_instantiate` で確定した値を他の未確定変数のドメインから削除する処理に、タイト条件ガード (`unfixed_count / pool_n > 閾値`) を導入して効果を検証
  - 0.95, 0.9, 0.8, 0.5 の各閾値を試行
- ガード有り (0.95) vs ガード無し (常に remove) で bench_alldiff.py ベンチマーク比較を実施
- 結果: ガード無しの方が最適化問題の obj 改善が大きい（特に cgt: 33223→8265）
  - ただし train-scheduling で解が見つからなくなる退行あり
- **ガード無し（常に remove）を採用**

**設計判断・理由**
- 値削除の伝播コストより、ドメイン縮小による探索空間削減の効果が大きい
- cgt での大幅改善が train-scheduling の退行を上回ると判断
- ガード条件を入れると閾値チューニングが必要になり、問題依存で最適値が変わるため、シンプルに無条件適用が妥当

**気づいた課題・TODO**
- train-scheduling でガード無しが退行する原因の調査（値削除が伝播チェーンを増やしすぎている可能性）
- stripboard が 0.30s→0.47s にやや遅くなっている点

**関連ファイル・コミット**
- `src/core/constraints/global/all_different.cpp` — ガード条件を削除、常に値削除を実行
- コミットなし

## 2026-03-14 02:36 int_lin_le / int_lin_le_reif bounds propagation 追加

**実施内容**
- `int_lin_le` に bounds propagation を追加（矛盾検出のみ → 各変数の上下限絞り込み）
  - `propagate_bounds()` メソッド追加、`on_instantiate` / `on_set_min` / `on_set_max` から呼び出し
  - `presolve` に固定点ループによる bounds tightening 追加
- `int_lin_le_reif` に同様の bounds propagation を追加
  - `propagate_bounds_le()` (b=1: sum <= bound) / `propagate_bounds_gt()` (b=0: sum > bound) の2メソッド
  - b 確定時のみ propagation 実行、b 未確定時は b 推論のみ（従来通り）
  - `presolve` にも b 確定時の bounds tightening 追加
- `int_le_reif` の defined_var 設定を修正: x か y が定数（is_assigned）の場合、b を defined_var にしない
- `all_different.cpp` の変更を revert

**設計判断・理由**
- `int_lin_le`: `max_rem_potential_` は不要（片側不等式なので `min_rem_potential_` だけで十分）。trail サイズ変更なし
- `int_lin_le_reif`: b=1 は int_lin_le と同じロジック、b=0 は逆向き（`max_rem_potential_` 使用）、b 未確定時は propagation 不可
- `int_le_reif` の b を決定変数にする理由: 片方が定数なら b を先に決めることで強力な枝刈りが発生。両方変数なら b を決めても枝が減らないので defined_var のまま

**気づいた課題・TODO**
- `int_eq_reif` / `int_ne_reif` にも同様の定数チェック（is_assigned ガード）を検討する余地あり

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp` — propagate_bounds 宣言追加
- `src/core/constraints/global/int_lin_le.cpp` — bounds propagation 実装
- `src/core/constraints/global/int_lin_le_reif.cpp` — bounds propagation (le/gt) 実装
- `src/fzn/constraint_registry.cpp` — int_le_reif の defined_var 条件修正

## 2026-03-14 21:19 int_lin_eq propagation 強化 / bump_activity 分母修正 / コミュニティタイブレーク実験

**実施内容**
- `int_lin_eq::on_instantiate` に bounds propagation を追加（残り2+変数の場合、`propagate_lower_bounds` と `propagate_upper_bounds` の両方を呼ぶように修正）
- `Constraint::bump_activity` の分母から、最初から instantiated な変数を除外（`search_var_count_` を `prepare_propagation` 時に計算）
- 変数選択のコミュニティタイブレーク（同コミュニティ優先）を追加後、ベンチマークで有意差なしのためrevert

**設計判断・理由**
- `int_lin_eq::on_instantiate`: solver は instantiate 時に `on_set_min`/`on_set_max` を dispatch しないため、`on_instantiate` 内で両方向の propagation が必要だった。これまで残り2+変数の場合は feasibility check すらなく、bounds が絞られていなかった
- `bump_activity` の分母: presolve で固定された変数を含めると `activity_inc / n` が不当に小さくなる。`Model::prepare_propagation()` 内で各制約の `compute_search_var_count()` を呼び、探索開始時の未確定変数数を記録
- コミュニティタイブレーク: NoGood Bloom 後・reservoir sampling 前に同コミュニティ優先を挿入したが、ベンチマークで有意差なしのため削除

**気づいた課題・TODO**
- `int_lin_le_imp`: b=1 時の bounds propagation なし（feasibility check のみ）。`int_lin_le` と同等の propagation を追加すべき
- `int_lin_le_imp`: `min_sum > bound` 時に `b=0` を推論できるが未実装（対偶推論）

**関連ファイル**
- `src/core/constraints/global/int_lin_eq.cpp` — on_instantiate に propagation 追加
- `src/core/constraint.cpp` — `bump_activity` 分母修正、`compute_search_var_count` 追加
- `src/core/model.cpp` — `prepare_propagation` 内で `compute_search_var_count` 呼び出し
- `include/sabori_csp/constraint.hpp` — `search_var_count_` フィールド、`compute_search_var_count` メソッド追加
- `include/sabori_csp/variable_selector.hpp`, `src/core/variable_selector.cpp`, `src/core/solver.cpp` — コミュニティタイブレーク追加→revert

## 2026-03-14 23:32 reif 制約の b 変数の値選択ヒューリスティック実験

**実施内容**
- reif 制約の `b` 変数に対する値選択ヒューリスティックを3パターン実験し、いずれもベンチマークで有意差なしのため revert
  1. `int_eq_reif` で x/y が定数の場合、b を決定変数化 + `preferred_value=1` → 効果なし
  2. 全6 reif 制約の b に `preferred_value=1`（true 優先）→ 効果なし
  3. 全6 reif 制約の b に `randomize_value_order`（ランダム試行）→ 効果なし
- 上記実験のインフラとして `VarData` / `Model` / `Solver::order_values` に以下を追加（残存）:
  - `VarData::preferred_value` — 優先試行値
  - `VarData::randomize_value_order` — 値順ランダム化フラグ
  - `Model::set_preferred_value()` / `preferred_value()` / `unset_defined_var()` / `set_randomize_value_order()`
  - `Solver::order_values` に `preferred_value` と `randomize_value_order` の処理分岐

**設計判断・理由**
- reif の b は domain={0,1} で小さいため、値順の変更が探索全体に与える影響が限定的
- activity ベースの変数選択 + リスタートの方が支配的で、b の初期 polarity は早期に上書きされる
- インフラは将来の他用途（最適化変数の方向バイアス等）に流用可能なため残存

**気づいた課題・TODO**
- reif の b の値選択よりも、reif 制約自体の propagation 強化の方が有望

**関連ファイル**
- `include/sabori_csp/model.hpp` — VarData に preferred_value, randomize_value_order 追加
- `include/sabori_csp/var_data.hpp` — 同上
- `include/sabori_csp/solver.hpp` — order_values シグネチャ変更
- `src/core/solver.cpp` — order_values に preferred_value / randomize 処理追加
