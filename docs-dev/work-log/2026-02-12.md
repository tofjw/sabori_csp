## 2026-02-12 var_ptr_to_idx_ 廃止作業

**実施内容**
- 11クラスから `std::unordered_map<Variable*, size_t> var_ptr_to_idx_` メンバを削除
- 非reif制約（7ファイル）: コールバック内の `model.variable(var_idx).get()` + `var_ptr_to_idx_.find()` パターンを `find_internal_idx(var_idx)` に置換
  - `int_lin_eq`, `int_lin_ne`, `int_lin_le`, `table`, `all_different`, `circuit`, `array_var_int_element`
- reif/imp制約（4ファイル）: `size_t b_id_` メンバを追加し、b_ の判定を `var_idx == b_id_` に、線形変数は `find_internal_idx(var_idx)` に置換
  - `int_lin_eq_reif`, `int_lin_ne_reif`, `int_lin_le_reif`, `int_lin_le_imp`
- ビルド成功、全126テスト通過

**設計判断・理由**
- `model.variable(var_idx)` の shared_ptr コピーと `unordered_map::find()` のハッシュ検索を排除
- `find_internal_idx()` は `var_ids_` の線形探索（2-4要素）で、キャッシュフレンドリーかつ低コスト
- reif/imp の b_ 判定は `size_t b_id_` の整数比較で O(1)
- スコープ外: `IntElementConstraint`, `ArrayIntMaximumConstraint`, `ArrayIntMinimumConstraint`（別パターン）、`logical.cpp` の制約（既に `var_id_to_idx_` 使用）

**気づいた課題・TODO**
- スコープ外の3クラス（IntElement, ArrayIntMaximum, ArrayIntMinimum）も同様に移行可能
- `logical.cpp` の `var_id_to_idx_` (size_t→size_t) も `find_internal_idx` に統一可能

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp`
- `src/core/constraints/global/` 配下11ファイル

## 2026-02-12 IHTC-2024 クラッシュ修正完了 + 回帰テスト追加

**実施内容**
- IHTC-2024 クラッシュ調査の完了
  - 前セッションで `Model::set_min`/`set_max` lazy パスの `index_of` → `SIZE_MAX` バグを修正済み
  - `Domain::swap_at` に追加していた一時デバッグガード（bounds check + abort）を削除
  - Release ビルドをクリーンリビルドし、ガードなしでもクラッシュしないことを確認
  - ASAN ビルドでもエラーなしを確認
  - 前回 Release ビルドがガードなしでクラッシュしていたのはビルドが stale だったため
- 回帰テスト追加 (`tests/cpp/test_domain_model.cpp`)
  - `Model set_min lazy path with hole in domain` — 穴あきドメイン `{1,3,5,7,9}` で lazy bounds が穴の値に収束するケース（6セクション）
  - `Model set_min/set_max with remove then lazy convergence` — `remove_value` で穴を作った後の bounds 収束（3セクション）
  - 全128テスト通過（126→128）

**設計判断・理由**
- テストは lazy bounds の特性を考慮して設計。`is_instantiated()` は `mins_ == maxs_` で判定するため、lazy path で bounds が穴をまたいでいる場合は singleton を検出しない — これは正常動作
- テストの主目的は「穴の値に収束してもクラッシュしない」ことの保証

**気づいた課題・TODO**
- `set_min`/`set_max` の lazy path で bounds が穴の値を持つ場合、`contains()` で穴の値を聞かれると `false` を返すが `mins_`/`maxs_` はその値を持ったまま — solver の propagation で問題になる可能性がある（現状のテスト・ベンチマークでは問題なし）

**関連ファイル**
- `src/core/model.cpp` — set_min/set_max の SIZE_MAX ガード
- `src/core/domain.cpp` — デバッグガード削除、元の状態に復元
- `tests/cpp/test_domain_model.cpp` — 回帰テスト追加

## 2026-02-12 protein リグレッション修正

**実施内容**
- `ec6aeaf` → `bd9bce2` で protein (proteindesign12) の途中解が出なくなったバグを調査・修正
- 原因特定: IntLinEqConstraint の `propagate_lower_bounds` / `propagate_upper_bounds` / `prepare_propagation` で `model.is_instantiated(var_ids_[j])` を使用していたのが原因
- `model.is_instantiated()` (`mins_[i] == maxs_[i]`) と `Variable::is_assigned()` (`domain.is_singleton()`: `n_ > 0 && min_ == max_`) の間に propagation chain 途中で不整合が発生
- 修正: 3関数内で `model.is_instantiated()` → `vars_[j]->is_assigned()` に戻す
- 全126テスト合格、protein で途中解出力を確認

**設計判断・理由**
- SoA 配列 (`mins_`/`maxs_`) は lazy path により Domain オブジェクトと完全に同期していない場合がある（前セッションの IHTC-2024 調査で発見した課題の顕在化）
- propagation callback 内では Domain ベースの `is_assigned()` が安全
- スラック最適化・2WL変更・find_internal_idx 変更は問題なし（個別テストで確認済み）

**気づいた課題・TODO**
- `model.is_instantiated()` は propagation callback 内で安全に使えない — これを明示的にコメントするか、model 側で Domain と完全同期を保証する仕組みが必要
- 他の制約で同様に `model.is_instantiated()` を使っている箇所がないか確認すべき

**関連ファイル**
- `src/core/constraints/global/int_lin_eq.cpp` — 修正箇所

## 2026-02-12 14:40〜15:00 run_search 再帰→反復化

**実施内容**
- `Solver::run_search()` を再帰呼び出しから明示的スタック（`std::vector<SearchFrame>`）による反復ループに変換
- `SearchFrame` 構造体に `remaining_cl`（per-frame conflict limit）を追加し、再帰版の by-value セマンティクスを保持
- `ascending` フラグによる状態機械で降下（新レベル進入）・上昇（子の結果処理）・TRY VALUES（値試行）を制御

**設計判断・理由**
- `conflict_limit` を単一グローバル変数にすると、子レベルの decrement が親に漏れてしまう。再帰版では by-value 渡しで各レベルが独立したCLコピーを持つため、per-frame `remaining_cl` で同一動作を再現
- ヘッダ（`solver.hpp`）は変更なし。`run_search` のシグネチャも同一を維持

**気づいた課題・TODO**
- Release ビルドで skill-allocation を90秒以上実行すると `corrupted size vs. prev_size` でクラッシュする問題は**変更前（再帰版）から存在する既知の問題**。ASan debug ビルドでは120秒完走するため、別途調査が必要
- 既存コードの NoGood activity 更新に dead code あり（`for` ループ内で即 `break`、L343相当）。そのまま保持

**関連ファイル・コミット**
- `src/core/solver.cpp` — `run_search` メソッド全面書き換え
- 全126テスト パス
- skill-allocation 60秒: 解あり（objective=61）、TIMEOUT で正常終了
- skill-allocation 120秒 ASan: 解あり（objective=65）、TIMEOUT で正常終了

## 2026-02-12 skill-allocation ヒープ破壊クラッシュ根本原因修正

**実施内容**
- `remove_value` 内の support 値更新を bounds 更新の**後**に移動（根本原因修正）
  - 旧: `vals[0]` を無条件に support に代入 → lazy な `mins_` 以下の値が support になりうる
  - 新: bounds スキャン後に `vals[0]` が bounds 内か検証し、bounds 外なら有効な値を探索
- `set_min`/`set_max` の lazy パスに support bounds チェックを追加（defense in depth）
  - `set_min`: `support_values_[var_idx] <= maxs_[var_idx]` 追加
  - `set_max`: `support_values_[var_idx] >= mins_[var_idx]` 追加
  - support が stale な場合は sync パスにフォールスルー
- デバッグ用 `printf`/`abort` を全て削除、`assert(idx != SIZE_MAX)` に置換（ユーザーによる手動変更）

**設計判断・理由**
- 破綻パス: `set_min` lazy → `mins_` 引き上げ → `remove_value` で support 削除 → `vals[0]` が `mins_` 以下 → `set_max` lazy singleton path で `index_of` が SIZE_MAX → ヒープ破壊
- support 更新を bounds 更新後に行うことで、常に bounds 内の値が support になることを保証
- lazy パスの bounds チェックは defense in depth（support が何らかの理由で stale でも安全にフォールスルー）

**気づいた課題・TODO**
- なし（クリーンに解決）

**関連ファイル**
- `src/core/model.cpp` — `remove_value`, `set_min`, `set_max` の3メソッド修正
- 全126テストパス、skill-allocation 120秒クラッシュなし

## 2026-02-12 is_defined_var アノテーション対応

**実施内容**
- FlatZinc の `is_defined_var` アノテーションを認識し、変数選択で decision var を優先する機能を実装
- `VarDecl` にフラグ追加、パーサの9箇所でアノテーション処理追加
- `Model` に SoA フラグ `is_defined_var_` と accessor を追加
- `Solver` の `var_order_` を `[decision vars | defined vars]` にパーティション分割
- `select_variable()` で decision 区間を先にスキャン、全 instantiated なら defined 区間にフォールバック
- リスタート時のシャッフルは各区間を独立に実行

**設計判断・理由**
- `var_order_` のパーティション分割方式を採用（別配列ではなく境界インデックス `decision_var_end_` で管理）
- defined var がない場合は `decision_var_end_ == var_order_.size()` で従来と同一動作を保証

**気づいた課題・TODO**
- ベンチマークでの性能比較はまだ未実施（tower, hitori 等で検証予定）

**関連ファイル**
- `include/sabori_csp/fzn/model.hpp`, `include/sabori_csp/model.hpp`, `include/sabori_csp/solver.hpp`
- `src/fzn/parser.yy`, `src/core/model.cpp`, `src/core/solver.cpp`, `src/fzn/model.cpp`

## 2026-02-12 In-Search Branch-and-Bound 最適化の実装

**実施内容**
- 最適化を探索ループ内で処理する branch-and-bound アプローチを実装
- `Model::find_variable_index()` を追加（名前→インデックス検索、エイリアス対応）
- `Solver::solve_optimize()` / `search_with_restart_optimize()` を追加
  - 解が見つかったら root へバックトラックし、目的変数のドメインを root level で永続的に縮小
  - 伝播で UNSAT になれば最適確定、探索空間が尽きても最適確定
  - NoGood・Activity・探索状態を全て自然に保持
- `main.cpp` の `solve_optimize()` を簡略化（外側 while ループ、Model 再構築、NoGood/Activity シリアライズを全て除去）

**設計判断・理由**
- `rewind_to(root_point)` は `level > root_point` のエントリのみ pop するため、`current_decision_ = root_point` にリセットしてから `process_queue` を呼べばドメイン縮小が root level に永続化される
- 解発見時に `inner_limit` を `initial_conflict_limit_` にリセット（新しい問題空間に入るため）
- `current_best_assignment_` を解で更新し、次の探索での値選択を改善

**気づいた課題・TODO**
- `tests/fzn/output_format/test_output_format.sh` の satisfy 単一解テストが `==========` を出力する既存バグがある（今回の変更とは無関係）

**関連ファイル**
- `include/sabori_csp/model.hpp` / `src/core/model.cpp` — `find_variable_index` 追加
- `include/sabori_csp/solver.hpp` / `src/core/solver.cpp` — `solve_optimize`, `search_with_restart_optimize` 追加
- `src/fzn/main.cpp` — `solve_optimize` 簡略化
- 全126テスト PASS

## 2026-02-12 最適化途中解でリスタートパラメータを完全リセット

**実施内容**
- `search_with_restart_optimize` で途中解（改善解）が得られた際に、リスタートスケジュールを完全リセットするよう変更
- `inner_limit` のリセットに加え、`outer_limit = 10.0` にリセット
- `continue` → `break` に変更し、outer for ループを抜けて while ループから再突入させることで `outer` カウンタも 0 から再開

**設計判断・理由**
- 途中解が見つかると目的変数のドメインが縮小され、実質的に新しい問題空間に入る
- 旧リスタートスケジュールの蓄積状態（大きくなった `outer_limit` やカウンタ位置）を引きずると、新しい問題空間に対して不適切なリスタート間隔になる
- `outer = -1` で直接ループ変数をリセットするより、`break` で外側 while に戻る方がクリーン

**気づいた課題・TODO**
- なし

**関連ファイル**
- `src/core/solver.cpp` L371-373（`search_with_restart_optimize` 内 SAT 分岐）

## 2026-02-12 count_eq 制約ネイティブ実装

**実施内容**
- `count_eq(x[], target, c)` 制約をネイティブ実装
  - `CountEqConstraint` クラス: definite_count / possible_count の差分追跡で O(1) インクリメンタル伝播
  - on_instantiate, on_set_min, on_set_max, on_remove_value, on_last_uninstantiated, on_final_instantiate を実装
  - Trail ベースのバックトラック（is_possible_ の変更履歴を記録）
- FlatZinc パーサに `fzn_count_eq` / `count_eq` マッピングを追加
- MiniZinc redefinition (`fzn_count_eq.mzn`) を作成し `redefinitions.mzn` に登録
- C++ ユニットテスト 5 件 + FlatZinc テスト 3 件を追加
- 全 131 テスト pass

**設計判断・理由**
- `vars_` = `[x[0], ..., x[n-1], c]` 配列で統一管理（IntLinEq と同パターン）
- Trail に `is_possible_changes` ベクタを持たせ、バックトラック時に is_possible_ を正確に復元
- target は定数のみ対応（FlatZinc で変数 target の場合は singleton なら値抽出、非 singleton はエラー）

**ベンチマーク結果 (groupsplitter)**

| Instance | | Vars | Cstr | Fails |
|---|---|---|---|---|
| u12g2pref0 | Native | 233 | 159 | **43** |
| | Decomp | 321 | 247 | 47 |
| u15g3pref0 | Native | 294 | 200 | **28** |
| | Decomp | 462 | 368 | 126 |
| u15g5pref1 | Native | 302 | 204 | **2** |
| | Decomp | 582 | 484 | 28 |

- 変数削減: 最大 48%、制約削減: 最大 58%
- 探索効率: u15g3pref0 で 4.5x、u15g5pref1 で 14x

**気づいた課題・TODO**
- `squashfs-root` への MiniZinc ファイルコピーが手動（ビルドスクリプトで自動化すべき）
- groupsplitter は全インスタンス UNSAT（solver 自体の探索力の問題）

**関連ファイル**
- `include/sabori_csp/constraints/global.hpp` — CountEqConstraint 宣言追加
- `src/core/constraints/global/count_eq.cpp` — 新規: 実装
- `src/core/CMakeLists.txt` — ビルド対象追加
- `src/fzn/model.cpp` — パーサマッピング追加
- `share/minizinc/sabori_csp/fzn_count_eq.mzn` — 新規
- `share/minizinc/sabori_csp/redefinitions.mzn` — include 追加
- `tests/fzn/constraints/count_eq/` — FlatZinc テスト 3 件
- `tests/cpp/test_global_constraints.cpp` — C++ テスト追加
- `docs/CONSTRAINTS.md` — ドキュメント更新
