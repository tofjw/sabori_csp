# 2026-06-11

## 2026-06-11 23:10 〜 00:15 作業開始〜終了
（前日 23:10 開始、日付をまたいだセッション）

**実施内容**
- ロードマップ施策①「BoolDomain + bool_clause 2WL」に着手する前に gprof で再プロファイル（build-prof/ に -pg ビルド）
  - elitserien handball1: ボトルネックは bool/reif ではなく**前日導入の適応 GAC が CPU の約63%**（compute_sccs_and_filter 38% + tarjan 17% + hk_bfs 6%）、bounds(Z) も並走で約9%
  - fillomino: **array_var_int_element が42%**（on_set_max 20% + propagate_via_queue 20%、172M回呼び出し）+ int_lin_eq 14%。bool_clause は上位に現れず（既に watched-literal 実装済みと判明）
  - → **施策①は実測で棄却し、GAC ホットパス最適化に方針転換**
- GAC ホットパス最適化（all_different_gac.cpp/hpp）:
  1. **エコー集約**: 親の forward-checking / GAC 自身が enqueue した値除去のエコーごとにフル GAC（マッチング+SCC）を再実行していたのを、PendingEcho キュー（FIFO 照合）で「バッチ末尾の1回」に集約。GAC は1パスで fixpoint に達するため自己除去エコーでは再実行不要
  2. **ハイブリッド方式**: 高頻度の bounds イベント（set_min/max、他制約の bounds 伝播で毎ノード大量発生）ではフル GAC を走らせず、親の軽量処理（Hall ペア + bounds(Z)）に委譲。フル GAC は instantiate / remove_value のみ
  3. hk_dfs の再帰内 std::vector 確保を深さ別バッファプール（hk_buf_pool_）に置換
- bench_alldiff.py で検証（ベースライン 22:47 の適応GAC → 00:05 ハイブリッド）:
  - Wins 5→6（✅ 増加）、Timeouts 1→1（✅ 同じ）
  - yumi-static が obj 925→611 で **Sabori 勝ちに反転**（CP-SAT 720 を上回る）
  - sudoku_fixed の解発見 41.9s→32.9s。sudoku_opt SOL obj=-3 維持
  - 悪化: elitserien handball2 obj 3→5、graph-clear 62→70、cgt 2826→4031（いずれも勝敗に影響なし）
- ctest 200件全合格。ピンポイント（sudoku_p20 / elitserien handball1）は解発見維持

**設計判断・理由**
- 「最適化前に再プロファイル必須」（リファクタ計画 v2 の鉄則）が機能した。ギャップ分析の机上仮説（bool レイヤ）は実測で覆り、真のホットスポットは自分が前日入れたコードだった
- エコー照合は FIFO 前提（Model の pending_updates_ は vector + read index で FIFO 処理）。照合失敗時は「外部イベント扱いでフル実行」に倒す（健全側）。バックトラック・fixpoint 再計算・prepare 時にキューをクリアして staleness を自己回復
- ハイブリッドの根拠: SCC 実行497万回の主因は remove エコーではなく set_min/max イベント（エコー集約だけでは 4.96M→4.97M でほぼ不変だった）。bounds イベントごとのフル GAC は「伝播キューに propagator スケジューリングがない」現アーキテクチャの構造問題で、イベント単位の即時実行をやめるには propagator queue の導入（リファクタ計画フェーズ5-6 級）が必要

**気づいた課題・TODO**
- **次の最有力候補（実測根拠あり）**: ArrayVarIntElementConstraint。fillomino で42%、on_set_max → propagate_via_queue 172M回。fillomino は TIMEOUT 負け問題（CP-SAT 0.3s）
- propagator queue（イベント即時実行→制約スケジューリング）はアーキテクチャ級の改善候補。GAC/element/regular 全てに効く
- elitserien はまだ CP-SAT に OPTIMAL 速度で負け（obj=5 vs optimal=2）。reif 大量モデルの本丸は conflict learning（施策②）
- ロードマップの施策①は「実測で棄却」と分析ドキュメントに反映済みにすべき → 次回更新

**関連ファイル・コミット**
- `src/core/constraints/global/all_different_gac.cpp` — エコー集約 + ハイブリッド方式 + バッファプール
- `include/sabori_csp/constraints/all_different_gac.hpp` — PendingEcho / hk_buf_pool_ / hk_dfs 署名
- `build-prof/` — gprof 用ビルド（-pg、未コミット対象外）
- ベンチレポート: `benchmarks/minizinc_challenge/sabori_benchmark_alldiff_20260611_000552.html`（ベースライン: `..._20260610_224725.html`）
- コミット: 未コミット（作業ツリーに変更あり）

## 2026-06-11 00:20 〜 01:50 作業開始〜終了

**実施内容**
- ArrayVarIntElementConstraint の非効率を分析・修正
  - 特定した問題: (1) 密度ゲートが広ドメインで support tracking を無効化し毎イベント全スキャン、(2) trail を save_point 重複チェックなしで毎イベント push（fillomino で push/rewind 各172M回）、(3) result bounds を変化の有無に関わらず毎スキャン enqueue（約3.4億件の no-op キュー流量）
  - (1) support tracking 常時有効化は実装したが、**伝播タイミングの変化で探索軌道が変わり stripboard が SOL→解なしに悪化**（決定論比較で確証）したため取り下げ
  - (2)(3) は no-op 削減のみで軌道不変（決定論比較で stripboard/sudoku/elitserien の結果一致を確認）のため採用。bench_alldiff はベースラインと同値（Wins 6 / TO 1）
  - fillomino は未解放: イベントが index/support 変数に集中しており残る全スキャンは真に必要な再計算。解放には conflict learning か propagator queue が必要
- **ユーザー報告のリグレッション修正: p1f-pjs (2020) が約2倍遅化**
  - 二分探索で原因特定: 適応 GAC (2026-06-10 22:33) が `make_circuit` の併設 alldifferent にも適用され、p1f-pjs では circuit ×36 の併設 alldiff が全て GAC 化 → **circuit 自身の SCC フィルタと同一グラフ構造の推論が二重化**
  - 修正: 併設 alldifferent は gac_favorable の対象外に（`-G` 明示強制は維持）
  - 結果: p1f-pjs[10] 12.6s → **5.0s**（GAC 導入前の 7.4〜8.1s より高速）。ベンチで OPTIMAL 7.1s vs CP-SAT 14.8s
- 検証: ctest 200件全合格。決定論ピンポイント5問（p1f10 / p1f-pjs-2021[12] / sudoku_p20 / elitserien handball1 / stripboard）全て解発見維持
- bench_circuit (01:42) Wins 7/TO 3、bench_alldiff (01:47) Wins 5/TO 1 — 17:22 / 00:57 比の差分は全てマージナル行（p1f系[12] は「33秒のボーダーライン発見」が4並列負荷で押し出されたもので fzn 直接では解1を維持。yumi-static は既知の揺れ行: 今日だけで 954/554/599/866/611/801）

**設計判断・理由**
- element の support tracking 常時有効化を取り下げた基準: 「理論上同等の伝播でも発火タイミングが変わると境界線上の問題で結果が反転する」。軌道不変な変更（no-op 削減）と軌道を変える変更（伝播タイミング変更）を区別し、後者はベンチゲートで個別に審査する
- circuit 併設 alldiff の GAC 除外の根拠: circuit SCC フィルタはドメイングラフの強連結性・到達可能性を直接推論しており、併設 alldiff の Régin GAC（二部グラフ SCC）と情報が大きく重複する。残した bounds(Z) + Hall ペアは circuit が見ない値レベルの情報を補完する

**気づいた課題・TODO**
- **教訓: registry レベルの変更（制約の構築方法）は、その制約を含む全ファミリのベンチで再検証する**。適応 GAC は bench_alldiff のみでゲートし、make_circuit 経由の影響（bench_circuit）を見落とした
- p1f-pjs[10] のプロファイルで GAC 有効 alldiff の bounds(Z) が25%（GAC は bounds(Z) を包含するため冗長）。スキップは過去に軌道悪化で取り下げたが、証明時間2割削減の余地として残存
- p1f 系インスタンス[12] は30秒ボーダーラインのマージナル行としてベンチ上で出入りする。真の改善は[12]を余裕で解く水準（conflict learning 等）
- fillomino / インスタンス16 以降の壁: 伝播強化では届かない。ロードマップの ②conflict learning / propagator queue が本丸

**関連ファイル・コミット**
- `src/fzn/constraint_registry.cpp` — make_circuit の併設 alldifferent から自動 GAC を除外（コメントに実測根拠を記載）
- `src/core/constraints/global/array_var_int_element.cpp` / `include/sabori_csp/constraints/global.hpp` — save_support_trail（dedup）+ 変化時のみ enqueue。support tracking ゲートは現状維持
- ベンチレポート: `benchmarks/minizinc_challenge/sabori_benchmark_circuit_20260611_014222.html` / `sabori_benchmark_alldiff_20260611_014721.html`
- コミット: 未コミット（作業ツリーに変更あり）

## 2026-06-11 02:00 〜 02:50 作業開始〜終了

**実施内容**
- 「conflict learning vs propagator queue のどちらが有望か」を本実装前に実測判定
  - 実験1: 壁問題8問を Gecode（学習なしCP）/ Chuffed（学習ありLCG）で30秒実行
  - 実験2: 同問題を sabori 300秒で実行しギャップ倍率を測定
  - スクリプト: benchmarks/minizinc_challenge/exp_oracle.sh / exp_gap.sh
- 結果と結論: **conflict learning が本命**。詳細は docs-dev/learning-vs-queue-experiment-2026-06-11.md
  - 「学習ありのみ解ける」5/8問、うち3問（fillomino/solbat/amaze）は sabori 300秒でも解なし（ギャップ10倍超 = 定数倍では届かない）
  - 一方 sudoku_opt 証明 83.9s / elitserien 証明 92.8s / oocsp UNSAT 29.1s / pentominoes 29.2s はギャップ 1〜3× で、propagator queue（定数倍）でも回収圏内
  - 進め方: queue → learning の順で同一路線として実装（queue は LCG の必須インフラ + 中間マイルストーン）
- 副次的発見: msc の stdFlags ["-a"] により minizinc 経由と fzn 直接実行で実行モードが異なり、境界問題（p1f-pjs[12]）では解の有無が変わる。決定論比較では -a の有無を揃えること

**設計判断・理由**
- 事前に判定ルールを固定してから実験（「Chuffed のみ過半 かつ ギャップ10倍超 → learning」）し、結果解釈の恣意性を排除
- Chuffed を上限見積りに使った理由: CP伝播+学習という構成が「sabori に学習を足した姿」に最も近く、CP-SAT より公平

**気づいた課題・TODO**
- sabori300 の初解時刻計測（--output-time）が機能していない（first_sol が timeout 値になる）。次回計測時に修正
- 実験は minizinc 経由（-a なし）で実行したため、ベンチ（-a あり）と探索挙動が微妙に異なる可能性。ギャップ倍率の傾向判断には影響しない水準と判断

**関連ファイル・コミット**
- docs-dev/learning-vs-queue-experiment-2026-06-11.md（実験記録・結論）
- benchmarks/minizinc_challenge/exp_oracle.sh / exp_gap.sh（再実行可能な実験スクリプト）
- コミット: 未コミット

## 2026-06-11 03:00 〜 10:50 作業開始〜終了（ベンチ待ち時間含む）

**実施内容**
- **propagator queue 第一段階を実装・採用**（オラクル実験の判定「queue → learning」の前段）
  - Model に schedule_constraint_batch / pop_scheduled_constraint を追加（フラグで重複登録排除、clear_pending_updates と連動してバックトラック時にクリア）
  - Constraint 基底に propagate_batch(model, save_point) 仮想フックを追加
  - Solver::process_queue を二層化: イベントキュー完全排出 → スケジュール済み制約のバッチ実行 → 発生イベント優先 → … の fixpoint ループ
  - 最初の搭載制約 = alldifferent: bounds(Z) をイベント毎 → バッチ毎1回に、GAC を remove イベント毎 → バッチ毎1回に。仮想ディスパッチで GAC 有効時は GAC・無効時は bounds(Z) を選択し、**GAC 時の bounds(Z) 二重実行（p1f で25%）も解消**
  - 旧エコー集約機構（PendingEcho）はバッチ化により不要となり削除
- 検証の経過:
  - 専用プローブ: sudoku_opt 証明 83.9s→31.6s、elitserien_h1 証明 92.8s→33.6s（2.8倍。オラクル実験のギャップ倍率予測と一致）
  - 3ファミリベンチ: circuit Wins 7→9/TO 3→2 と大幅改善。alldiff/diffn はカナリア行（stripboard, code-gen-2023）の反転で TO が数値上悪化 → ユーザー判断で全年度ベンチへ
  - **全年度ベンチ（279問、20260611_batchq/）**: Sabori 110→104勝だが、解なし TIMEOUT 29→25 件、ステータス向上7件（elitserien×4 TIMEOUT→SOL/OPTIMAL、sudoku×2 TIMEOUT→SOL、bnn-planner SOL→OPTIMAL）、OPTIMAL 高速化 13件 vs 遅化 6件
  - **60秒検証（ユーザー提案）**: 新たに負けた14問を新版60秒で実行 → **同等7問 / 改善7問 / 悪化0問**。ma-path-finding obj 369→279、code-gen 16323→14046、proteindesign 1474→1473、mqueens 証明 12.6→9.0s 等。Wins -6 が「30秒境界の到達タイミングジッタ」であり能力劣化ゼロであることを確証 → 採用決定
- ctest 200件全合格

**設計判断・理由**
- イベント優先（バッチはキューが空の時のみ1つ実行）: 安価なインクリメンタル伝播（lin の差分更新等）の即時性を保ちつつ、フル再計算型だけをバッチ化する二層構成。既存制約は無変更で共存可能
- 採用判定で「ヘッドラインの Wins」ではなく (1) 解なしTOの減少 (2) ステータス向上 (3) 60秒での同等以上、を重視した。30秒境界のジッタは能力の劣化ではない（60秒検証がこれを直接証明）

**気づいた課題・TODO**
- 次の搭載候補: regular（compute_and_filter がイベント毎）、circuit SCC、element の propagate_via_queue → さらなる定数倍が見込める
- 本命の conflict learning へ: バッチ実行点は将来の「理由記録 → 衝突解析」の挿入点になる
- ベンチの30秒境界ジッタが Wins 比較のノイズ源。判定には「解なしTO数 + ステータス遷移 + 延長タイムアウト検証」の併用が有効（今回の手順を標準化する価値あり）

**関連ファイル・コミット**
- `include/sabori_csp/model.hpp` / `src/core/model.cpp` — スケジューリング API・状態
- `include/sabori_csp/constraint.hpp` — propagate_batch フック
- `src/core/solver.cpp` — process_queue 二層化
- `src/core/constraints/global/all_different.cpp` / `all_different_gac.cpp` + 各ヘッダー — バッチ化、PendingEcho 削除
- ベンチ: `benchmarks/minizinc_challenge/20260611_batchq/`（全年度）、`exp_60s_check.sh`（60秒検証）
- コミット: 未コミット

## 2026-06-11 11:00 〜 12:30 作業開始〜終了

**実施内容**
- バッチ化の横展開を1制約ずつ実施・個別判定（propagator queue 第一段階の続き）
  1. **element（array_var_int_element）: 実装 → 実測悪化 → 取り下げ（revert）**
     - elitserien 証明が 33.6s → 51.0s（単独実行）に悪化
     - 教訓: **バッチ化が効くのは「1回が高価な propagator」だけ**。element のフルスキャンは O(|dom(index)|) と安価で、遅延させても節約がなく channeling（index確定→arr↔result 同期）の即時性だけ失う
  2. **regular: 採用**
     - 4コールバック全てが「trailマーク + DFAフルパス」だったのをバッチ毎1回に
     - elitserien_h1 証明 **17.5s**（33.6s から半減、学習なしの Gecode 19.0s を初めて超えた）
     - **pentominoes SOL 0.3s**（300秒プローブで29.2s → 100倍。Gecode/Chuffed の 9 秒も大幅超え）
     - ゲート: bench_alldiff Wins 5→5 / TO 2→1 ✅。sudoku_opt OPTIMAL 34.9s / sudoku_fixed OPTIMAL 16.4s
  3. **circuit SCC: 採用**
     - on_instantiate 毎の SCC パス（build_scc_graph + 強連結 + 入次数ルール）をバッチ毎1回に
     - ゲート: bench_circuit Wins 9→9 / **TO 2→0** ✅
     - p1f[12] / p1f-pjs-2021[12] がマージナル圏から安定 SOL に（CP-SAT は TIMEOUT のまま）、cvrp obj 1488 で逆転勝ち、stochastic-vrp OPTIMAL 4.9s
- 各ステップで ctest 200件全合格

**設計判断・理由**
- 「1制約 = 実装 → ピンポイント決定論比較 → 家族ベンチゲート → 採用/取り下げ」のサイクルを厳守。element の取り下げ判断が高速にできた（単独実行の決定論比較で +50% 悪化を即検出）
- バッチ化の適用基準を確立: **propagator 1回のコストが O(n log n) 以上（GAC マッチング、bounds(Z)、DFA パス、SCC グラフパス）ならバッチ化、O(domain) 程度の軽量スキャンや O(1) インクリメンタル更新はイベント粒度のまま**

**気づいた課題・TODO**
- 次のバッチ化候補（高価則に合致）: cumulative の TTEF（O(n³)）、disjunctive の edge-finding、inverse の presolve 風固定点。要プロファイル確認
- elitserien_h1 は 17.5s vs Chuffed 4.3s — 残りギャップは conflict learning の領域
- stripboard は queue 採用以降「fzn直接(-aなし)では解なし / ベンチ(-aあり60秒)では obj=140」の状態が継続。-a の有無で探索が変わる問題は別途調査価値あり

**関連ファイル・コミット**
- `src/core/constraints/global/regular.cpp` — 4コールバック → schedule + propagate_batch
- `src/core/constraints/global/circuit.cpp` — SCC パス → schedule + propagate_batch
- `include/sabori_csp/constraints/global.hpp` — 両クラスに propagate_batch 宣言
- element の変更は revert 済み（作業ツリーに痕跡なし）
- ベンチ: `sabori_benchmark_alldiff_20260611_*.html`（regular ゲート）、`sabori_benchmark_circuit_20260611_*.html`（circuit ゲート）
- コミット: 未コミット（regular + circuit のバッチ化）

## 2026-06-11 13:10 〜 14:00 作業開始〜終了

**実施内容**
- **cumulative のバッチ化を実装・採用**（横展開4件目。高価則: timetable O(n^2) + TTEF O(n^3) がイベント毎だった）
  - on_instantiate / on_set_min / on_set_max → schedule_constraint_batch、propagate_batch = run_all_engines
- 判定（標準手順: 解なしTO + ステータス遷移 + 60秒検証）:
  - bench_cumulative: Wins 6→5 / **TO 1→0** ✅
  - ステータス向上1件: yumi-dynamic TIMEOUT→SOL obj=762。低下0件
  - obj 改善: code-generator 17373→11201、ihtc-kletzander 600→565
  - 新たな負け2件の精査: mrcpsp = 両者 OPTIMAL のサブ秒タイブレーク（当方も 1.3→0.5s に高速化）、ihtc-marte = 60秒検証で **obj=8139 < 旧8154** → 30秒境界ジッタと確定
- ctest 200件全合格

**関連ファイル・コミット**
- `src/core/constraints/global/cumulative.cpp` / `include/sabori_csp/constraints/global.hpp`
- ベンチ: bench_cumulative ベースライン+ゲートの2回分 HTML
- コミット: 未コミット（cumulative バッチ化）

**次の候補**: disjunctive（edge-finding がイベント毎なら同パターン）。その後は本命の conflict learning へ

## 2026-06-11 14:10 〜 15:00 作業開始〜終了

**実施内容**
- **disjunctive のバッチ化を実装・採用**（横展開5件目、これで完了）
  - コールバックのインクリメンタル処理（compulsory part の trail 更新、確定タスクの bitmap 設置、衝突チェック）はイベント粒度のまま残し、高価な `propagate_bounds`（全タスクスキャン + edge-finding O(n²×horizon)）の呼び出し7箇所をバッチ登録に置換
- 判定: bench_disjunctive **Wins 5→6 ✅ / TO 1→0 ✅**（両基準同時クリア）
  - **stripboard が「両者TIMEOUT」→ SOL obj=190 で Sabori 勝ちに**（数日揺れ続けたカナリア行が初の安定圏入り。CP-SAT は TIMEOUT のまま）
  - test-scheduling OPTIMAL 1.4→0.7s / 0.9→0.6s。悪化なし
- ctest 200件全合格

**バッチ化横展開の総括**
| 制約 | 判定 |
|------|------|
| alldifferent (bounds(Z)/GAC) | 採用（第一段階） |
| regular | 採用（elitserien 17.5s、pentominoes 0.3s） |
| circuit SCC | 採用（bench_circuit TO 2→0） |
| cumulative | 採用（TO 1→0） |
| disjunctive | 採用（Wins+1 / TO-1、stripboard 圏内化） |
| element | 取り下げ（軽量 propagator は不向き） |

**次**: 本命の conflict learning（バッチ実行点 = 将来の理由記録→衝突解析の挿入点）

**関連ファイル・コミット**
- `src/core/constraints/global/disjunctive.cpp` / `include/sabori_csp/constraints/global.hpp`
- コミット: 未コミット（disjunctive バッチ化）

## 2026-06-11 15:10 〜 16:40 作業開始〜終了

**実施内容**
- **conflict learning Phase L0(基盤)を実装**(設計: docs-dev/conflict-learning-design.md v1.1)
  1. 発生源追跡: `PendingUpdate.source_cid`(enqueue 時スナップショット)+ `ScopedPropagator`(RAII)。
     ディスパッチループ6箇所・バッチ2箇所・nogood伝播にガード設置
  2. 推論トレイル: `InferenceEntry`(16B POD: value/var/source24bit/type8bit)+ `level_start_` 境界配列。
     **記録時に current_decision_ へ遅延自己同期**する方式により、決定/バックトラックの全箇所への計装が不要
  3. 記録点: process_queue の Instantiate(Eq)/SetMin(Geq)/SetMax(Leq)成功直後 + バイナリドメインの
     RemoveValue(Eq[x=1-v] 正規化)。`if (learning_enabled_)` 1分岐のみ
  4. `analyze_conflict`(L0: シンボリック decision-approx、結果は構成上 decision_trail_ と一致)
  5. `bounds_at(var, T)`: 推論時点の bounds 再構成。**bounds の単調性により「T より前の最後の
     Geq/Leq/Eq エントリの値」で十分**(設計書の「逆適用」より単純化。旧値の保存不要)
  6. `-L` CLI フラグ / `Solver::set_learning`

**L0 ゲート結果(全合格)**
- off 同等性: p1f10 PROVED / elitserien 解1 / stripboard 解1 — 変更前と同一
- 記録オーバーヘッド: p1f10 で CPU 時間 off 6.06s vs on 6.00s — **ノイズ未満**(on/off は L0 では同一軌道)
- 退化性: Debug ビルド + -L で sudoku 60秒(大量衝突)+ p1f10 完走、`分析結果 == decision nogood` の assert 違反ゼロ
- bounds_at ユニットテスト + learning on のランダム全解数照合(15試行)合格
- ctest **202件**(新規2件含む)全合格

**設計判断・理由**
- レビュー指摘の反映が機能: source_cid のスナップショット方式(処理時推測の禁止)、
  シンボリック decision-approx(フォールバック O(1))、bounds_at の単調性ベース実装
- L0 の「on でも同一軌道」は意図的(学習経路は既存 learn_from_conflict のまま)。
  L1 で bool_clause の説明が入って初めて節が変わる

**次: Phase L1**(bool_clause + nogood 伝播の説明、explain_failure)。ピンポイント = solbat UNSAT / oocsp

**関連ファイル・コミット**
- `include/sabori_csp/model.hpp` — source_cid / センチネル / ScopedPropagator
- `include/sabori_csp/solver.hpp` / `src/core/solver.cpp` — 推論トレイル・記録・分析・bounds_at・配線
- `src/fzn/main.cpp` — -L フラグ
- `tests/cpp/test_nogood.cpp` — L0 テスト2件
- コミット: 未コミット

## 2026-06-11 16:50 〜 19:00 作業開始〜終了

**実施内容: conflict learning Phase L1（+L2先行分）— チェックポイント**
- **重大な設計修正**: 学習フックを handle_failure から**衝突検出点**（try_enumerate/try_bisect の process_queue 直後）へ移動
  - handle_failure は「全値枯渇後」でモデルが衝突状態から巻き戻し済み → explain_failure が常に失敗していた（説明率0%の原因）
  - 併せて: enumerate の決定は process_queue を通らないため明示的に record_inference（bisect は enqueue 経由なので自動）。フォールバック nogood は decision_trail_ + 現試行リテラル
- **説明を実装した制約（6種、全て検証付き verify-or-fallback 方式）**:
  - bool_clause（遅延・payload 不要）、array_bool_and（バイナリ Eq）
  - int_lin_eq / int_lin_eq_reif（ExplainContext.bounds_at で**推論時点の bounds** から含意 bound を厳密整数演算で再計算・検証。ceil/floor div 対応）
  - int_eq_reif / int_ne_reif（bounds 交差・同値固定・バイナリ ≠ 伝播）
- インフラ拡張: ExplainContext（trail_pos + bounds_at）、PendingUpdate/InferenceEntry に aux（nogood id）、
  nogood 伝播の説明（id_map + last_conflict_nogood）、解決ループの acyclicity 強制（resolution_limit）
- 一時デバッグ計装で fallback 内訳を段階的に特定 → seed_explain 44k→3.4k、resolve の主因を順に潰した（除去済み）

**現状と正直な評価**
- 説明率: 0% → 約17%（solbat 30秒時点、説明実装の途中経過）。衝突レートは学習により増加（節が刈れている証拠）
- off 同等性 ✓ / on オーバーヘッド: p1f10 で off 6.4s vs on 6.2s（学習 on でも勝ち領域問題に悪影響なし）
- ctest 202件全合格（各制約追加ごとに確認）
- **solbat はまだ未解決**（180秒 UNKNOWN、Chuffed 13.5s）。残る resolve 失敗源は int_one_hot_channel と
  set_in_reif、および DECISION 解決（要調査: 現レベルに複数決定エントリ?）

**次のステップ（L1完了に向けて）**
1. int_one_hot_channel / set_in_reif の説明実装（同パターン）
2. resolve-fail src=DECISION の調査（1UIP 不変条件の検証）
3. それでも solbat が落ちなければ L3 要素（assertive backjump・節最小化）の前倒しを検討
4. ベンチゲートは solbat/oocsp が動いてから（説明率が低い段階でのベンチは無意味）

**関連ファイル・コミット**
- `src/core/solver.cpp` — 衝突点フック・learn_at_conflict・分析の acyclicity 強制
- `include/sabori_csp/constraint.hpp` — ExplainContext・explain/explain_failure 仮想IF
- `include/sabori_csp/literal.hpp` **新規** — Literal を solver.hpp から分離（循環回避）
- `src/core/constraints/logical.cpp` / `comparison.cpp` / `global/int_lin_eq.cpp` / `global/int_lin_eq_reif.cpp` — 説明6種
- `src/core/nogood_manager.cpp/.hpp` — id_map・衝突 nogood 捕捉・伝播の aux タグ
- コミット: 未コミット

## 2026-06-11 19:30 〜 21:30 作業開始〜終了

**実施内容: 「2025 ベンチ遅化」の調査 → 計測アーティファクトと判明**
- ユーザー指摘: batchq → lcg1 で 2025 が一律 +8〜20% 遅化(obj/ステータスは全問同一)
- 系統的バイセクト: pre-L0/L0/現在 の3バイナリ交互実行 → 当初「L1 が +11%」に見えた
- 容疑を順に潰すも全て非該当(explain コード存在 / ディスパッチストア / solver.cpp 学習コード /
  Solver メンバ配置 / comparison.cpp / nogood ガード / id_map)
- **真相1: `-fPIC` の不公平比較** — メイン build/ は BUILD_PYTHON_BINDINGS=ON で core が -fPIC(約12%遅)、
  切り分け用 worktree はデフォルト OFF で -fPIC 無し。同一ソースでもバイナリが約12%違った
- **真相2: ベンチの負荷汚染** — lcg1 の 2025 ベンチ(19:47)は当方の solbat 長時間実験と並走していた
- **公平条件(-fPIC 同士・交互実行)の最終結果: learning off の L0+L1 オーバーヘッドはゼロ(ノイズ内)**。
  hitori 10.6-10.8 vs 10.7-10.8、EchoSched 1.93-1.95 vs 1.93-1.95。C1 充足。ctest 202件合格

**追跡中に適用した恒久的な緩和(維持)**
- PendingUpdate 32B → 24B 再パック(Type:uint8 + var:uint32)
- ディスパッチループの発生源設定を RAII → 直接代入(決定 enqueue 前に明示リセット)
- record_inference のアウトライン化 + __builtin_expect(process_queue の肥大抑制)
- nogood 伝播ガードの de-RAII、id_map_ 維持の学習モード限定化(set_learning_mode)

**教訓(メモリ保存)**
- ベンチ比較の前提: (a) 同一 configure フラグ(特に BUILD_PYTHON_BINDINGS → -fPIC で約12%差)
  (b) 他の重いプロセス非並走。worktree ビルドでは -DBUILD_PYTHON_BINDINGS=ON を明示
- 「全容疑の revert で消えない一様な数%差」はコード犯人ではなく環境差を疑う

**関連ファイル・コミット**
- 緩和: model.hpp / solver.cpp / solver.hpp / nogood_manager.* (L1 と同梱、未コミット)
- worktree: /tmp/pre-l0, /tmp/l0-only(調査用、削除可)
