## 2026-05-06 22:00〜2026-05-07 00:30 (深夜跨ぎ)

**実施内容**

- `bench_compare.py 2021` で `connect` が CHECK_FAIL になる件を調査
  - 既存コメントの「MiniZinc 2.9.5 の -O1 alias-loss バグ」説が誤りと判明（-O0 でも UNSAT）
  - 真の原因: `connect__0018.mzn` の `nNzPosXYZ` 定義内 `is_fixed(nPosXYZ[m,c])` 分岐が、変数固定有無で flatten 経路を変える。検証時 (全変数固定) と本番時 (一部自由) で生成される FZN が異なり、検証側で誤 UNSAT 検出
  - sabori の解は FZN 制約をすべて満たすことを gecode で確認
- `verify_solution` を再設計（`benchmarks/minizinc_challenge/bench_compare.py`）
  - V1 (FZN単独): gecode 用 .fzn に sabori の output_var 値を `int_eq` で注入する方式に置換 → connect は OK だが enum/opt int で SKIP 多発
  - V2 (alias chain): `.ozn` から `inFlow = array2d(...,X_INTRODUCED_57_)` 等のエイリアスを抽出し X_INTRODUCED_ を直接固定 → mapping 等の TIMEOUT 解消
  - V3 (hybrid 確定版): MZN レベル検証を先に実行し、FAIL のときだけ FZN レベルでセカンドオピニオン取得。FZN が OK なら誤検出として CHECK_OK に上書き。enum/opt int 系のカバレッジを保ったまま connect の誤検出も解消（14/14 CHECK_OK）
- FlatZinc 出力仕様違反を発見・修正（`src/fzn/main.cpp:280-282`）
  - SAT 問題で `-a` なし・1解見つけて終了する場合、`==========` を出してはいけない（探索完了を証明していないため）
  - 出力末尾を `----------` のみに修正
- 既存の `tests/fzn/output_format/test_output_format.sh` を ctest に統合（`tests/CMakeLists.txt`）
  - これまで ctest に組み込まれていなかったため、上記仕様違反を検出できていなかった

**設計判断・理由**

- **ハイブリッド検証**: 純 FZN レベルだけだと enum / opt int (`<>`) / indexed array は `int_eq` で固定不能で SKIP になる。純 MZN レベルだけだと `is_fixed()` 起因の誤 FAIL を防げない。MZN を主、FZN を FAIL 救済用フォールバックにすることで両方の弱点を相殺
- **`.ozn` 経由のエイリアス解決**: gecode 用 flatten で生成される `.ozn` は同じ flatten の `.fzn` と整合する。sabori 用の `.ozn` を流用しないことで、X_INTRODUCED_ の番号ずれ問題を回避
- **`==========` 削除**: FlatZinc 仕様 (handbook §8.2) で `==========` は探索完了マーカー。SAT で `-a` なし・1解で停止した場合は探索完了を証明していないので出すと誤った情報を与える

**気づいた課題・TODO**

- `bench_compare.py` で残る検証 SKIP は無し（2021 ベンチで全 14/14 CHECK_OK）
- ハイブリッドのため CHECK_FAIL 時は MZN+FZN の2回検証が走るが、connect では両方とも数秒で完了するため実害なし
- `tests/fzn/output_format/test_output_format.sh` の他にも未統合の shell テストがあるかは未調査

**関連ファイル**

- `benchmarks/minizinc_challenge/bench_compare.py` — verify_solution をハイブリッド化、judge_winner のコメント修正
- `src/fzn/main.cpp` — solve_satisfy() の no-`-a` パスから余分な `==========` 削除
- `tests/CMakeLists.txt` — `fzn_output_format` テスト追加
