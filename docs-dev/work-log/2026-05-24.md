## 2026-05-24 コミュニティ分析の診断専用化と関連デッドコード削除

### 2026-05-24 07:58 作業開始〜終了

**実施内容**
- (2026-05-22) Label Propagation のタイブレーク偏りを修正
  - `src/core/community_analysis.cpp`: ID 昇順タイブレーク (`comm < best_community`) を
    候補を全列挙してランダム選択する形に変更。自コミュニティが候補内なら優先して振動抑制。
- (2026-05-23) コミュニティ分析を診断専用に格下げ
  - `-c` ヘルプ文を "diagnostic only — does not speed up search" に。
  - `community_analysis.hpp` / `community_analysis.cpp` / `main.cpp` のコメントに
    「診断専用、探索高速化目的では使わない」旨を明記。
- (2026-05-24) `update_community_rotation_flag` 周辺の不要コード削除
  - `Solver::update_community_rotation_flag()` 本体と宣言・呼び出しを削除。
  - `Solver::community_rotation_enabled_` メンバ削除。
  - `VariableSelector::select_restart_pivot` から `use_community_rotation` /
    `CommunityAnalysis&` パラメータと死分岐 (`abort()` 入り) を削除。
  - `select_restart_pivot` を 31 行 → 18 行にシンプル化（pool 経由を廃し、
    var_order_ を 1 パスで走査して MRV 候補を構築）。

**設計判断・理由**
- 複数ベンチマークで「コミュニティ情報を用いた探索」が VSIDS/activity ベース
  ヒューリスティクスに対して有意な改善を出せなかったため、探索ロジック側の
  コミュニティ依存を完全に剥がし、`-c` は観察ツールとして温存する方針に決定。
- Label Propagation のタイブレーク偏りは「先頭コミュニティ肥大化」の根本原因
  ではないが、ID 昇順固定のバイアスは Raghavan et al. 2007 の元定式とも乖離
  しており、診断ツールとして残す以上は素直な実装にしておくべきと判断。
- `select_restart_pivot` は MRV + activity-weighted pivot のみのフォール
  バック経路のみを残す。`restart_count` は今後グループローテーション等を
  再導入する余地のため引数シグネチャに残置 (`/*restart_count*/` 化)。

**気づいた課題・TODO**
- ベンチマーク上、コミュニティ分析が高速化に寄与しなかった理由は未深掘り。
  問題によっては Q > 0.6 級の分解可能ケースで余地が残る可能性あり (要検証だが
  優先度は低い)。
- `select_restart_pivot` の `restart_count` 引数は現状未使用 (`/*restart_count*/`)。
  将来使わないなら削除可。

**関連ファイル**
- `src/core/community_analysis.cpp`, `include/sabori_csp/community_analysis.hpp`
- `src/core/solver.cpp`, `include/sabori_csp/solver.hpp`
- `src/core/variable_selector.cpp`, `include/sabori_csp/variable_selector.hpp`
- `src/fzn/main.cpp`

### 2026-05-24 08:04 select_restart_pivot シグネチャ整理

**実施内容**
- `VariableSelector::select_restart_pivot` から未使用の `restart_count` 引数を削除。
  - `include/sabori_csp/variable_selector.hpp` の宣言、
    `src/core/variable_selector.cpp` の定義、
    `src/core/solver.cpp` の呼び出し2箇所 (restart 後 / restart-from-best 後) を更新。

**設計判断・理由**
- 直前のリファクタで `/*restart_count*/` 化していた引数。「将来グループローテーション
  等を再導入する余地」として残していたが、再導入予定が明確でない以上、未使用引数を
  シグネチャに残し続ける方が読み手を混乱させると判断し削除。
  必要になったタイミングで再度追加すれば済む。

**気づいた課題・TODO**
- なし。前ログの TODO 「`restart_count` 引数は将来使わないなら削除可」を解消。

**関連ファイル**
- `src/core/variable_selector.cpp`, `include/sabori_csp/variable_selector.hpp`
- `src/core/solver.cpp`
