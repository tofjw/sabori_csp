# 2026-06-12

## 2026-06-12 00:30 〜 08:17 作業開始〜終了

**実施内容**

- **int_lin の項順序実験** (`SABORI_LIN_ORDER` 環境変数, `lin_term_order.hpp`)
  - 初出順(mode0) / |c|降順 / |c|昇順 / |c|×幅 降順 / |c|×幅 昇順 の5モードを
    int_lin≥500 の26問で比較(並列×2 + 判別12問の逐次×1、各30秒)
  - 結論: 一貫した優位なし(mode4 が 7勝4敗だが p≈0.27 で有意でない)。
    **初出順をデフォルト維持**。実験基盤は bench_lin_order.py として残置
  - 副産物の教訓: 並列4ワーカー負荷で 30 秒時点の obj が揺れる
    (同一バイナリ・同一モードでも反転)。判別はラウンド間で再現した差のみ採用
- **conflict learning L1 続き**: solbat の説明率 20.2% → **99.4%**
  - フォールバック原因別カウンタ (`learn_fb_reasons_`、`-L -s` 時のみ収集)
  - `IntOneHotChannelConstraint::explain / explain_failure` 実装
    (verify-or-fallback、全伝播規則 + exhaustive/at-most-one)
  - 適用時 wipeout の種付け: `kSourceApplyFail` + `note_apply_fail()`。
    伝播由来の更新がドメインと矛盾した時に「矛盾相手の bound 事実 +
    発生源制約の explain」で種を構成(従来は種なしフォールバック)
  - シングルトン削除失敗は発生源制約の `explain_failure` を種に
  - try_enumerate の `last_conflict_source_` 上書きバグ修正
    (bump_activity が捕捉した衝突制約を kSourceDecision で潰していた)
  - **健全性バグ修正: backtrack で推論トレイルを eager 切り詰め**。
    lazy 切り詰めだけでは同一レベルの失敗した兄弟試行のエントリが残留し、
    bounds 単調性が壊れて死んだ枝の事実で説明を検証していた
    (resolve:DECISION / acyclic 誤判定の主因でもあった)
  - root で自明に真なリテラルを locate 前に判定(acyclic 誤判定 76k→2k)
  - 境界値除去で bounds が動いたら Geq/Leq をトレイル記録(locate 失敗解消)
  - **解決不能エントリの「降格」**: explain できないエントリは全面フォール
    バックせずリテラル自体を学習節に残す(健全・decision-trail より強い節)
- **ihtc-2024-marte「NG有効時タイムアウト」調査**(ユーザー報告)
  - デフォルトモードの UNKNOWN は **stale build の幻**: git 操作の mtime
    巻き戻りで古いオブジェクトが残留。ソース同一の worktree クリーンビルド
    + ファイル単位二分探索で特定。クリーンリビルドで 8437 に復帰
  - `-L` の劣化は本物: avg_depth 170 の深い探索で 300-400 リテラル節 ×
    O(trail) の locate/bounds_at スキャンがノードスループットを 15% 低下
  - **修正: depth cap 64**(決定数 > 64 の衝突は分析スキップ →
    従来 nogood)。`SABORI_LEARN_DEPTH_CAP` で上書き、0=無制限

**設計判断・理由**

- 項順序は「軌道のくじ引き」で系統的傾向なし → 採用せず初出順を維持
  (どの順序も勝ち負けが problem 依存で、採用には全年度ゲートが必要)
- 降格 (demote-to-literal) は resolution の途中停止に相当し常に健全。
  全面フォールバック(decision-trail 全体)より小さい節になる
- depth cap は「浅い衝突 = 説明が安く節が強い / 深い衝突 = 分析が高く
  節が弱い」の実測に基づくコスト配分。64 は cg2023(64で頭打ち)、
  cargo2018(64が最良)、marte(復活)、solbat(影響なし)から選定
- 診断マップの文字列構築は `-s` 時のみ(`set_learn_diagnostics`)で
  ホットパスから排除

**気づいた課題・TODO**

- `-L` A/B(30秒): cg2023 **6340で最適性証明**(base 18006 TIMEOUT)、
  cargo2018 -58%、p1f-pjs 516、cvrp 1634 vs 負け: marte +4%、yumidyn24。
  デフォルト有効化はまだ先(全年度ゲート要)
- yumidyn24 は cap=32 が最良(701、base 813 超え)→ cap の適応化が将来候補
  (例: trail 長 × 節長の予算ベース)
- solbat は説明率 99.4% でも 180s で UNSAT 証明に届かず → 次は L3:
  asserting clause 化 + バックジャンプ、節の最小化、demote:nogood 削減
  (nogood GC との連携: トレイルが参照中の nogood の id_map 維持)
- 計測教訓: git 操作後はクリーンリビルド必須(memory に追記済み)

**関連ファイル・コミット**

- ベース: 0b06741 (Add conflict learning L1) — 本日分は未コミット
- src/core/solver.cpp(analyze_conflict 降格/種付け/depth cap、backtrack
  切り詰め、process_queue 記録強化)
- include/sabori_csp/solver.hpp(kSourceApplyFail、ApplyFail、診断メンバ)
- src/core/constraints/global/int_one_hot_channel.cpp、
  include/sabori_csp/constraints/global.hpp(explain 実装)
- src/fzn/main.cpp(Learning 統計出力、set_learn_diagnostics)
- src/core/constraints/global/lin_term_order.hpp、
  benchmarks/minizinc_challenge/bench_lin_order.py(項順序実験基盤)
- docs-dev/work-log/2026-06-12.md(本ログ)

## 2026-06-12 08:20 〜 09:09 作業開始〜終了

**実施内容**

- **conflict learning (-L) の全年度ベンチゲート実施**(コミット ca8dcfd 時点)
  - 同一バイナリで -L あり/なしを minizinc 経由・各30秒・4並列で比較
    (2010, 2012〜2025 の 290 問、73分)
  - 基盤: bench_learning_gate.py(新規)+ /tmp の -L ラッパー msc
    (`/tmp/fzn_sabori_learn.sh` + `/tmp/sabori_csp_learn.msc`)
  - 結果: **-L wins=56 / losses=69 / ties=165 → デフォルト有効化は見送り**
  - **健全性: 学習有効時の解検証が全問題でパス**(不健全な学習節なし)
  - 結果保存: benchmarks/minizinc_challenge/20260612_learning_gate/results.txt

**設計判断・理由**

- ゲートは「今(depth cap 版)」で実施(バックジャンプ後ではなく)。
  理由: リスタート頻度が高く(solbat 1029回/60s)バックジャンプの限界
  効用は小さい / 負けの分類が L3 の設計材料になる / -L はオプトインで
  ゲートが何もブロックしない
- 大きな勝ち: fillomino TIMEOUT→**UNSAT証明1秒**、code-generator 2023
  SOL 18284→**OPTIMAL 6340**、cgt -78%、cargo2018 -65%、yumi-static 2件、
  rcpsp/zephyrus/elitserien/tsptw が SOL→OPTIMAL
- 負け 69 問の分類:
  - **純オーバーヘッド負け ~25問 (36%)**: obj/status 同一で時間だけ悪化
    (tiny-cvrp 3→27s、mario 4→23s、community-detection OPTIMAL→SOL 等)。
    衝突毎の locate/bounds_at O(trail) スキャンが衝突回数の多い高速問題で
    累積。軌道を変えずに直せる種類
  - 軌道負け ~44問: gfd-schedule、roster-shifts-bool、SOL→TIMEOUT 数件

**気づいた課題・TODO**

1. 変数ごとのトレイル索引で locate/bounds_at を O(log) 化
   (オーバーヘッド負け 25 問を直接狙う軌道不変の改善)→ 最優先
2. 節品質: 最小化、demote 削減、nogood GC との連携(id_map 維持)→ 再ゲート
3. バックジャンプは後回し(リスタート頻度から効果限定的)
4. 軌道大負けの個別調査候補: road-cons (obj 29643→49M)、
   connect (2.7M→27.7M)、roster-shifts-bool (2317→493 MAX)

**関連ファイル・コミット**

- ca8dcfd (Improve conflict learning: one_hot explanations, sound trail
  truncation, depth cap) — ゲート対象
- benchmarks/minizinc_challenge/bench_learning_gate.py(新規、未コミット)
- benchmarks/minizinc_challenge/20260612_learning_gate/results.txt

## 2026-06-12 16:00 〜 23:04 作業開始〜終了

**実施内容**

- **トレイル索引後の再ゲート**(コミット 69b99fa): -L wins=63 / losses=55 /
  ties=172 で初の勝ち越し(索引前 56/69)。OPTIMAL 108→110、UNSAT 3→4、
  TIMEOUT 同数。ユーザー判断で静的 full を `-L` 既定として確定・コミット
- **大負け問題の調査**(ユーザー依頼): 3つのメカニズムを解明
  1. **fallback 節の incumbent 毒**(road-cons/seat-moving/crosswords):
     衝突毎の decision-trail nogood が暫定解近傍の再降下と improvement
     probe を塞ぐ。base の road-cons の強さは probe 依存(-p0 で 98M に崩壊)
  2. **初回降下の説明節撹乱**(connect): わずか19節で初解が 1.8M→88M 圏に。
     無学習なら base と完全一致(オーバーヘッドは 0.7% のみ)
  3. **勝ち問題は学習が改善ドライブに必須**: cargo2018 は fallback 節が勝因、
     p1f は初解発見自体に学習が必要 → 静的ノブ(bit4/8/16/32/64, maxlen)の
     全組合せが勝ち負けのトレードオフになることを実測で確認
- **学習ポリシーの EMA バンディット実装**(mix_p と同型):
  アーム {off / explained-only / full}、エポック制8サイクル、改善幅の
  EMA 正規化報酬、fallback 節の transient 化(リスタートで消滅)、
  probe 中の nogood 伝播停止は逆効果(49→59M)で取り消し
- **バンディット2種の全年度ゲート**: decay0.5 → 54/55/181、
  decay0(直近100%、ユーザー提案)→ 55/61/174。いずれも TIMEOUT は
  22→18 と最良だが純勝ち数で静的 full (63/55) に届かず**不採用**。
  decay0 は win-stay で cg2023 の OPTIMAL を回復させる効果は確認
- 既定動作の確定: バンディット off(`SABORI_LEARN_BANDIT=1` でオプトイン)、
  off 時は RNG 消費ゼロで静的 full のゲート挙動を完全再現(road-cons
  49014972 一致で検証)。ctest 202/202

**設計判断・理由**

- バンディット不採用の理由: 探索コスト(1/3 が非最適アーム)が大勝ち問題を
  削る損失 > catastrophic 救済の利得。また road-cons 級は「probe 連鎖の
  起動に複数エポック連続のクリーン状態が必要」でアーム回転と構造的に相性が
  悪く、報酬差も出ない(連鎖が起きないと全アームが同程度の crawl になる)
- 個別問題の最適政策が真っ向から割れる(arm1 静的: road-cons◎ connect✗ /
  バンディット: connect◎ road-cons✗)ため、局所チューニングを打ち切り
  全年度ゲートの大局で判定する方針を貫いた

**気づいた課題・TODO**

- バンディットの一貫した優位は TIMEOUT 22→18 のみ → 「解なし削減」用の
  portfolio 構成要素としての将来価値はある
- 文脈付きバンディット(explained率・bisect比を特徴量に)や demote 削減に
  よる節品質向上で「full を安全化」する方向が次の候補
- 残る大負け: road-cons / connect / cargo2018(バンディットでも不可)、
  physician-scheduling、codegen2019
- 診断ノブ一覧: SABORI_LEARN_MODE(bit: 1=bumpなし 2=分析のみ 4=fallback
  学習なし 8=初解まで遅延 16=bisect除外 32=transient 64=probe除外)、
  SABORI_LEARN_ARM、SABORI_LEARN_MAXLEN、SABORI_LEARN_DEPTH_CAP、
  SABORI_LEARN_BANDIT、SABORI_LEARN_DECAY

**関連ファイル・コミット**

- 69b99fa (Improve LCG = トレイル索引) — 再ゲート 63/55 の対象
- 未コミット: バンディット+診断ノブ一式(src/core/solver.cpp、
  include/sabori_csp/solver.hpp、src/core/nogood_manager.{cpp,hpp}
  [transient 対応]、src/fzn/main.cpp [arm_cycles 統計])
- ゲート結果4種: benchmarks/minizinc_challenge/20260612_learning_gate/
  results_pre_index / results_trail_index / results_bandit /
  results_bandit_decay0
