# 2026-06-26 作業ログ

## 2026-06-26 探索コンポーネントの A/B ablation 計測と記事化

**実施内容**
- 記事 `articles/search-algorithm-explained.md`（CDCL/LCG/MiniZinc 系との違い解説）を、未計測の「独自色」主張を排し、各機能を A/B 計測で裏打ちする方針に全面改稿。
- 計測用 env トグルを実装（すべて既定動作不変）:
  - `SABORI_BUMP_MODE`(0/1/2) / `SABORI_BUMP_STRUCT_ONLY`: 制約側 activity 配分（なし/generic poor man's/構造特化、制約別切り分け）
  - `SABORI_BLOOM`: NoGood-Bloom タイブレーク
  - `SABORI_GRADIENT`: 擬似勾配ヒント（値選択）
  - `SABORI_NOGOOD` / `SABORI_NG_NOBUMP`: decision-trail NoGood（丸ごと/activity bump だけ。枝刈りは維持）
  - `SABORI_ONEHOT`: one-hot チャネル集約 presolve
  - `SABORI_SEED`: 多シード検証用の RNG シード差し替え
- 5 シード bench スクリプト 7 本を実装し、MiniZinc Challenge 2023+2024（38 問・30 秒・目的値判定）で計測。
- 計測結果（5 シード、net = 問題×シードの勝ち越し）:
  - **効く**: decision-trail NoGood（+17、全シード正）、mix_p バンディット（robustness、既存）、one-hot 集約（+6、探索量大幅減）
  - **効かない**: Bloom タイブレーク（93% no-op）、構造特化責任配分（generic と差なし、符号反転）
  - **問題依存**: 擬似勾配（平均 net−13。資源結合スケジューリングで裏目・設計/割当系で当たり＝ポートフォリオ向き）
- NoGood の +17 を分解（`SABORI_NG_NOBUMP`）: activity 寄与 +18 ＞ 純枝刈り +9。**NoGood の価値は主に activity 経由**と確定。
- 英語短縮版 `articles/poor-mans-explanation-en.md`（HN/Reddit 向け、4 章の poor man's explanation 中心）と README の hook を整備。
- main へ 2 コミットを cherry-pick（A=計測インフラ、B=記事）。-C（conflict learning, feature/ng2 の 3aff86e）は main に持ち込まず。全テスト 252/252 緑。

**設計判断・理由**
- **「測って晒す」方針**: 信頼狙い（CP/SAT 界隈）では未計測の独自性主張は逆効果。各機能を反証可能な A/B にかけ、効かないものも正直に出す方が信頼が積む。
- **記事の概念的背骨**: 計測群が「LCG は論理で無駄探索を演繹的に止める／sabori は同じ矛盾情報を主に activity に流して傾向で止める」という一線に収束。NoGood 分解（activity 寄与 +18 ＞ 枝刈り +9）がこれを直接裏付ける。タイトル「LCG と何が違うのか」への芯を食った答え。
- **「効く土台 vs 効かない精緻化」**: 効くのは activity を育てる土台（NoGood・mix_p）とモデル縮約（one-hot）。activity が先に決着するため、その上に乗せた精緻化（Bloom タイブレーク・構造特化・勾配）は出番が回らない、という統一的説明。
- **勾配＝値選択軸**: activity（変数選択）とは別軸。「得意不得意があること自体は欠点でない（並列アンサンブル前提なら多様性が資産）」という注を追加。
- **多シードの必要性**: 単一シードでは構造特化が「base に 9–6 で負け」と出たが、5 シードで差消滅（符号反転）。狭サンプルの勝敗はシードで入れ替わる罠を再確認。timing でなく目的値判定でも net 値は run 間でブレる（向きは安定）。
- **Bloom はソフト着地**: 「撤去」と断ぜず「指紋インフラ `var_ng_bloom` は残し、タイブレーク以外の用途を探すか撤去」。NG→activity が主経路だからタイブレークに出番が来ない、と機構的に説明。
- **main に -C を入れない**: feature/ng2 の conflict learning は既定オフ（-C ゲート）でベンチ未使用のため、記事の数字は main の既定挙動を正しく記述。cherry-pick 時に member 宣言の context 衝突が出たが `conflict_learning_` 行を落として解消。

**気づいた課題・TODO**
- bench スクリプトを文字列置換で生成した際、置換ターゲット文字列のミスで env が未設定のまま 1 回 Bloom を再計測した（bench_onehot.py）。**生成後は「狙った env が設定されているか」を grep 確認**してから実行すること。
- NoGood の枝刈り vs activity 寄与の分離は完了。さらに細かく「学習時 bump」と「伝播時 bump」を分けるのは未実施（伝播時 `:74/100/120` が主と推測）。
- 勾配の「効く問題群」の自動判別: 制約密度（制約/変数比）では分離できず、問題種別（資源結合スケジューリング）で観察的に分離。より良い構造指標は future work。
- one-hot 集約の効果は 30 秒・目的値判定だと過小評価（探索量は大幅減でも同目的値で tie）。time-to-solution 指標なら差が出るはず。
- push は未実施（ユーザーが実行）。

**関連ファイル・コミット**
- main: `3604855`（A: トグル + bench 7 本）, `7223275`（B: 記事 3 点）
- feature/ng2: `6c0d315`（A）, `75b2a89`（B）。`3aff86e`（-C）は main 未反映
- 記事: `articles/search-algorithm-explained.md`, `articles/poor-mans-explanation-en.md`, `README.md`
- 実装: `include/sabori_csp/solver.hpp`, `src/core/solver.cpp`, `src/core/solver_frame.cpp`, `include/sabori_csp/nogood_manager.hpp`, `src/core/nogood_manager.cpp`
- bench: `benchmarks/minizinc_challenge/bench_{bump,bump_perconstraint,bloom,gradient,nogood,onehot,ng_split}.py`
