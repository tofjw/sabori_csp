## 2026-07-11 フラグ purge 第1弾 (feature/more_constraint 分)

**実施内容**
- タグ `pre-flag-purge` を 2821fe1 に作成
- SABORI_NG_LEARN_BUMP / SABORI_NG_PROP_BUMP の env 読み取りと
  NoGoodManager の個別 setter を削除（SABORI_NG_NOBUMP は温存）
- SABORI_BUMP_STRUCT_ONLY を structural_mask 機構ごと削除
  （SABORI_BUMP_MODE は温存）
- SABORI_RESTART_POLICY を adaptive / scrambled / luby の3種に縮小
  （inverted/prune_only/depth_only/always_*/geometric/constant を
  enum・テストごと削除。Adaptive は既定 enum 値=出荷経路なので温存）
- SABORI_CLAUSE_WITNESS_PLAIN を削除し witness を plain 意味論に一本化
  （ユーザ選択。min 意味論のコードを撤去、-a では解が重複するため
  コメントで単解/最適化専用と明記）

**設計判断・理由**
- env-flags-inventory.md の「mp 統合時の整理方針」の削除リストを実施
- witness は mp arm 用途（探索系ワーカー・単解/最適化）の実測勝ち形が
  plain (1WL A/B net pl8+3/pl4+3) のため plain を採用
- PROMOTE_DEF_BOOL / BOUND_EXPL は feature/mp のみ → mp 統合時に削除

**検証**
- フルビルド警告なし、ctest 293/293 pass、golden 260/260 green
- SABORI_CLAUSE_WITNESS=2 スモークテスト（bool_clause 系 fzn 6件）で
  base と同一解

**気づいた課題・TODO**
- ブロックコメント内の `always_*/` が `*/` としてコメントを閉じる罠を踏んだ
- feature/mp 側の削除（PROMOTE_DEF_BOOL / BOUND_EXPL）が残タスク

**関連ファイル・コミット**
- src/core/solver.cpp, include/sabori_csp/{solver,nogood_manager,restart_controller}.hpp
- src/core/constraints/logical.cpp, include/sabori_csp/constraints/logical.hpp
- src/fzn/constraint_registry.cpp, tests/cpp/test_restart_controller.cpp
- タグ: pre-flag-purge (2821fe1)
