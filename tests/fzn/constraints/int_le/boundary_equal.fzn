% Boundary: x == y satisfies int_le (<=), unlike int_lt. Pin x=y=3.
var 1..5: x :: output_var;
var 1..5: y :: output_var;
constraint int_le(x, y);
constraint int_eq(x, 3);
constraint int_eq(y, 3);
solve satisfy;
