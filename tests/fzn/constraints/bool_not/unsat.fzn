% UNSAT: bool_not(a,b) forces a != b, but both pinned to 1.
var bool: a :: output_var;
var bool: b :: output_var;

constraint bool_not(a, b);
constraint int_eq(a, 1);
constraint int_eq(b, 1);

solve satisfy;
