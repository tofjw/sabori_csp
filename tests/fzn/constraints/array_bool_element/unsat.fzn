% UNSAT: arr=[1,0,1,0], idx=2 selects arr[2]=0, but result is pinned to 1.
var 1..4: idx :: output_var;
var 0..1: result :: output_var;
array [1..4] of int: arr = [1, 0, 1, 0];
constraint array_bool_element(idx, arr, result);
constraint int_eq(idx, 2);
constraint int_eq(result, 1);
solve satisfy;
