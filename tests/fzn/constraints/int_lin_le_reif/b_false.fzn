% int_lin_le_reif test: b = false, so constraint must NOT hold
% 2*x + 3*y <= 5, with b=false means 2*x + 3*y > 5
var 1..5: x :: output_var;
var 1..5: y :: output_var;
var bool: b = false;
constraint int_lin_le_reif([2, 3], [x, y], 5, b);
solve satisfy;
