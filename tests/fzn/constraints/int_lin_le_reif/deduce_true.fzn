% int_lin_le_reif test: constraint always holds, so b must be true
% 1*x <= 10, where x in 1..5, always holds
var 1..5: x :: output_var;
var bool: b :: output_var;
constraint int_lin_le_reif([1], [x], 10, b);
solve satisfy;
